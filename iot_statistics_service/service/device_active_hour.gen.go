// Code generated by sgen.exe,2022-08-06 09:32:12. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_statistics/model"
	"cloud_platform/iot_model/db_statistics/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
	"cloud_platform/iot_statistics_service/convert"
)

type DeviceActiveHourSvc struct {
	Ctx context.Context
}

// 创建DeviceActiveHour
func (s *DeviceActiveHourSvc) CreateDeviceActiveHour(req *proto.DeviceActiveHour) (*proto.DeviceActiveHour, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TDeviceActiveHour
	do := t.WithContext(context.Background())
	dbObj := convert.DeviceActiveHour_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateDeviceActiveHour error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除DeviceActiveHour
func (s *DeviceActiveHourSvc) DeleteDeviceActiveHour(req *proto.DeviceActiveHour) (*proto.DeviceActiveHour, error) {
	t := orm.Use(iotmodel.GetDB()).TDeviceActiveHour
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.TenantId != "" { //字符串
		do = do.Where(t.TenantId.Eq(req.TenantId))
	}
	if req.ProductKey != "" { //字符串
		do = do.Where(t.ProductKey.Eq(req.ProductKey))
	}
	if req.ActiveSum != 0 { //整数
		do = do.Where(t.ActiveSum.Eq(req.ActiveSum))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteDeviceActiveHour error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除DeviceActiveHour
func (s *DeviceActiveHourSvc) DeleteByIdDeviceActiveHour(req *proto.DeviceActiveHour) (*proto.DeviceActiveHour, error) {
	t := orm.Use(iotmodel.GetDB()).TDeviceActiveHour
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.TenantId != "" { //字符串
		do = do.Where(t.TenantId.Eq(req.TenantId))
	}
	if req.ProductKey != "" { //字符串
		do = do.Where(t.ProductKey.Eq(req.ProductKey))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdDeviceActiveHour error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除DeviceActiveHour
func (s *DeviceActiveHourSvc) DeleteByIdsDeviceActiveHour(req *proto.DeviceActiveHourBatchDeleteRequest) (*proto.DeviceActiveHourBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TDeviceActiveHour
		do := t.WithContext(context.Background())

		do = do.Where(t.DataTime.Eq(k.DataTime.AsTime()))

		do = do.Where(t.TenantId.Eq(k.TenantId))

		do = do.Where(t.ProductKey.Eq(k.ProductKey))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsDeviceActiveHour error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新DeviceActiveHour
func (s *DeviceActiveHourSvc) UpdateDeviceActiveHour(req *proto.DeviceActiveHour) (*proto.DeviceActiveHour, error) {
	t := orm.Use(iotmodel.GetDB()).TDeviceActiveHour
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.ActiveSum != 0 { //整数
		updateField = append(updateField, t.ActiveSum)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.TenantId != "" { //字符串
		do = do.Where(t.TenantId.Eq(req.TenantId))
		HasPrimaryKey = true
	}
	if req.ProductKey != "" { //字符串
		do = do.Where(t.ProductKey.Eq(req.ProductKey))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateDeviceActiveHour error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.DeviceActiveHour_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateDeviceActiveHour error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段DeviceActiveHour
func (s *DeviceActiveHourSvc) UpdateAllDeviceActiveHour(req *proto.DeviceActiveHour) (*proto.DeviceActiveHour, error) {
	t := orm.Use(iotmodel.GetDB()).TDeviceActiveHour
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.ActiveSum)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.TenantId != "" { //字符串
		do = do.Where(t.TenantId.Eq(req.TenantId))
		HasPrimaryKey = true
	}
	if req.ProductKey != "" { //字符串
		do = do.Where(t.ProductKey.Eq(req.ProductKey))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllDeviceActiveHour error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.DeviceActiveHour_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllDeviceActiveHour error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *DeviceActiveHourSvc) UpdateFieldsDeviceActiveHour(req *proto.DeviceActiveHourUpdateFieldsRequest) (*proto.DeviceActiveHour, error) {
	t := orm.Use(iotmodel.GetDB()).TDeviceActiveHour
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsDeviceActiveHour error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.TenantId != "" { //字符串
		do = do.Where(t.TenantId.Eq(req.Data.TenantId))
		HasPrimaryKey = true
	}
	if req.Data.ProductKey != "" { //字符串
		do = do.Where(t.ProductKey.Eq(req.Data.ProductKey))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsDeviceActiveHour error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.DeviceActiveHour_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsDeviceActiveHour error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找DeviceActiveHour
func (s *DeviceActiveHourSvc) FindDeviceActiveHour(req *proto.DeviceActiveHourFilter) (*proto.DeviceActiveHour, error) {
	t := orm.Use(iotmodel.GetDB()).TDeviceActiveHour
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.TenantId != "" { //字符串
		do = do.Where(t.TenantId.Eq(req.TenantId))
	}
	if req.ProductKey != "" { //字符串
		do = do.Where(t.ProductKey.Eq(req.ProductKey))
	}
	if req.ActiveSum != 0 { //整数
		do = do.Where(t.ActiveSum.Eq(req.ActiveSum))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindDeviceActiveHour error : %s", err.Error())
		return nil, err
	}
	res := convert.DeviceActiveHour_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找DeviceActiveHour
func (s *DeviceActiveHourSvc) FindByIdDeviceActiveHour(req *proto.DeviceActiveHourFilter) (*proto.DeviceActiveHour, error) {
	t := orm.Use(iotmodel.GetDB()).TDeviceActiveHour
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.TenantId != "" { //字符串
		do = do.Where(t.TenantId.Eq(req.TenantId))
	}
	if req.ProductKey != "" { //字符串
		do = do.Where(t.ProductKey.Eq(req.ProductKey))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdDeviceActiveHour error : %s", err.Error())
		return nil, err
	}
	res := convert.DeviceActiveHour_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找DeviceActiveHour,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *DeviceActiveHourSvc) GetListDeviceActiveHour(req *proto.DeviceActiveHourListRequest) ([]*proto.DeviceActiveHour, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TDeviceActiveHour
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {
		if query.TenantId != "" { //字符串
			do = do.Where(t.TenantId.Like("%" + query.TenantId + "%"))
		}
		if query.ProductKey != "" { //字符串
			do = do.Where(t.ProductKey.Like("%" + query.ProductKey + "%"))
		}
		if query.DataTimeStart != nil {
			do = do.Where(t.DataTime.Gte(query.DataTimeStart.AsTime()))
		}
		if query.DataTimeEnd != nil {
			do = do.Where(t.DataTime.Lt(query.DataTimeEnd.AsTime()))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.DataTime
		orderCol = t.TenantId
		orderCol = t.ProductKey
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TDeviceActiveHour
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListDeviceActiveHour error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.DeviceActiveHour, len(list))
	for i, v := range list {
		result[i] = convert.DeviceActiveHour_db2pb(v)
	}
	return result, total, nil
}
