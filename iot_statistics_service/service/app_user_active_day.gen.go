// Code generated by sgen.exe,2022-08-10 22:27:08. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_statistics/model"
	"cloud_platform/iot_model/db_statistics/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
	"cloud_platform/iot_statistics_service/convert"
)

type AppUserActiveDaySvc struct {
	Ctx context.Context
}

// 创建AppUserActiveDay
func (s *AppUserActiveDaySvc) CreateAppUserActiveDay(req *proto.AppUserActiveDay) (*proto.AppUserActiveDay, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TAppUserActiveDay
	do := t.WithContext(context.Background())
	dbObj := convert.AppUserActiveDay_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateAppUserActiveDay error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除AppUserActiveDay
func (s *AppUserActiveDaySvc) DeleteAppUserActiveDay(req *proto.AppUserActiveDay) (*proto.AppUserActiveDay, error) {
	t := orm.Use(iotmodel.GetDB()).TAppUserActiveDay
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.TenantId != "" { //字符串
		do = do.Where(t.TenantId.Eq(req.TenantId))
	}
	if req.AppKey != "" { //字符串
		do = do.Where(t.AppKey.Eq(req.AppKey))
	}
	if req.ActiveSum != 0 { //整数
		do = do.Where(t.ActiveSum.Eq(req.ActiveSum))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteAppUserActiveDay error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除AppUserActiveDay
func (s *AppUserActiveDaySvc) DeleteByIdAppUserActiveDay(req *proto.AppUserActiveDay) (*proto.AppUserActiveDay, error) {
	t := orm.Use(iotmodel.GetDB()).TAppUserActiveDay
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.TenantId != "" { //字符串
		do = do.Where(t.TenantId.Eq(req.TenantId))
	}
	if req.AppKey != "" { //字符串
		do = do.Where(t.AppKey.Eq(req.AppKey))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdAppUserActiveDay error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除AppUserActiveDay
func (s *AppUserActiveDaySvc) DeleteByIdsAppUserActiveDay(req *proto.AppUserActiveDayBatchDeleteRequest) (*proto.AppUserActiveDayBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TAppUserActiveDay
		do := t.WithContext(context.Background())

		do = do.Where(t.DataTime.Eq(k.DataTime.AsTime()))

		do = do.Where(t.TenantId.Eq(k.TenantId))

		do = do.Where(t.AppKey.Eq(k.AppKey))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsAppUserActiveDay error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新AppUserActiveDay
func (s *AppUserActiveDaySvc) UpdateAppUserActiveDay(req *proto.AppUserActiveDay) (*proto.AppUserActiveDay, error) {
	t := orm.Use(iotmodel.GetDB()).TAppUserActiveDay
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.ActiveSum != 0 { //整数
		updateField = append(updateField, t.ActiveSum)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.TenantId != "" { //字符串
		do = do.Where(t.TenantId.Eq(req.TenantId))
		HasPrimaryKey = true
	}
	if req.AppKey != "" { //字符串
		do = do.Where(t.AppKey.Eq(req.AppKey))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateAppUserActiveDay error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.AppUserActiveDay_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAppUserActiveDay error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段AppUserActiveDay
func (s *AppUserActiveDaySvc) UpdateAllAppUserActiveDay(req *proto.AppUserActiveDay) (*proto.AppUserActiveDay, error) {
	t := orm.Use(iotmodel.GetDB()).TAppUserActiveDay
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.ActiveSum)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.TenantId != "" { //字符串
		do = do.Where(t.TenantId.Eq(req.TenantId))
		HasPrimaryKey = true
	}
	if req.AppKey != "" { //字符串
		do = do.Where(t.AppKey.Eq(req.AppKey))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllAppUserActiveDay error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.AppUserActiveDay_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllAppUserActiveDay error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *AppUserActiveDaySvc) UpdateFieldsAppUserActiveDay(req *proto.AppUserActiveDayUpdateFieldsRequest) (*proto.AppUserActiveDay, error) {
	t := orm.Use(iotmodel.GetDB()).TAppUserActiveDay
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsAppUserActiveDay error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.TenantId != "" { //字符串
		do = do.Where(t.TenantId.Eq(req.Data.TenantId))
		HasPrimaryKey = true
	}
	if req.Data.AppKey != "" { //字符串
		do = do.Where(t.AppKey.Eq(req.Data.AppKey))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsAppUserActiveDay error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.AppUserActiveDay_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsAppUserActiveDay error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找AppUserActiveDay
func (s *AppUserActiveDaySvc) FindAppUserActiveDay(req *proto.AppUserActiveDayFilter) (*proto.AppUserActiveDay, error) {
	t := orm.Use(iotmodel.GetDB()).TAppUserActiveDay
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.TenantId != "" { //字符串
		do = do.Where(t.TenantId.Eq(req.TenantId))
	}
	if req.AppKey != "" { //字符串
		do = do.Where(t.AppKey.Eq(req.AppKey))
	}
	if req.ActiveSum != 0 { //整数
		do = do.Where(t.ActiveSum.Eq(req.ActiveSum))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindAppUserActiveDay error : %s", err.Error())
		return nil, err
	}
	res := convert.AppUserActiveDay_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找AppUserActiveDay
func (s *AppUserActiveDaySvc) FindByIdAppUserActiveDay(req *proto.AppUserActiveDayFilter) (*proto.AppUserActiveDay, error) {
	t := orm.Use(iotmodel.GetDB()).TAppUserActiveDay
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.TenantId != "" { //字符串
		do = do.Where(t.TenantId.Eq(req.TenantId))
	}
	if req.AppKey != "" { //字符串
		do = do.Where(t.AppKey.Eq(req.AppKey))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdAppUserActiveDay error : %s", err.Error())
		return nil, err
	}
	res := convert.AppUserActiveDay_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找AppUserActiveDay,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *AppUserActiveDaySvc) GetListAppUserActiveDay(req *proto.AppUserActiveDayListRequest) ([]*proto.AppUserActiveDay, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TAppUserActiveDay
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.TenantId != "" { //字符串
			do = do.Where(t.TenantId.Like("%" + query.TenantId + "%"))
		}
		if query.AppKey != "" { //字符串
			do = do.Where(t.AppKey.Like("%" + query.AppKey + "%"))
		}
		if query.ActiveSum != 0 { //整数
			do = do.Where(t.ActiveSum.Eq(query.ActiveSum))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.DataTime
		orderCol = t.TenantId
		orderCol = t.AppKey
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TAppUserActiveDay
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListAppUserActiveDay error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.AppUserActiveDay, len(list))
	for i, v := range list {
		result[i] = convert.AppUserActiveDay_db2pb(v)
	}
	return result, total, nil
}
