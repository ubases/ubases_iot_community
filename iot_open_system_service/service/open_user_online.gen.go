// Code generated by sgen.exe,2022-04-27 10:55:26. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"cloud_platform/iot_common/iotutil"
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_open_system/model"
	"cloud_platform/iot_model/db_open_system/orm"
	"cloud_platform/iot_open_system_service/convert"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type OpenUserOnlineSvc struct {
	Ctx context.Context
}

// 创建OpenUserOnline
func (s *OpenUserOnlineSvc) CreateOpenUserOnline(req *proto.OpenUserOnline) (*proto.OpenUserOnline, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TOpenUserOnline
	do := t.WithContext(context.Background())
	dbObj := convert.OpenUserOnline_pb2db(req)
	dbObj.Id = iotutil.GetNextSeqInt64()
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateOpenUserOnline error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除OpenUserOnline
func (s *OpenUserOnlineSvc) DeleteOpenUserOnline(req *proto.OpenUserOnline) (*proto.OpenUserOnline, error) {
	t := orm.Use(iotmodel.GetDB()).TOpenUserOnline
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.Uuid != "" { //字符串
		do = do.Where(t.Uuid.Eq(req.Uuid))
	}
	if req.Token != "" { //字符串
		do = do.Where(t.Token.Eq(req.Token))
	}
	if req.UserName != "" { //字符串
		do = do.Where(t.UserName.Eq(req.UserName))
	}
	if req.Ip != "" { //字符串
		do = do.Where(t.Ip.Eq(req.Ip))
	}
	if req.Explorer != "" { //字符串
		do = do.Where(t.Explorer.Eq(req.Explorer))
	}
	if req.Os != "" { //字符串
		do = do.Where(t.Os.Eq(req.Os))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteOpenUserOnline error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除OpenUserOnline
func (s *OpenUserOnlineSvc) DeleteByIdOpenUserOnline(req *proto.OpenUserOnline) (*proto.OpenUserOnline, error) {
	t := orm.Use(iotmodel.GetDB()).TOpenUserOnline
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdOpenUserOnline error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除OpenUserOnline
func (s *OpenUserOnlineSvc) DeleteByIdsOpenUserOnline(req *proto.OpenUserOnlineBatchDeleteRequest) (*proto.OpenUserOnlineBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TOpenUserOnline
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsOpenUserOnline error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新OpenUserOnline
func (s *OpenUserOnlineSvc) UpdateOpenUserOnline(req *proto.OpenUserOnline) (*proto.OpenUserOnline, error) {
	t := orm.Use(iotmodel.GetDB()).TOpenUserOnline
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.Uuid != "" { //字符串
		updateField = append(updateField, t.Uuid)
	}
	if req.Token != "" { //字符串
		updateField = append(updateField, t.Token)
	}
	if req.UserName != "" { //字符串
		updateField = append(updateField, t.UserName)
	}
	if req.Ip != "" { //字符串
		updateField = append(updateField, t.Ip)
	}
	if req.Explorer != "" { //字符串
		updateField = append(updateField, t.Explorer)
	}
	if req.Os != "" { //字符串
		updateField = append(updateField, t.Os)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateOpenUserOnline error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.OpenUserOnline_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateOpenUserOnline error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段OpenUserOnline
func (s *OpenUserOnlineSvc) UpdateAllOpenUserOnline(req *proto.OpenUserOnline) (*proto.OpenUserOnline, error) {
	t := orm.Use(iotmodel.GetDB()).TOpenUserOnline
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.Uuid)
	updateField = append(updateField, t.Token)
	updateField = append(updateField, t.CreateTime)
	updateField = append(updateField, t.UserName)
	updateField = append(updateField, t.Ip)
	updateField = append(updateField, t.Explorer)
	updateField = append(updateField, t.Os)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllOpenUserOnline error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OpenUserOnline_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllOpenUserOnline error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *OpenUserOnlineSvc) UpdateFieldsOpenUserOnline(req *proto.OpenUserOnlineUpdateFieldsRequest) (*proto.OpenUserOnline, error) {
	t := orm.Use(iotmodel.GetDB()).TOpenUserOnline
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsOpenUserOnline error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsOpenUserOnline error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OpenUserOnline_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsOpenUserOnline error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找OpenUserOnline
func (s *OpenUserOnlineSvc) FindOpenUserOnline(req *proto.OpenUserOnlineFilter) (*proto.OpenUserOnline, error) {
	t := orm.Use(iotmodel.GetDB()).TOpenUserOnline
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.Uuid != "" { //字符串
		do = do.Where(t.Uuid.Eq(req.Uuid))
	}
	if req.Token != "" { //字符串
		do = do.Where(t.Token.Eq(req.Token))
	}
	if req.UserName != "" { //字符串
		do = do.Where(t.UserName.Eq(req.UserName))
	}
	if req.Ip != "" { //字符串
		do = do.Where(t.Ip.Eq(req.Ip))
	}
	if req.Explorer != "" { //字符串
		do = do.Where(t.Explorer.Eq(req.Explorer))
	}
	if req.Os != "" { //字符串
		do = do.Where(t.Os.Eq(req.Os))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindOpenUserOnline error : %s", err.Error())
		return nil, err
	}
	res := convert.OpenUserOnline_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找OpenUserOnline
func (s *OpenUserOnlineSvc) FindByIdOpenUserOnline(req *proto.OpenUserOnlineFilter) (*proto.OpenUserOnline, error) {
	t := orm.Use(iotmodel.GetDB()).TOpenUserOnline
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdOpenUserOnline error : %s", err.Error())
		return nil, err
	}
	res := convert.OpenUserOnline_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找OpenUserOnline,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *OpenUserOnlineSvc) GetListOpenUserOnline(req *proto.OpenUserOnlineListRequest) ([]*proto.OpenUserOnline, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TOpenUserOnline
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.Uuid != "" { //字符串
			do = do.Where(t.Uuid.Like("%" + query.Uuid + "%"))
		}
		if query.Token != "" { //字符串
			do = do.Where(t.Token.Like("%" + query.Token + "%"))
		}
		if query.UserName != "" { //字符串
			do = do.Where(t.UserName.Like("%" + query.UserName + "%"))
		}
		if query.Ip != "" { //字符串
			do = do.Where(t.Ip.Like("%" + query.Ip + "%"))
		}
		if query.Explorer != "" { //字符串
			do = do.Where(t.Explorer.Like("%" + query.Explorer + "%"))
		}
		if query.Os != "" { //字符串
			do = do.Where(t.Os.Like("%" + query.Os + "%"))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TOpenUserOnline
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListOpenUserOnline error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.OpenUserOnline, len(list))
	for i, v := range list {
		result[i] = convert.OpenUserOnline_db2pb(v)
	}
	return result, total, nil
}

func (s *OpenUserOnlineSvc) GetOpenUserOnlines(userId string) ([]*model.TOpenUserOnline, error) {
	if userId == "" {
		return nil, errors.New("userId is empty")
	}
	var err error
	t := orm.Use(iotmodel.GetDB()).TOpenUserOnline
	do := t.WithContext(context.Background()).Where(t.Uuid.Eq(userId))

	list, err := do.Find()
	if err != nil {
		logger.Errorf("GetOpenUserOnlines error : %s", err.Error())
		return nil, err
	}
	return list, nil
}
