// Code generated by sgen.exe,2022-05-13 10:42:25. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_open_system/model"
	"cloud_platform/iot_model/db_open_system/orm"
	"cloud_platform/iot_open_system_service/convert"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type OpenCompanyAuthLogsSvc struct {
	Ctx context.Context
}

// 创建OpenCompanyAuthLogs
func (s *OpenCompanyAuthLogsSvc) CreateOpenCompanyAuthLogs(req *proto.OpenCompanyAuthLogs) (*proto.OpenCompanyAuthLogs, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TOpenCompanyAuthLogs
	do := t.WithContext(context.Background())
	dbObj := convert.OpenCompanyAuthLogs_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateOpenCompanyAuthLogs error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除OpenCompanyAuthLogs
func (s *OpenCompanyAuthLogsSvc) DeleteOpenCompanyAuthLogs(req *proto.OpenCompanyAuthLogs) (*proto.OpenCompanyAuthLogs, error) {
	t := orm.Use(iotmodel.GetDB()).TOpenCompanyAuthLogs
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.CompanyId != 0 { //整数
		do = do.Where(t.CompanyId.Eq(req.CompanyId))
	}
	if req.AuthResult != "" { //字符串
		do = do.Where(t.AuthResult.Eq(req.AuthResult))
	}
	if req.AuthName != "" { //字符串
		do = do.Where(t.AuthName.Eq(req.AuthName))
	}
	if req.Why != "" { //字符串
		do = do.Where(t.Why.Eq(req.Why))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteOpenCompanyAuthLogs error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除OpenCompanyAuthLogs
func (s *OpenCompanyAuthLogsSvc) DeleteByIdOpenCompanyAuthLogs(req *proto.OpenCompanyAuthLogs) (*proto.OpenCompanyAuthLogs, error) {
	t := orm.Use(iotmodel.GetDB()).TOpenCompanyAuthLogs
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdOpenCompanyAuthLogs error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除OpenCompanyAuthLogs
func (s *OpenCompanyAuthLogsSvc) DeleteByIdsOpenCompanyAuthLogs(req *proto.OpenCompanyAuthLogsBatchDeleteRequest) (*proto.OpenCompanyAuthLogsBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TOpenCompanyAuthLogs
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsOpenCompanyAuthLogs error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新OpenCompanyAuthLogs
func (s *OpenCompanyAuthLogsSvc) UpdateOpenCompanyAuthLogs(req *proto.OpenCompanyAuthLogs) (*proto.OpenCompanyAuthLogs, error) {
	t := orm.Use(iotmodel.GetDB()).TOpenCompanyAuthLogs
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.CompanyId != 0 { //整数
		updateField = append(updateField, t.CompanyId)
	}
	if req.AuthResult != "" { //字符串
		updateField = append(updateField, t.AuthResult)
	}
	if req.AuthName != "" { //字符串
		updateField = append(updateField, t.AuthName)
	}
	if req.Why != "" { //字符串
		updateField = append(updateField, t.Why)
	}
	if req.CreatedBy != 0 { //整数
		updateField = append(updateField, t.CreatedBy)
	}
	if req.UpdatedBy != 0 { //整数
		updateField = append(updateField, t.UpdatedBy)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateOpenCompanyAuthLogs error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.OpenCompanyAuthLogs_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateOpenCompanyAuthLogs error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段OpenCompanyAuthLogs
func (s *OpenCompanyAuthLogsSvc) UpdateAllOpenCompanyAuthLogs(req *proto.OpenCompanyAuthLogs) (*proto.OpenCompanyAuthLogs, error) {
	t := orm.Use(iotmodel.GetDB()).TOpenCompanyAuthLogs
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.CompanyId)
	updateField = append(updateField, t.AuthResult)
	updateField = append(updateField, t.AuthName)
	updateField = append(updateField, t.AuthDate)
	updateField = append(updateField, t.Why)
	updateField = append(updateField, t.CreatedBy)
	updateField = append(updateField, t.UpdatedBy)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllOpenCompanyAuthLogs error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OpenCompanyAuthLogs_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllOpenCompanyAuthLogs error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *OpenCompanyAuthLogsSvc) UpdateFieldsOpenCompanyAuthLogs(req *proto.OpenCompanyAuthLogsUpdateFieldsRequest) (*proto.OpenCompanyAuthLogs, error) {
	t := orm.Use(iotmodel.GetDB()).TOpenCompanyAuthLogs
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsOpenCompanyAuthLogs error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsOpenCompanyAuthLogs error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OpenCompanyAuthLogs_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsOpenCompanyAuthLogs error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找OpenCompanyAuthLogs
func (s *OpenCompanyAuthLogsSvc) FindOpenCompanyAuthLogs(req *proto.OpenCompanyAuthLogsFilter) (*proto.OpenCompanyAuthLogs, error) {
	t := orm.Use(iotmodel.GetDB()).TOpenCompanyAuthLogs
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.CompanyId != 0 { //整数
		do = do.Where(t.CompanyId.Eq(req.CompanyId))
	}
	if req.AuthResult != "" { //字符串
		do = do.Where(t.AuthResult.Eq(req.AuthResult))
	}
	if req.AuthName != "" { //字符串
		do = do.Where(t.AuthName.Eq(req.AuthName))
	}
	if req.Why != "" { //字符串
		do = do.Where(t.Why.Eq(req.Why))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindOpenCompanyAuthLogs error : %s", err.Error())
		return nil, err
	}
	res := convert.OpenCompanyAuthLogs_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找OpenCompanyAuthLogs
func (s *OpenCompanyAuthLogsSvc) FindByIdOpenCompanyAuthLogs(req *proto.OpenCompanyAuthLogsFilter) (*proto.OpenCompanyAuthLogs, error) {
	t := orm.Use(iotmodel.GetDB()).TOpenCompanyAuthLogs
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdOpenCompanyAuthLogs error : %s", err.Error())
		return nil, err
	}
	res := convert.OpenCompanyAuthLogs_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找OpenCompanyAuthLogs,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *OpenCompanyAuthLogsSvc) GetListOpenCompanyAuthLogs(req *proto.OpenCompanyAuthLogsListRequest) ([]*proto.OpenCompanyAuthLogs, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TOpenCompanyAuthLogs
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.CompanyId != 0 { //整数
			do = do.Where(t.CompanyId.Eq(query.CompanyId))
		}
		if query.AuthResult != "" { //字符串
			do = do.Where(t.AuthResult.Like("%" + query.AuthResult + "%"))
		}
		if query.AuthName != "" { //字符串
			do = do.Where(t.AuthName.Like("%" + query.AuthName + "%"))
		}
		if query.Why != "" { //字符串
			do = do.Where(t.Why.Like("%" + query.Why + "%"))
		}
		if query.CreatedBy != 0 { //整数
			do = do.Where(t.CreatedBy.Eq(query.CreatedBy))
		}
		if query.UpdatedBy != 0 { //整数
			do = do.Where(t.UpdatedBy.Eq(query.UpdatedBy))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.CreatedAt
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TOpenCompanyAuthLogs
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListOpenCompanyAuthLogs error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.OpenCompanyAuthLogs, len(list))
	for i, v := range list {
		result[i] = convert.OpenCompanyAuthLogs_db2pb(v)
	}
	return result, total, nil
}
