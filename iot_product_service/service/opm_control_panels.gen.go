// Code generated by sgen.exe,2022-05-06 14:01:20. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_product/model"
	"cloud_platform/iot_model/db_product/orm"
	"cloud_platform/iot_product_service/convert"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type OpmControlPanelsSvc struct {
	Ctx context.Context
}

// 创建OpmControlPanels
func (s *OpmControlPanelsSvc) CreateOpmControlPanels(req *proto.OpmControlPanels) (*proto.OpmControlPanels, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TOpmControlPanels
	do := t.WithContext(context.Background())
	dbObj := convert.OpmControlPanels_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateOpmControlPanels error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除OpmControlPanels
func (s *OpmControlPanelsSvc) DeleteOpmControlPanels(req *proto.OpmControlPanels) (*proto.OpmControlPanels, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmControlPanels
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.Name != "" { //字符串
		do = do.Where(t.Name.Eq(req.Name))
	}
	if req.NameEn != "" { //字符串
		do = do.Where(t.NameEn.Eq(req.NameEn))
	}
	if req.Lang != "" { //字符串
		do = do.Where(t.Lang.Eq(req.Lang))
	}
	if req.Desc != "" { //字符串
		do = do.Where(t.Desc.Eq(req.Desc))
	}
	if req.Url != "" { //字符串
		do = do.Where(t.Url.Eq(req.Url))
	}
	if req.UrlName != "" { //字符串
		do = do.Where(t.UrlName.Eq(req.UrlName))
	}
	if req.PanelSize != 0 { //整数
		do = do.Where(t.PanelSize.Eq(req.PanelSize))
	}
	if req.PreviewName != "" { //字符串
		do = do.Where(t.PreviewName.Eq(req.PreviewName))
	}
	if req.PreviewUrl != "" { //字符串
		do = do.Where(t.PreviewUrl.Eq(req.PreviewUrl))
	}
	if req.PreviewSize != 0 { //整数
		do = do.Where(t.PreviewSize.Eq(req.PreviewSize))
	}
	if req.ProductTypeId != 0 { //整数
		do = do.Where(t.ProductTypeId.Eq(req.ProductTypeId))
	}
	if req.ProductId != 0 { //整数
		do = do.Where(t.ProductId.Eq(req.ProductId))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	if req.TenantId != "" { //字符串
		do = do.Where(t.TenantId.Eq(req.TenantId))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteOpmControlPanels error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除OpmControlPanels
func (s *OpmControlPanelsSvc) DeleteByIdOpmControlPanels(req *proto.OpmControlPanels) (*proto.OpmControlPanels, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmControlPanels
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdOpmControlPanels error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除OpmControlPanels
func (s *OpmControlPanelsSvc) DeleteByIdsOpmControlPanels(req *proto.OpmControlPanelsBatchDeleteRequest) (*proto.OpmControlPanelsBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TOpmControlPanels
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsOpmControlPanels error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新OpmControlPanels
func (s *OpmControlPanelsSvc) UpdateOpmControlPanels(req *proto.OpmControlPanels) (*proto.OpmControlPanels, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmControlPanels
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.Name != "" { //字符串
		updateField = append(updateField, t.Name)
	}
	if req.NameEn != "" { //字符串
		updateField = append(updateField, t.NameEn)
	}
	if req.Lang != "" { //字符串
		updateField = append(updateField, t.Lang)
	}
	if req.Desc != "" { //字符串
		updateField = append(updateField, t.Desc)
	}
	if req.Url != "" { //字符串
		updateField = append(updateField, t.Url)
	}
	if req.UrlName != "" { //字符串
		updateField = append(updateField, t.UrlName)
	}
	if req.PanelSize != 0 { //整数
		updateField = append(updateField, t.PanelSize)
	}
	if req.PreviewName != "" { //字符串
		updateField = append(updateField, t.PreviewName)
	}
	if req.PreviewUrl != "" { //字符串
		updateField = append(updateField, t.PreviewUrl)
	}
	if req.PreviewSize != 0 { //整数
		updateField = append(updateField, t.PreviewSize)
	}
	if req.ProductTypeId != 0 { //整数
		updateField = append(updateField, t.ProductTypeId)
	}
	if req.ProductId != 0 { //整数
		updateField = append(updateField, t.ProductId)
	}
	if req.CreatedBy != 0 { //整数
		updateField = append(updateField, t.CreatedBy)
	}
	if req.UpdatedBy != 0 { //整数
		updateField = append(updateField, t.UpdatedBy)
	}
	if req.TenantId != "" { //字符串
		updateField = append(updateField, t.TenantId)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateOpmControlPanels error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.OpmControlPanels_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateOpmControlPanels error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段OpmControlPanels
func (s *OpmControlPanelsSvc) UpdateAllOpmControlPanels(req *proto.OpmControlPanels) (*proto.OpmControlPanels, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmControlPanels
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.Name)
	updateField = append(updateField, t.NameEn)
	updateField = append(updateField, t.Lang)
	updateField = append(updateField, t.Desc)
	updateField = append(updateField, t.Url)
	updateField = append(updateField, t.UrlName)
	updateField = append(updateField, t.PanelSize)
	updateField = append(updateField, t.PreviewName)
	updateField = append(updateField, t.PreviewUrl)
	updateField = append(updateField, t.PreviewSize)
	updateField = append(updateField, t.ProductTypeId)
	updateField = append(updateField, t.ProductId)
	updateField = append(updateField, t.CreatedBy)
	updateField = append(updateField, t.UpdatedBy)
	updateField = append(updateField, t.TenantId)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllOpmControlPanels error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OpmControlPanels_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllOpmControlPanels error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *OpmControlPanelsSvc) UpdateFieldsOpmControlPanels(req *proto.OpmControlPanelsUpdateFieldsRequest) (*proto.OpmControlPanels, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmControlPanels
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsOpmControlPanels error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsOpmControlPanels error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OpmControlPanels_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsOpmControlPanels error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找OpmControlPanels
func (s *OpmControlPanelsSvc) FindOpmControlPanels(req *proto.OpmControlPanelsFilter) (*proto.OpmControlPanels, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmControlPanels
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.Name != "" { //字符串
		do = do.Where(t.Name.Eq(req.Name))
	}
	if req.NameEn != "" { //字符串
		do = do.Where(t.NameEn.Eq(req.NameEn))
	}
	if req.Lang != "" { //字符串
		do = do.Where(t.Lang.Eq(req.Lang))
	}
	if req.Desc != "" { //字符串
		do = do.Where(t.Desc.Eq(req.Desc))
	}
	if req.Url != "" { //字符串
		do = do.Where(t.Url.Eq(req.Url))
	}
	if req.UrlName != "" { //字符串
		do = do.Where(t.UrlName.Eq(req.UrlName))
	}
	if req.PanelSize != 0 { //整数
		do = do.Where(t.PanelSize.Eq(req.PanelSize))
	}
	if req.PreviewName != "" { //字符串
		do = do.Where(t.PreviewName.Eq(req.PreviewName))
	}
	if req.PreviewUrl != "" { //字符串
		do = do.Where(t.PreviewUrl.Eq(req.PreviewUrl))
	}
	if req.PreviewSize != 0 { //整数
		do = do.Where(t.PreviewSize.Eq(req.PreviewSize))
	}
	if req.ProductTypeId != 0 { //整数
		do = do.Where(t.ProductTypeId.Eq(req.ProductTypeId))
	}
	if req.ProductId != 0 { //整数
		do = do.Where(t.ProductId.Eq(req.ProductId))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	if req.TenantId != "" { //字符串
		do = do.Where(t.TenantId.Eq(req.TenantId))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindOpmControlPanels error : %s", err.Error())
		return nil, err
	}
	res := convert.OpmControlPanels_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找OpmControlPanels
func (s *OpmControlPanelsSvc) FindByIdOpmControlPanels(req *proto.OpmControlPanelsFilter) (*proto.OpmControlPanels, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmControlPanels
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdOpmControlPanels error : %s", err.Error())
		return nil, err
	}
	res := convert.OpmControlPanels_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找OpmControlPanels,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *OpmControlPanelsSvc) GetListOpmControlPanels(req *proto.OpmControlPanelsListRequest) ([]*proto.OpmControlPanels, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TOpmControlPanels
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.Name != "" { //字符串
			do = do.Where(t.Name.Like("%" + query.Name + "%"))
		}
		if query.NameEn != "" { //字符串
			do = do.Where(t.NameEn.Like("%" + query.NameEn + "%"))
		}
		if query.Lang != "" { //字符串
			do = do.Where(t.Lang.Like("%" + query.Lang + "%"))
		}
		if query.Desc != "" { //字符串
			do = do.Where(t.Desc.Like("%" + query.Desc + "%"))
		}
		if query.Url != "" { //字符串
			do = do.Where(t.Url.Like("%" + query.Url + "%"))
		}
		if query.UrlName != "" { //字符串
			do = do.Where(t.UrlName.Like("%" + query.UrlName + "%"))
		}
		if query.PanelSize != 0 { //整数
			do = do.Where(t.PanelSize.Eq(query.PanelSize))
		}
		if query.PreviewName != "" { //字符串
			do = do.Where(t.PreviewName.Like("%" + query.PreviewName + "%"))
		}
		if query.PreviewUrl != "" { //字符串
			do = do.Where(t.PreviewUrl.Like("%" + query.PreviewUrl + "%"))
		}
		if query.PreviewSize != 0 { //整数
			do = do.Where(t.PreviewSize.Eq(query.PreviewSize))
		}
		if query.ProductTypeId != 0 { //整数
			do = do.Where(t.ProductTypeId.Eq(query.ProductTypeId))
		}
		if query.ProductId != 0 { //整数
			do = do.Where(t.ProductId.Eq(query.ProductId))
		}
		if query.CreatedBy != 0 { //整数
			do = do.Where(t.CreatedBy.Eq(query.CreatedBy))
		}
		if query.UpdatedBy != 0 { //整数
			do = do.Where(t.UpdatedBy.Eq(query.UpdatedBy))
		}
		if query.TenantId != "" { //字符串
			do = do.Where(t.TenantId.Like("%" + query.TenantId + "%"))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TOpmControlPanels
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListOpmControlPanels error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.OpmControlPanels, len(list))
	for i, v := range list {
		result[i] = convert.OpmControlPanels_db2pb(v)
	}
	return result, total, nil
}
