// Code generated by sgen.exe,2022-05-06 14:01:21. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"cloud_platform/iot_common/iotutil"
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_product/model"
	"cloud_platform/iot_model/db_product/orm"
	"cloud_platform/iot_product_service/convert"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type OpmProductFirmwareRelationSvc struct {
	Ctx context.Context
}

// 创建OpmProductFirmwareRelation
func (s *OpmProductFirmwareRelationSvc) CreateOpmProductFirmwareRelation(req *proto.OpmProductFirmwareRelation) (*proto.OpmProductFirmwareRelation, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TOpmProductFirmwareRelation
	do := t.WithContext(context.Background())
	req.Id = iotutil.GetNextSeqInt64()
	dbObj := convert.OpmProductFirmwareRelation_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateOpmProductFirmwareRelation error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除OpmProductFirmwareRelation
func (s *OpmProductFirmwareRelationSvc) DeleteOpmProductFirmwareRelation(req *proto.OpmProductFirmwareRelation) (*proto.OpmProductFirmwareRelation, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmProductFirmwareRelation
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.ProductId != 0 { //整数
		do = do.Where(t.ProductId.Eq(req.ProductId))
	}
	if req.FirmwareId != 0 { //整数
		do = do.Where(t.FirmwareId.Eq(req.FirmwareId))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteOpmProductFirmwareRelation error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除OpmProductFirmwareRelation
func (s *OpmProductFirmwareRelationSvc) DeleteByIdOpmProductFirmwareRelation(req *proto.OpmProductFirmwareRelation) (*proto.OpmProductFirmwareRelation, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmProductFirmwareRelation
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdOpmProductFirmwareRelation error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除OpmProductFirmwareRelation
func (s *OpmProductFirmwareRelationSvc) DeleteByIdsOpmProductFirmwareRelation(req *proto.OpmProductFirmwareRelationBatchDeleteRequest) (*proto.OpmProductFirmwareRelationBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TOpmProductFirmwareRelation
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsOpmProductFirmwareRelation error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新OpmProductFirmwareRelation
func (s *OpmProductFirmwareRelationSvc) UpdateOpmProductFirmwareRelation(req *proto.OpmProductFirmwareRelation) (*proto.OpmProductFirmwareRelation, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmProductFirmwareRelation
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.ProductId != 0 { //整数
		updateField = append(updateField, t.ProductId)
	}
	if req.FirmwareId != 0 { //整数
		updateField = append(updateField, t.FirmwareId)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateOpmProductFirmwareRelation error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.OpmProductFirmwareRelation_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateOpmProductFirmwareRelation error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段OpmProductFirmwareRelation
func (s *OpmProductFirmwareRelationSvc) UpdateAllOpmProductFirmwareRelation(req *proto.OpmProductFirmwareRelation) (*proto.OpmProductFirmwareRelation, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmProductFirmwareRelation
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.ProductId)
	updateField = append(updateField, t.FirmwareId)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllOpmProductFirmwareRelation error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OpmProductFirmwareRelation_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllOpmProductFirmwareRelation error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *OpmProductFirmwareRelationSvc) UpdateFieldsOpmProductFirmwareRelation(req *proto.OpmProductFirmwareRelationUpdateFieldsRequest) (*proto.OpmProductFirmwareRelation, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmProductFirmwareRelation
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsOpmProductFirmwareRelation error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsOpmProductFirmwareRelation error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OpmProductFirmwareRelation_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsOpmProductFirmwareRelation error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找OpmProductFirmwareRelation
func (s *OpmProductFirmwareRelationSvc) FindOpmProductFirmwareRelation(req *proto.OpmProductFirmwareRelationFilter) (*proto.OpmProductFirmwareRelation, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmProductFirmwareRelation
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.ProductId != 0 { //整数
		do = do.Where(t.ProductId.Eq(req.ProductId))
	}
	if req.FirmwareId != 0 { //整数
		do = do.Where(t.FirmwareId.Eq(req.FirmwareId))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindOpmProductFirmwareRelation error : %s", err.Error())
		return nil, err
	}
	res := convert.OpmProductFirmwareRelation_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找OpmProductFirmwareRelation
func (s *OpmProductFirmwareRelationSvc) FindByIdOpmProductFirmwareRelation(req *proto.OpmProductFirmwareRelationFilter) (*proto.OpmProductFirmwareRelation, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmProductFirmwareRelation
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdOpmProductFirmwareRelation error : %s", err.Error())
		return nil, err
	}
	res := convert.OpmProductFirmwareRelation_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找OpmProductFirmwareRelation,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *OpmProductFirmwareRelationSvc) GetListOpmProductFirmwareRelation(req *proto.OpmProductFirmwareRelationListRequest) ([]*proto.OpmProductFirmwareRelation, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TOpmProductFirmwareRelation
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.ProductId != 0 { //整数
			do = do.Where(t.ProductId.Eq(query.ProductId))
		}
		if query.FirmwareId != 0 { //整数
			do = do.Where(t.FirmwareId.Eq(query.FirmwareId))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TOpmProductFirmwareRelation
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListOpmProductFirmwareRelation error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.OpmProductFirmwareRelation, len(list))
	for i, v := range list {
		result[i] = convert.OpmProductFirmwareRelation_db2pb(v)
	}
	return result, total, nil
}
