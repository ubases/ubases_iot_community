// Code generated by sgen.exe,2022-08-31 14:21:29. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_product/model"
	"cloud_platform/iot_model/db_product/orm"
	"cloud_platform/iot_product_service/convert"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type OpmVoicePublishRecordSvc struct {
	Ctx context.Context
}

// 创建OpmVoicePublishRecord
func (s *OpmVoicePublishRecordSvc) CreateOpmVoicePublishRecord(req *proto.OpmVoicePublishRecord) (*proto.OpmVoicePublishRecord, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TOpmVoicePublishRecord
	do := t.WithContext(context.Background())
	dbObj := convert.OpmVoicePublishRecord_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateOpmVoicePublishRecord error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除OpmVoicePublishRecord
func (s *OpmVoicePublishRecordSvc) DeleteOpmVoicePublishRecord(req *proto.OpmVoicePublishRecord) (*proto.OpmVoicePublishRecord, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmVoicePublishRecord
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.ProductKey != "" { //字符串
		do = do.Where(t.ProductKey.Eq(req.ProductKey))
	}
	if req.ProductName != "" { //字符串
		do = do.Where(t.ProductName.Eq(req.ProductName))
	}
	if req.VoiceName != "" { //字符串
		do = do.Where(t.VoiceName.Eq(req.VoiceName))
	}
	if req.AttrJson != "" { //字符串
		do = do.Where(t.AttrJson.Eq(req.AttrJson))
	}
	if req.VoiceNo != "" { //字符串
		do = do.Where(t.VoiceNo.Eq(req.VoiceNo))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteOpmVoicePublishRecord error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除OpmVoicePublishRecord
func (s *OpmVoicePublishRecordSvc) DeleteByIdOpmVoicePublishRecord(req *proto.OpmVoicePublishRecord) (*proto.OpmVoicePublishRecord, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmVoicePublishRecord
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdOpmVoicePublishRecord error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除OpmVoicePublishRecord
func (s *OpmVoicePublishRecordSvc) DeleteByIdsOpmVoicePublishRecord(req *proto.OpmVoicePublishRecordBatchDeleteRequest) (*proto.OpmVoicePublishRecordBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TOpmVoicePublishRecord
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsOpmVoicePublishRecord error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新OpmVoicePublishRecord
func (s *OpmVoicePublishRecordSvc) UpdateOpmVoicePublishRecord(req *proto.OpmVoicePublishRecord) (*proto.OpmVoicePublishRecord, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmVoicePublishRecord
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.ProductKey != "" { //字符串
		updateField = append(updateField, t.ProductKey)
	}
	if req.ProductName != "" { //字符串
		updateField = append(updateField, t.ProductName)
	}
	if req.VoiceName != "" { //字符串
		updateField = append(updateField, t.VoiceName)
	}
	if req.AttrJson != "" { //字符串
		updateField = append(updateField, t.AttrJson)
	}
	if req.VoiceNo != "" { //字符串
		updateField = append(updateField, t.VoiceNo)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateOpmVoicePublishRecord error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.OpmVoicePublishRecord_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateOpmVoicePublishRecord error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段OpmVoicePublishRecord
func (s *OpmVoicePublishRecordSvc) UpdateAllOpmVoicePublishRecord(req *proto.OpmVoicePublishRecord) (*proto.OpmVoicePublishRecord, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmVoicePublishRecord
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.ProductKey)
	updateField = append(updateField, t.ProductName)
	updateField = append(updateField, t.VoiceName)
	updateField = append(updateField, t.AttrJson)
	updateField = append(updateField, t.VoiceNo)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllOpmVoicePublishRecord error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OpmVoicePublishRecord_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllOpmVoicePublishRecord error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *OpmVoicePublishRecordSvc) UpdateFieldsOpmVoicePublishRecord(req *proto.OpmVoicePublishRecordUpdateFieldsRequest) (*proto.OpmVoicePublishRecord, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmVoicePublishRecord
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsOpmVoicePublishRecord error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsOpmVoicePublishRecord error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OpmVoicePublishRecord_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsOpmVoicePublishRecord error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找OpmVoicePublishRecord
func (s *OpmVoicePublishRecordSvc) FindOpmVoicePublishRecord(req *proto.OpmVoicePublishRecordFilter) (*proto.OpmVoicePublishRecord, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmVoicePublishRecord
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.ProductKey != "" { //字符串
		do = do.Where(t.ProductKey.Eq(req.ProductKey))
	}
	if req.ProductName != "" { //字符串
		do = do.Where(t.ProductName.Eq(req.ProductName))
	}
	if req.VoiceName != "" { //字符串
		do = do.Where(t.VoiceName.Eq(req.VoiceName))
	}
	if req.AttrJson != "" { //字符串
		do = do.Where(t.AttrJson.Eq(req.AttrJson))
	}
	if req.VoiceNo != "" { //字符串
		do = do.Where(t.VoiceNo.Eq(req.VoiceNo))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindOpmVoicePublishRecord error : %s", err.Error())
		return nil, err
	}
	res := convert.OpmVoicePublishRecord_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找OpmVoicePublishRecord
func (s *OpmVoicePublishRecordSvc) FindByIdOpmVoicePublishRecord(req *proto.OpmVoicePublishRecordFilter) (*proto.OpmVoicePublishRecord, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmVoicePublishRecord
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdOpmVoicePublishRecord error : %s", err.Error())
		return nil, err
	}
	res := convert.OpmVoicePublishRecord_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找OpmVoicePublishRecord,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *OpmVoicePublishRecordSvc) GetListOpmVoicePublishRecord(req *proto.OpmVoicePublishRecordListRequest) ([]*proto.OpmVoicePublishRecord, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TOpmVoicePublishRecord
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.ProductKey != "" { //字符串
			do = do.Where(t.ProductKey.Like("%" + query.ProductKey + "%"))
		}
		if query.ProductName != "" { //字符串
			do = do.Where(t.ProductName.Like("%" + query.ProductName + "%"))
		}
		if query.VoiceName != "" { //字符串
			do = do.Where(t.VoiceName.Like("%" + query.VoiceName + "%"))
		}
		if query.AttrJson != "" { //字符串
			do = do.Where(t.AttrJson.Like("%" + query.AttrJson + "%"))
		}
		if query.VoiceNo != "" { //字符串
			do = do.Where(t.VoiceNo.Like("%" + query.VoiceNo + "%"))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TOpmVoicePublishRecord
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListOpmVoicePublishRecord error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.OpmVoicePublishRecord, len(list))
	for i, v := range list {
		result[i] = convert.OpmVoicePublishRecord_db2pb(v)
	}
	return result, total, nil
}
