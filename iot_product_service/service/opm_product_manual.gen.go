// Code generated by sgen.exe,2023-01-04 17:12:13. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_product/model"
	"cloud_platform/iot_model/db_product/orm"
	"cloud_platform/iot_product_service/convert"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type OpmProductManualSvc struct {
	Ctx context.Context
}

// 创建OpmProductManual
func (s *OpmProductManualSvc) CreateOpmProductManual(req *proto.OpmProductManual) (*proto.OpmProductManual, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TOpmProductManual
	do := t.WithContext(context.Background())
	dbObj := convert.OpmProductManual_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateOpmProductManual error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除OpmProductManual
func (s *OpmProductManualSvc) DeleteOpmProductManual(req *proto.OpmProductManual) (*proto.OpmProductManual, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmProductManual
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.TenantId != "" { //字符串
		do = do.Where(t.TenantId.Eq(req.TenantId))
	}
	if req.ProductKey != "" { //字符串
		do = do.Where(t.ProductKey.Eq(req.ProductKey))
	}
	if req.FileName != "" { //字符串
		do = do.Where(t.FileName.Eq(req.FileName))
	}
	if req.FileUrl != "" { //字符串
		do = do.Where(t.FileUrl.Eq(req.FileUrl))
	}
	if req.FileMd5 != "" { //字符串
		do = do.Where(t.FileMd5.Eq(req.FileMd5))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteOpmProductManual error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除OpmProductManual
func (s *OpmProductManualSvc) DeleteByIdOpmProductManual(req *proto.OpmProductManual) (*proto.OpmProductManual, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmProductManual
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdOpmProductManual error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除OpmProductManual
func (s *OpmProductManualSvc) DeleteByIdsOpmProductManual(req *proto.OpmProductManualBatchDeleteRequest) (*proto.OpmProductManualBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TOpmProductManual
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsOpmProductManual error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新OpmProductManual
func (s *OpmProductManualSvc) UpdateOpmProductManual(req *proto.OpmProductManual) (*proto.OpmProductManual, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmProductManual
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.TenantId != "" { //字符串
		updateField = append(updateField, t.TenantId)
	}
	if req.ProductKey != "" { //字符串
		updateField = append(updateField, t.ProductKey)
	}
	if req.FileName != "" { //字符串
		updateField = append(updateField, t.FileName)
	}
	if req.FileUrl != "" { //字符串
		updateField = append(updateField, t.FileUrl)
	}
	if req.FileMd5 != "" { //字符串
		updateField = append(updateField, t.FileMd5)
	}
	if req.CreatedBy != 0 { //整数
		updateField = append(updateField, t.CreatedBy)
	}
	if req.UpdatedBy != 0 { //整数
		updateField = append(updateField, t.UpdatedBy)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateOpmProductManual error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.OpmProductManual_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateOpmProductManual error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段OpmProductManual
func (s *OpmProductManualSvc) UpdateAllOpmProductManual(req *proto.OpmProductManual) (*proto.OpmProductManual, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmProductManual
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.TenantId)
	updateField = append(updateField, t.ProductKey)
	updateField = append(updateField, t.FileName)
	updateField = append(updateField, t.FileUrl)
	updateField = append(updateField, t.FileMd5)
	updateField = append(updateField, t.CreatedBy)
	updateField = append(updateField, t.UpdatedBy)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllOpmProductManual error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OpmProductManual_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllOpmProductManual error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *OpmProductManualSvc) UpdateFieldsOpmProductManual(req *proto.OpmProductManualUpdateFieldsRequest) (*proto.OpmProductManual, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmProductManual
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsOpmProductManual error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsOpmProductManual error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OpmProductManual_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsOpmProductManual error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找OpmProductManual
func (s *OpmProductManualSvc) FindOpmProductManual(req *proto.OpmProductManualFilter) (*proto.OpmProductManual, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmProductManual
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.TenantId != "" { //字符串
		do = do.Where(t.TenantId.Eq(req.TenantId))
	}
	if req.ProductKey != "" { //字符串
		do = do.Where(t.ProductKey.Eq(req.ProductKey))
	}
	if req.FileName != "" { //字符串
		do = do.Where(t.FileName.Eq(req.FileName))
	}
	if req.FileUrl != "" { //字符串
		do = do.Where(t.FileUrl.Eq(req.FileUrl))
	}
	if req.FileMd5 != "" { //字符串
		do = do.Where(t.FileMd5.Eq(req.FileMd5))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindOpmProductManual error : %s", err.Error())
		return nil, err
	}
	res := convert.OpmProductManual_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找OpmProductManual
func (s *OpmProductManualSvc) FindByIdOpmProductManual(req *proto.OpmProductManualFilter) (*proto.OpmProductManual, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmProductManual
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdOpmProductManual error : %s", err.Error())
		return nil, err
	}
	res := convert.OpmProductManual_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找OpmProductManual,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *OpmProductManualSvc) GetListOpmProductManual(req *proto.OpmProductManualListRequest) ([]*proto.OpmProductManual, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TOpmProductManual
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.TenantId != "" { //字符串
			do = do.Where(t.TenantId.Like("%" + query.TenantId + "%"))
		}
		if query.ProductKey != "" { //字符串
			do = do.Where(t.ProductKey.Like("%" + query.ProductKey + "%"))
		}
		if query.FileName != "" { //字符串
			do = do.Where(t.FileName.Like("%" + query.FileName + "%"))
		}
		if query.FileUrl != "" { //字符串
			do = do.Where(t.FileUrl.Like("%" + query.FileUrl + "%"))
		}
		if query.FileMd5 != "" { //字符串
			do = do.Where(t.FileMd5.Like("%" + query.FileMd5 + "%"))
		}
		if query.CreatedBy != 0 { //整数
			do = do.Where(t.CreatedBy.Eq(query.CreatedBy))
		}
		if query.UpdatedBy != 0 { //整数
			do = do.Where(t.UpdatedBy.Eq(query.UpdatedBy))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TOpmProductManual
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListOpmProductManual error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.OpmProductManual, len(list))
	for i, v := range list {
		result[i] = convert.OpmProductManual_db2pb(v)
	}
	return result, total, nil
}
