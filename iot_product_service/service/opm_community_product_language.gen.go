// Code generated by sgen,2023-07-10 11:45:47. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_product/model"
	"cloud_platform/iot_model/db_product/orm"
	"cloud_platform/iot_product_service/convert"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type OpmCommunityProductLanguageSvc struct {
	Ctx context.Context
}

// 创建OpmCommunityProductLanguage
func (s *OpmCommunityProductLanguageSvc) CreateOpmCommunityProductLanguage(req *proto.OpmCommunityProductLanguage) (*proto.OpmCommunityProductLanguage, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TOpmCommunityProductLanguage
	do := t.WithContext(context.Background())
	dbObj := convert.OpmCommunityProductLanguage_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateOpmCommunityProductLanguage error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除OpmCommunityProductLanguage
func (s *OpmCommunityProductLanguageSvc) DeleteOpmCommunityProductLanguage(req *proto.OpmCommunityProductLanguage) (*proto.OpmCommunityProductLanguage, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmCommunityProductLanguage
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.CommunityProductId != 0 { //整数
		do = do.Where(t.CommunityProductId.Eq(req.CommunityProductId))
	}
	if req.Lang != "" { //字符串
		do = do.Where(t.Lang.Eq(req.Lang))
	}
	if req.Name != "" { //字符串
		do = do.Where(t.Name.Eq(req.Name))
	}
	if req.Description != "" { //字符串
		do = do.Where(t.Description.Eq(req.Description))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteOpmCommunityProductLanguage error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除OpmCommunityProductLanguage
func (s *OpmCommunityProductLanguageSvc) DeleteByIdOpmCommunityProductLanguage(req *proto.OpmCommunityProductLanguage) (*proto.OpmCommunityProductLanguage, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmCommunityProductLanguage
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdOpmCommunityProductLanguage error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除OpmCommunityProductLanguage
func (s *OpmCommunityProductLanguageSvc) DeleteByIdsOpmCommunityProductLanguage(req *proto.OpmCommunityProductLanguageBatchDeleteRequest) (*proto.OpmCommunityProductLanguageBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TOpmCommunityProductLanguage
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsOpmCommunityProductLanguage error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新OpmCommunityProductLanguage
func (s *OpmCommunityProductLanguageSvc) UpdateOpmCommunityProductLanguage(req *proto.OpmCommunityProductLanguage) (*proto.OpmCommunityProductLanguage, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmCommunityProductLanguage
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.CommunityProductId != 0 { //整数
		updateField = append(updateField, t.CommunityProductId)
	}
	if req.Lang != "" { //字符串
		updateField = append(updateField, t.Lang)
	}
	if req.Name != "" { //字符串
		updateField = append(updateField, t.Name)
	}
	if req.Description != "" { //字符串
		updateField = append(updateField, t.Description)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateOpmCommunityProductLanguage error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.OpmCommunityProductLanguage_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateOpmCommunityProductLanguage error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段OpmCommunityProductLanguage
func (s *OpmCommunityProductLanguageSvc) UpdateAllOpmCommunityProductLanguage(req *proto.OpmCommunityProductLanguage) (*proto.OpmCommunityProductLanguage, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmCommunityProductLanguage
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.CommunityProductId)
	updateField = append(updateField, t.Lang)
	updateField = append(updateField, t.Name)
	updateField = append(updateField, t.Description)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllOpmCommunityProductLanguage error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OpmCommunityProductLanguage_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllOpmCommunityProductLanguage error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *OpmCommunityProductLanguageSvc) UpdateFieldsOpmCommunityProductLanguage(req *proto.OpmCommunityProductLanguageUpdateFieldsRequest) (*proto.OpmCommunityProductLanguage, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmCommunityProductLanguage
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsOpmCommunityProductLanguage error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsOpmCommunityProductLanguage error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OpmCommunityProductLanguage_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsOpmCommunityProductLanguage error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找OpmCommunityProductLanguage
func (s *OpmCommunityProductLanguageSvc) FindOpmCommunityProductLanguage(req *proto.OpmCommunityProductLanguageFilter) (*proto.OpmCommunityProductLanguage, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmCommunityProductLanguage
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.CommunityProductId != 0 { //整数
		do = do.Where(t.CommunityProductId.Eq(req.CommunityProductId))
	}
	if req.Lang != "" { //字符串
		do = do.Where(t.Lang.Eq(req.Lang))
	}
	if req.Name != "" { //字符串
		do = do.Where(t.Name.Eq(req.Name))
	}
	if req.Description != "" { //字符串
		do = do.Where(t.Description.Eq(req.Description))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindOpmCommunityProductLanguage error : %s", err.Error())
		return nil, err
	}
	res := convert.OpmCommunityProductLanguage_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找OpmCommunityProductLanguage
func (s *OpmCommunityProductLanguageSvc) FindByIdOpmCommunityProductLanguage(req *proto.OpmCommunityProductLanguageFilter) (*proto.OpmCommunityProductLanguage, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmCommunityProductLanguage
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdOpmCommunityProductLanguage error : %s", err.Error())
		return nil, err
	}
	res := convert.OpmCommunityProductLanguage_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找OpmCommunityProductLanguage,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *OpmCommunityProductLanguageSvc) GetListOpmCommunityProductLanguage(req *proto.OpmCommunityProductLanguageListRequest) ([]*proto.OpmCommunityProductLanguage, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TOpmCommunityProductLanguage
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.CommunityProductId != 0 { //整数
			do = do.Where(t.CommunityProductId.Eq(query.CommunityProductId))
		}
		if query.Lang != "" { //字符串
			do = do.Where(t.Lang.Like("%" + query.Lang + "%"))
		}
		if query.Name != "" { //字符串
			do = do.Where(t.Name.Like("%" + query.Name + "%"))
		}
		if query.Description != "" { //字符串
			do = do.Where(t.Description.Like("%" + query.Description + "%"))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TOpmCommunityProductLanguage
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListOpmCommunityProductLanguage error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.OpmCommunityProductLanguage, len(list))
	for i, v := range list {
		result[i] = convert.OpmCommunityProductLanguage_db2pb(v)
	}
	return result, total, nil
}

func (s *OpmCommunityProductLanguageSvc) GetList(productIds []int64) ([]*proto.OpmCommunityProductLanguage, error) {
	var err error
	t := orm.Use(iotmodel.GetDB()).TOpmCommunityProductLanguage
	do := t.WithContext(context.Background())
	var list []*model.TOpmCommunityProductLanguage

	list, err = do.Where(t.CommunityProductId.In(productIds...)).Find()
	if err != nil {
		logger.Errorf("GetListOpmCommunityProductLanguage error : %s", err.Error())
		return nil, err
	}
	result := make([]*proto.OpmCommunityProductLanguage, len(list))
	for i, v := range list {
		result[i] = convert.OpmCommunityProductLanguage_db2pb(v)
	}
	return result, nil
}
