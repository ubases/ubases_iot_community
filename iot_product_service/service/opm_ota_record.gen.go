// Code generated by sgen.exe,2022-05-06 14:01:20. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"cloud_platform/iot_common/iotutil"
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_product/model"
	"cloud_platform/iot_model/db_product/orm"
	"cloud_platform/iot_product_service/convert"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type OpmOtaRecordSvc struct {
	Ctx context.Context
}

// 创建OpmOtaRecord
func (s *OpmOtaRecordSvc) CreateOpmOtaRecord(req *proto.OpmOtaRecord) (*proto.OpmOtaRecord, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TOpmOtaRecord
	do := t.WithContext(context.Background())
	dbObj := convert.OpmOtaRecord_pb2db(req)
	dbObj.Id = iotutil.GetNextSeqInt64()
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateOpmOtaRecord error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除OpmOtaRecord
func (s *OpmOtaRecordSvc) DeleteOpmOtaRecord(req *proto.OpmOtaRecord) (*proto.OpmOtaRecord, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmOtaRecord
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.PkgId != 0 { //整数
		do = do.Where(t.PkgId.Eq(req.PkgId))
	}
	if req.PoductName != "" { //字符串
		do = do.Where(t.PoductName.Eq(req.PoductName))
	}
	if req.Type != 0 { //整数
		do = do.Where(t.Type.Eq(req.Type))
	}
	if req.Name != "" { //字符串
		do = do.Where(t.Name.Eq(req.Name))
	}
	if req.Version != "" { //字符串
		do = do.Where(t.Version.Eq(req.Version))
	}
	if req.IsGray != 0 { //整数
		do = do.Where(t.IsGray.Eq(req.IsGray))
	}
	if req.GrayType != 0 { //整数
		do = do.Where(t.GrayType.Eq(req.GrayType))
	}
	if req.GrayScale != 0 { //整数
		do = do.Where(t.GrayScale.Eq(req.GrayScale))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	if req.TenantId != "" { //字符串
		do = do.Where(t.TenantId.Eq(req.TenantId))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteOpmOtaRecord error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除OpmOtaRecord
func (s *OpmOtaRecordSvc) DeleteByIdOpmOtaRecord(req *proto.OpmOtaRecord) (*proto.OpmOtaRecord, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmOtaRecord
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.PkgId != 0 { //整数
		do = do.Where(t.PkgId.Eq(req.PkgId))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdOpmOtaRecord error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除OpmOtaRecord
func (s *OpmOtaRecordSvc) DeleteByIdsOpmOtaRecord(req *proto.OpmOtaRecordBatchDeleteRequest) (*proto.OpmOtaRecordBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TOpmOtaRecord
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		do = do.Where(t.PkgId.Eq(k.PkgId))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsOpmOtaRecord error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新OpmOtaRecord
func (s *OpmOtaRecordSvc) UpdateOpmOtaRecord(req *proto.OpmOtaRecord) (*proto.OpmOtaRecord, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmOtaRecord
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.PoductName != "" { //字符串
		updateField = append(updateField, t.PoductName)
	}
	if req.Type != 0 { //整数
		updateField = append(updateField, t.Type)
	}
	if req.Name != "" { //字符串
		updateField = append(updateField, t.Name)
	}
	if req.Version != "" { //字符串
		updateField = append(updateField, t.Version)
	}
	if req.IsGray != 0 { //整数
		updateField = append(updateField, t.IsGray)
	}
	if req.GrayType != 0 { //整数
		updateField = append(updateField, t.GrayType)
	}
	if req.GrayScale != 0 { //整数
		updateField = append(updateField, t.GrayScale)
	}
	if req.CreatedBy != 0 { //整数
		updateField = append(updateField, t.CreatedBy)
	}
	if req.UpdatedBy != 0 { //整数
		updateField = append(updateField, t.UpdatedBy)
	}
	if req.TenantId != "" { //字符串
		updateField = append(updateField, t.TenantId)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if req.PkgId != 0 { //整数
		do = do.Where(t.PkgId.Eq(req.PkgId))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateOpmOtaRecord error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.OpmOtaRecord_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateOpmOtaRecord error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段OpmOtaRecord
func (s *OpmOtaRecordSvc) UpdateAllOpmOtaRecord(req *proto.OpmOtaRecord) (*proto.OpmOtaRecord, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmOtaRecord
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.PoductName)
	updateField = append(updateField, t.Type)
	updateField = append(updateField, t.Name)
	updateField = append(updateField, t.Version)
	updateField = append(updateField, t.IsGray)
	updateField = append(updateField, t.GrayType)
	updateField = append(updateField, t.GrayScale)
	updateField = append(updateField, t.OperateAt)
	updateField = append(updateField, t.CreatedBy)
	updateField = append(updateField, t.UpdatedBy)
	updateField = append(updateField, t.TenantId)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if req.PkgId != 0 { //整数
		do = do.Where(t.PkgId.Eq(req.PkgId))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllOpmOtaRecord error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OpmOtaRecord_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllOpmOtaRecord error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *OpmOtaRecordSvc) UpdateFieldsOpmOtaRecord(req *proto.OpmOtaRecordUpdateFieldsRequest) (*proto.OpmOtaRecord, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmOtaRecord
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsOpmOtaRecord error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if req.Data.PkgId != 0 { //整数
		do = do.Where(t.PkgId.Eq(req.Data.PkgId))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsOpmOtaRecord error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OpmOtaRecord_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsOpmOtaRecord error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找OpmOtaRecord
func (s *OpmOtaRecordSvc) FindOpmOtaRecord(req *proto.OpmOtaRecordFilter) (*proto.OpmOtaRecord, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmOtaRecord
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.PkgId != 0 { //整数
		do = do.Where(t.PkgId.Eq(req.PkgId))
	}
	if req.PoductName != "" { //字符串
		do = do.Where(t.PoductName.Eq(req.PoductName))
	}
	if req.Type != 0 { //整数
		do = do.Where(t.Type.Eq(req.Type))
	}
	if req.Name != "" { //字符串
		do = do.Where(t.Name.Eq(req.Name))
	}
	if req.Version != "" { //字符串
		do = do.Where(t.Version.Eq(req.Version))
	}
	if req.IsGray != 0 { //整数
		do = do.Where(t.IsGray.Eq(req.IsGray))
	}
	if req.GrayType != 0 { //整数
		do = do.Where(t.GrayType.Eq(req.GrayType))
	}
	if req.GrayScale != 0 { //整数
		do = do.Where(t.GrayScale.Eq(req.GrayScale))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	if req.TenantId != "" { //字符串
		do = do.Where(t.TenantId.Eq(req.TenantId))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindOpmOtaRecord error : %s", err.Error())
		return nil, err
	}
	res := convert.OpmOtaRecord_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找OpmOtaRecord
func (s *OpmOtaRecordSvc) FindByIdOpmOtaRecord(req *proto.OpmOtaRecordFilter) (*proto.OpmOtaRecord, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmOtaRecord
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.PkgId != 0 { //整数
		do = do.Where(t.PkgId.Eq(req.PkgId))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdOpmOtaRecord error : %s", err.Error())
		return nil, err
	}
	res := convert.OpmOtaRecord_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找OpmOtaRecord,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *OpmOtaRecordSvc) GetListOpmOtaRecord(req *proto.OpmOtaRecordListRequest) ([]*proto.OpmOtaRecord, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TOpmOtaRecord
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.PkgId != 0 { //整数
			do = do.Where(t.PkgId.Eq(query.PkgId))
		}
		if query.PoductName != "" { //字符串
			do = do.Where(t.PoductName.Like("%" + query.PoductName + "%"))
		}
		if query.Type != 0 { //整数
			do = do.Where(t.Type.Eq(query.Type))
		}
		if query.Name != "" { //字符串
			do = do.Where(t.Name.Like("%" + query.Name + "%"))
		}
		if query.Version != "" { //字符串
			do = do.Where(t.Version.Like("%" + query.Version + "%"))
		}
		if query.IsGray != 0 { //整数
			do = do.Where(t.IsGray.Eq(query.IsGray))
		}
		if query.GrayType != 0 { //整数
			do = do.Where(t.GrayType.Eq(query.GrayType))
		}
		if query.GrayScale != 0 { //整数
			do = do.Where(t.GrayScale.Eq(query.GrayScale))
		}
		if query.CreatedBy != 0 { //整数
			do = do.Where(t.CreatedBy.Eq(query.CreatedBy))
		}
		if query.UpdatedBy != 0 { //整数
			do = do.Where(t.UpdatedBy.Eq(query.UpdatedBy))
		}
		if query.TenantId != "" { //字符串
			do = do.Where(t.TenantId.Like("%" + query.TenantId + "%"))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
		orderCol = t.PkgId
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TOpmOtaRecord
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListOpmOtaRecord error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.OpmOtaRecord, len(list))
	for i, v := range list {
		result[i] = convert.OpmOtaRecord_db2pb(v)
	}
	return result, total, nil
}
