// Code generated by sgen.exe,2022-04-20 14:41:34. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_product/model"
	"cloud_platform/iot_model/db_product/orm"
	"cloud_platform/iot_product_service/convert"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type PmNetworkGuideStepSvc struct {
	Ctx context.Context
}

// 创建PmNetworkGuideStep
func (s *PmNetworkGuideStepSvc) CreatePmNetworkGuideStep(req *proto.PmNetworkGuideStep) (*proto.PmNetworkGuideStep, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TPmNetworkGuideStep
	do := t.WithContext(context.Background())
	dbObj := convert.PmNetworkGuideStep_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreatePmNetworkGuideStep error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 批量创建PmNetworkGuideStep
func (s *PmNetworkGuideStepSvc) BatchCreatePmNetworkGuideStep(tx *orm.Query, req []*proto.PmNetworkGuideStep) ([]*proto.PmNetworkGuideStep, error) {
	// fixme 请在这里校验参数
	t := tx.TPmNetworkGuideStep
	do := t.WithContext(context.Background())
	var dbObjs = make([]*model.TPmNetworkGuideStep, len(req))
	for i, relation := range req {
		dbObjs[i] = convert.PmNetworkGuideStep_pb2db(relation)
	}
	err := do.CreateInBatches(dbObjs, len(req))
	if err != nil {
		logger.Errorf("BatchCreatePmNetworkGuideStep error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除PmNetworkGuideStep
func (s *PmNetworkGuideStepSvc) DeletePmNetworkGuideStep(tx *orm.Query, req *proto.PmNetworkGuideStep) (*proto.PmNetworkGuideStep, error) {
	t := tx.TPmNetworkGuideStep
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.NetworkGuideId != 0 { //整数
		do = do.Where(t.NetworkGuideId.Eq(req.NetworkGuideId))
	}
	if req.Instruction != "" { //字符串
		do = do.Where(t.Instruction.Eq(req.Instruction))
	}
	if req.InstructionEn != "" { //字符串
		do = do.Where(t.InstructionEn.Eq(req.InstructionEn))
	}
	if req.ImageUrl != "" { //字符串
		do = do.Where(t.ImageUrl.Eq(req.ImageUrl))
	}
	if req.VideoUrl != "" { //字符串
		do = do.Where(t.VideoUrl.Eq(req.VideoUrl))
	}
	if req.Sort != 0 { //整数
		do = do.Where(t.Sort.Eq(req.Sort))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeletePmNetworkGuideStep error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除PmNetworkGuideStep
func (s *PmNetworkGuideStepSvc) DeleteByIdPmNetworkGuideStep(req *proto.PmNetworkGuideStep) (*proto.PmNetworkGuideStep, error) {
	t := orm.Use(iotmodel.GetDB()).TPmNetworkGuideStep
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdPmNetworkGuideStep error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除PmNetworkGuideStep
func (s *PmNetworkGuideStepSvc) DeleteByIdsPmNetworkGuideStep(req *proto.PmNetworkGuideStepBatchDeleteRequest) (*proto.PmNetworkGuideStepBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TPmNetworkGuideStep
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsPmNetworkGuideStep error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新PmNetworkGuideStep
func (s *PmNetworkGuideStepSvc) UpdatePmNetworkGuideStep(req *proto.PmNetworkGuideStep) (*proto.PmNetworkGuideStep, error) {
	t := orm.Use(iotmodel.GetDB()).TPmNetworkGuideStep
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.NetworkGuideId != 0 { //整数
		updateField = append(updateField, t.NetworkGuideId)
	}
	if req.Instruction != "" { //字符串
		updateField = append(updateField, t.Instruction)
	}
	if req.InstructionEn != "" { //字符串
		updateField = append(updateField, t.InstructionEn)
	}
	if req.ImageUrl != "" { //字符串
		updateField = append(updateField, t.ImageUrl)
	}
	if req.VideoUrl != "" { //字符串
		updateField = append(updateField, t.VideoUrl)
	}
	if req.Sort != 0 { //整数
		updateField = append(updateField, t.Sort)
	}
	if req.CreatedBy != 0 { //整数
		updateField = append(updateField, t.CreatedBy)
	}
	if req.UpdatedBy != 0 { //整数
		updateField = append(updateField, t.UpdatedBy)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdatePmNetworkGuideStep error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.PmNetworkGuideStep_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdatePmNetworkGuideStep error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段PmNetworkGuideStep
func (s *PmNetworkGuideStepSvc) UpdateAllPmNetworkGuideStep(req *proto.PmNetworkGuideStep) (*proto.PmNetworkGuideStep, error) {
	t := orm.Use(iotmodel.GetDB()).TPmNetworkGuideStep
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.NetworkGuideId)
	updateField = append(updateField, t.Instruction)
	updateField = append(updateField, t.InstructionEn)
	updateField = append(updateField, t.ImageUrl)
	updateField = append(updateField, t.VideoUrl)
	updateField = append(updateField, t.Sort)
	updateField = append(updateField, t.CreatedBy)
	updateField = append(updateField, t.UpdatedBy)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllPmNetworkGuideStep error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.PmNetworkGuideStep_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllPmNetworkGuideStep error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *PmNetworkGuideStepSvc) UpdateFieldsPmNetworkGuideStep(req *proto.PmNetworkGuideStepUpdateFieldsRequest) (*proto.PmNetworkGuideStep, error) {
	t := orm.Use(iotmodel.GetDB()).TPmNetworkGuideStep
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsPmNetworkGuideStep error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsPmNetworkGuideStep error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.PmNetworkGuideStep_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsPmNetworkGuideStep error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找PmNetworkGuideStep
func (s *PmNetworkGuideStepSvc) FindPmNetworkGuideStep(req *proto.PmNetworkGuideStepFilter) (*proto.PmNetworkGuideStep, error) {
	t := orm.Use(iotmodel.GetDB()).TPmNetworkGuideStep
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.NetworkGuideId != 0 { //整数
		do = do.Where(t.NetworkGuideId.Eq(req.NetworkGuideId))
	}
	if req.Instruction != "" { //字符串
		do = do.Where(t.Instruction.Like("%" + req.Instruction + "%"))
	}
	if req.InstructionEn != "" { //字符串
		do = do.Where(t.InstructionEn.Like("%" + req.InstructionEn + "%"))
	}
	if req.ImageUrl != "" { //字符串
		do = do.Where(t.ImageUrl.Like("%" + req.ImageUrl + "%"))
	}
	if req.VideoUrl != "" { //字符串
		do = do.Where(t.VideoUrl.Like("%" + req.VideoUrl + "%"))
	}
	if req.Sort != 0 { //整数
		do = do.Where(t.Sort.Eq(req.Sort))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindPmNetworkGuideStep error : %s", err.Error())
		return nil, err
	}
	res := convert.PmNetworkGuideStep_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找PmNetworkGuideStep
func (s *PmNetworkGuideStepSvc) FindByIdPmNetworkGuideStep(req *proto.PmNetworkGuideStepFilter) (*proto.PmNetworkGuideStep, error) {
	t := orm.Use(iotmodel.GetDB()).TPmNetworkGuideStep
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdPmNetworkGuideStep error : %s", err.Error())
		return nil, err
	}
	res := convert.PmNetworkGuideStep_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找PmNetworkGuideStep,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *PmNetworkGuideStepSvc) GetListPmNetworkGuideStep(req *proto.PmNetworkGuideStepListRequest) ([]*proto.PmNetworkGuideStep, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TPmNetworkGuideStep
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.NetworkGuideId != 0 { //整数
			do = do.Where(t.NetworkGuideId.Eq(query.NetworkGuideId))
		}
		if query.Instruction != "" { //字符串
			do = do.Where(t.Instruction.Like("%" + query.Instruction + "%"))
		}
		if query.InstructionEn != "" { //字符串
			do = do.Where(t.InstructionEn.Like("%" + query.InstructionEn + "%"))
		}
		if query.ImageUrl != "" { //字符串
			do = do.Where(t.ImageUrl.Like("%" + query.ImageUrl + "%"))
		}
		if query.VideoUrl != "" { //字符串
			do = do.Where(t.VideoUrl.Like("%" + query.VideoUrl + "%"))
		}
		if query.Sort != 0 { //整数
			do = do.Where(t.Sort.Eq(query.Sort))
		}
		if query.CreatedBy != 0 { //整数
			do = do.Where(t.CreatedBy.Eq(query.CreatedBy))
		}
		if query.UpdatedBy != 0 { //整数
			do = do.Where(t.UpdatedBy.Eq(query.UpdatedBy))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TPmNetworkGuideStep
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListPmNetworkGuideStep error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.PmNetworkGuideStep, len(list))
	for i, v := range list {
		result[i] = convert.PmNetworkGuideStep_db2pb(v)
	}
	return result, total, nil
}
