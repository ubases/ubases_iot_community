// Code generated by sgen.exe,2022-04-20 14:41:18. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_product/model"
	"cloud_platform/iot_model/db_product/orm"
	"cloud_platform/iot_product_service/convert"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type PmNetworkGuideSvc struct {
	Ctx context.Context
}

// 创建PmNetworkGuide
func (s *PmNetworkGuideSvc) CreatePmNetworkGuide(tx *orm.Query, req *proto.PmNetworkGuide) (*proto.PmNetworkGuide, error) {
	// fixme 请在这里校验参数
	t := tx.TPmNetworkGuide
	do := t.WithContext(context.Background())
	dbObj := convert.PmNetworkGuide_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreatePmNetworkGuide error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除PmNetworkGuide
func (s *PmNetworkGuideSvc) DeletePmNetworkGuide(tx *orm.Query, req *proto.PmNetworkGuide) (*proto.PmNetworkGuide, error) {
	t := tx.TPmNetworkGuide
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.ProductId != 0 { //整数
		do = do.Where(t.ProductId.Eq(req.ProductId))
	}
	if req.ProductTypeId != 0 { //整数
		do = do.Where(t.ProductTypeId.Eq(req.ProductTypeId))
	}
	if req.Type != 0 { //整数
		do = do.Where(t.Type.Eq(req.Type))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeletePmNetworkGuide error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除PmNetworkGuide
func (s *PmNetworkGuideSvc) DeleteByIdPmNetworkGuide(req *proto.PmNetworkGuide) (*proto.PmNetworkGuide, error) {
	t := orm.Use(iotmodel.GetDB()).TPmNetworkGuide
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdPmNetworkGuide error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除PmNetworkGuide
func (s *PmNetworkGuideSvc) DeleteByIdsPmNetworkGuide(req *proto.PmNetworkGuideBatchDeleteRequest) (*proto.PmNetworkGuideBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TPmNetworkGuide
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsPmNetworkGuide error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新PmNetworkGuide
func (s *PmNetworkGuideSvc) UpdatePmNetworkGuide(req *proto.PmNetworkGuide) (*proto.PmNetworkGuide, error) {
	t := orm.Use(iotmodel.GetDB()).TPmNetworkGuide
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.ProductId != 0 { //整数
		updateField = append(updateField, t.ProductId)
	}
	if req.ProductTypeId != 0 { //整数
		updateField = append(updateField, t.ProductTypeId)
	}
	if req.Type != 0 { //整数
		updateField = append(updateField, t.Type)
	}
	if req.CreatedBy != 0 { //整数
		updateField = append(updateField, t.CreatedBy)
	}
	if req.UpdatedBy != 0 { //整数
		updateField = append(updateField, t.UpdatedBy)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdatePmNetworkGuide error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.PmNetworkGuide_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdatePmNetworkGuide error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段PmNetworkGuide
func (s *PmNetworkGuideSvc) UpdateAllPmNetworkGuide(req *proto.PmNetworkGuide) (*proto.PmNetworkGuide, error) {
	t := orm.Use(iotmodel.GetDB()).TPmNetworkGuide
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.ProductId)
	updateField = append(updateField, t.ProductTypeId)
	updateField = append(updateField, t.Type)
	updateField = append(updateField, t.CreatedBy)
	updateField = append(updateField, t.UpdatedBy)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllPmNetworkGuide error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.PmNetworkGuide_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllPmNetworkGuide error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *PmNetworkGuideSvc) UpdateFieldsPmNetworkGuide(req *proto.PmNetworkGuideUpdateFieldsRequest) (*proto.PmNetworkGuide, error) {
	t := orm.Use(iotmodel.GetDB()).TPmNetworkGuide
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsPmNetworkGuide error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsPmNetworkGuide error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.PmNetworkGuide_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsPmNetworkGuide error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找PmNetworkGuide
func (s *PmNetworkGuideSvc) FindPmNetworkGuide(req *proto.PmNetworkGuideFilter) (*proto.PmNetworkGuide, error) {
	t := orm.Use(iotmodel.GetDB()).TPmNetworkGuide
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.ProductId != 0 { //整数
		do = do.Where(t.ProductId.Eq(req.ProductId))
	}
	if req.ProductTypeId != 0 { //整数
		do = do.Where(t.ProductTypeId.Eq(req.ProductTypeId))
	}
	if req.Type != 0 { //整数
		do = do.Where(t.Type.Eq(req.Type))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindPmNetworkGuide error : %s", err.Error())
		return nil, err
	}
	res := convert.PmNetworkGuide_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找PmNetworkGuide
func (s *PmNetworkGuideSvc) FindByIdPmNetworkGuide(req *proto.PmNetworkGuideFilter) (*proto.PmNetworkGuide, error) {
	t := orm.Use(iotmodel.GetDB()).TPmNetworkGuide
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdPmNetworkGuide error : %s", err.Error())
		return nil, err
	}
	res := convert.PmNetworkGuide_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找PmNetworkGuide,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *PmNetworkGuideSvc) GetListPmNetworkGuide(req *proto.PmNetworkGuideListRequest) ([]*proto.PmNetworkGuide, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TPmNetworkGuide
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.ProductId != 0 { //整数
			do = do.Where(t.ProductId.Eq(query.ProductId))
		}
		if query.ProductTypeId != 0 { //整数
			do = do.Where(t.ProductTypeId.Eq(query.ProductTypeId))
		}
		if query.Type != 0 { //整数
			do = do.Where(t.Type.Eq(query.Type))
		}
		if query.CreatedBy != 0 { //整数
			do = do.Where(t.CreatedBy.Eq(query.CreatedBy))
		}
		if query.UpdatedBy != 0 { //整数
			do = do.Where(t.UpdatedBy.Eq(query.UpdatedBy))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TPmNetworkGuide
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListPmNetworkGuide error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.PmNetworkGuide, len(list))
	for i, v := range list {
		result[i] = convert.PmNetworkGuide_db2pb(v)
	}
	return result, total, nil
}
