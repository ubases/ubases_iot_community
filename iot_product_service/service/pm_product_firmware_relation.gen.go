// Code generated by sgen.exe,2022-04-21 12:44:21. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_product/model"
	"cloud_platform/iot_model/db_product/orm"
	"cloud_platform/iot_product_service/convert"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type PmProductFirmwareRelationSvc struct {
	Ctx context.Context
}

// 创建PmProductFirmwareRelation
func (s *PmProductFirmwareRelationSvc) CreatePmProductFirmwareRelation(req *proto.PmProductFirmwareRelation) (*proto.PmProductFirmwareRelation, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TPmProductFirmwareRelation
	do := t.WithContext(context.Background())
	dbObj := convert.PmProductFirmwareRelation_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreatePmProductFirmwareRelation error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 批量创建PmProductFirmwareRelation
func (s *PmProductFirmwareRelationSvc) BatchCreatePmProductFirmwareRelation(tx *orm.Query, req []*proto.PmProductFirmwareRelation) ([]*proto.PmProductFirmwareRelation, error) {
	// fixme 请在这里校验参数
	t := tx.TPmProductFirmwareRelation
	do := t.WithContext(context.Background())
	var dbObjs = make([]*model.TPmProductFirmwareRelation, len(req))
	for i, relation := range req {
		dbObjs[i] = convert.PmProductFirmwareRelation_pb2db(relation)
	}
	err := do.CreateInBatches(dbObjs, len(req))
	if err != nil {
		logger.Errorf("BatchCreatePmProductFirmwareRelation error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除PmProductFirmwareRelation
func (s *PmProductFirmwareRelationSvc) DeletePmProductFirmwareRelation(tx *orm.Query, req *proto.PmProductFirmwareRelation) (*proto.PmProductFirmwareRelation, error) {
	t := tx.TPmProductFirmwareRelation
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.ProductId != 0 { //整数
		do = do.Where(t.ProductId.Eq(req.ProductId))
	}
	if req.FirmwareId != 0 { //整数
		do = do.Where(t.FirmwareId.Eq(req.FirmwareId))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeletePmProductFirmwareRelation error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除PmProductFirmwareRelation
func (s *PmProductFirmwareRelationSvc) DeleteByIdPmProductFirmwareRelation(req *proto.PmProductFirmwareRelation) (*proto.PmProductFirmwareRelation, error) {
	t := orm.Use(iotmodel.GetDB()).TPmProductFirmwareRelation
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdPmProductFirmwareRelation error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除PmProductFirmwareRelation
func (s *PmProductFirmwareRelationSvc) DeleteByIdsPmProductFirmwareRelation(req *proto.PmProductFirmwareRelationBatchDeleteRequest) (*proto.PmProductFirmwareRelationBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TPmProductFirmwareRelation
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsPmProductFirmwareRelation error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新PmProductFirmwareRelation
func (s *PmProductFirmwareRelationSvc) UpdatePmProductFirmwareRelation(req *proto.PmProductFirmwareRelation) (*proto.PmProductFirmwareRelation, error) {
	t := orm.Use(iotmodel.GetDB()).TPmProductFirmwareRelation
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.ProductId != 0 { //整数
		updateField = append(updateField, t.ProductId)
	}
	if req.FirmwareId != 0 { //整数
		updateField = append(updateField, t.FirmwareId)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdatePmProductFirmwareRelation error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.PmProductFirmwareRelation_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdatePmProductFirmwareRelation error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段PmProductFirmwareRelation
func (s *PmProductFirmwareRelationSvc) UpdateAllPmProductFirmwareRelation(req *proto.PmProductFirmwareRelation) (*proto.PmProductFirmwareRelation, error) {
	t := orm.Use(iotmodel.GetDB()).TPmProductFirmwareRelation
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.ProductId)
	updateField = append(updateField, t.FirmwareId)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllPmProductFirmwareRelation error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.PmProductFirmwareRelation_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllPmProductFirmwareRelation error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *PmProductFirmwareRelationSvc) UpdateFieldsPmProductFirmwareRelation(req *proto.PmProductFirmwareRelationUpdateFieldsRequest) (*proto.PmProductFirmwareRelation, error) {
	t := orm.Use(iotmodel.GetDB()).TPmProductFirmwareRelation
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsPmProductFirmwareRelation error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsPmProductFirmwareRelation error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.PmProductFirmwareRelation_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsPmProductFirmwareRelation error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找PmProductFirmwareRelation
func (s *PmProductFirmwareRelationSvc) FindPmProductFirmwareRelation(req *proto.PmProductFirmwareRelationFilter) (*proto.PmProductFirmwareRelation, error) {
	t := orm.Use(iotmodel.GetDB()).TPmProductFirmwareRelation
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.ProductId != 0 { //整数
		do = do.Where(t.ProductId.Eq(req.ProductId))
	}
	if req.FirmwareId != 0 { //整数
		do = do.Where(t.FirmwareId.Eq(req.FirmwareId))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindPmProductFirmwareRelation error : %s", err.Error())
		return nil, err
	}
	res := convert.PmProductFirmwareRelation_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找PmProductFirmwareRelation
func (s *PmProductFirmwareRelationSvc) FindByIdPmProductFirmwareRelation(req *proto.PmProductFirmwareRelationFilter) (*proto.PmProductFirmwareRelation, error) {
	t := orm.Use(iotmodel.GetDB()).TPmProductFirmwareRelation
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdPmProductFirmwareRelation error : %s", err.Error())
		return nil, err
	}
	res := convert.PmProductFirmwareRelation_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找PmProductFirmwareRelation,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *PmProductFirmwareRelationSvc) GetListPmProductFirmwareRelation(req *proto.PmProductFirmwareRelationListRequest) ([]*proto.PmProductFirmwareRelation, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TPmProductFirmwareRelation
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.ProductId != 0 { //整数
			do = do.Where(t.ProductId.Eq(query.ProductId))
		}
		if query.FirmwareId != 0 { //整数
			do = do.Where(t.FirmwareId.Eq(query.FirmwareId))
		}

	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TPmProductFirmwareRelation
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListPmProductFirmwareRelation error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.PmProductFirmwareRelation, len(list))
	for i, v := range list {
		result[i] = convert.PmProductFirmwareRelation_db2pb(v)
	}
	return result, total, nil
}
