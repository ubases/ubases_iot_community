// Code generated by sgen.exe,2022-08-31 14:21:29. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_product/model"
	"cloud_platform/iot_model/db_product/orm"
	"cloud_platform/iot_product_service/convert"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type OpmVoiceSvc struct {
	Ctx context.Context
}

// 创建OpmVoice
func (s *OpmVoiceSvc) CreateOpmVoice(req *proto.OpmVoice) (*proto.OpmVoice, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TOpmVoice
	do := t.WithContext(context.Background())
	dbObj := convert.OpmVoice_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateOpmVoice error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除OpmVoice
func (s *OpmVoiceSvc) DeleteOpmVoice(req *proto.OpmVoice) (*proto.OpmVoice, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmVoice
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.VoiceNo != "" { //字符串
		do = do.Where(t.VoiceNo.Eq(req.VoiceNo))
	}
	if req.VoiceName != "" { //字符串
		do = do.Where(t.VoiceName.Eq(req.VoiceName))
	}
	if req.VoiceEnName != "" { //字符串
		do = do.Where(t.VoiceEnName.Eq(req.VoiceEnName))
	}
	if req.VoiceCategory != "" { //字符串
		do = do.Where(t.VoiceCategory.Eq(req.VoiceCategory))
	}
	if req.VoiceDoc != "" { //字符串
		do = do.Where(t.VoiceDoc.Eq(req.VoiceDoc))
	}
	if req.VoiceLogo != "" { //字符串
		do = do.Where(t.VoiceLogo.Eq(req.VoiceLogo))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteOpmVoice error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除OpmVoice
func (s *OpmVoiceSvc) DeleteByIdOpmVoice(req *proto.OpmVoice) (*proto.OpmVoice, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmVoice
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdOpmVoice error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除OpmVoice
func (s *OpmVoiceSvc) DeleteByIdsOpmVoice(req *proto.OpmVoiceBatchDeleteRequest) (*proto.OpmVoiceBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TOpmVoice
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsOpmVoice error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新OpmVoice
func (s *OpmVoiceSvc) UpdateOpmVoice(req *proto.OpmVoice) (*proto.OpmVoice, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmVoice
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.VoiceNo != "" { //字符串
		updateField = append(updateField, t.VoiceNo)
	}
	if req.VoiceName != "" { //字符串
		updateField = append(updateField, t.VoiceName)
	}
	if req.VoiceEnName != "" { //字符串
		updateField = append(updateField, t.VoiceEnName)
	}
	if req.VoiceCategory != "" { //字符串
		updateField = append(updateField, t.VoiceCategory)
	}
	if req.VoiceDoc != "" { //字符串
		updateField = append(updateField, t.VoiceDoc)
	}
	if req.VoiceLogo != "" { //字符串
		updateField = append(updateField, t.VoiceLogo)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateOpmVoice error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.OpmVoice_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateOpmVoice error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段OpmVoice
func (s *OpmVoiceSvc) UpdateAllOpmVoice(req *proto.OpmVoice) (*proto.OpmVoice, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmVoice
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.VoiceNo)
	updateField = append(updateField, t.VoiceName)
	updateField = append(updateField, t.VoiceEnName)
	updateField = append(updateField, t.VoiceCategory)
	updateField = append(updateField, t.VoiceDoc)
	updateField = append(updateField, t.VoiceLogo)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllOpmVoice error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OpmVoice_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllOpmVoice error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *OpmVoiceSvc) UpdateFieldsOpmVoice(req *proto.OpmVoiceUpdateFieldsRequest) (*proto.OpmVoice, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmVoice
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsOpmVoice error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsOpmVoice error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OpmVoice_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsOpmVoice error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找OpmVoice
func (s *OpmVoiceSvc) FindOpmVoice(req *proto.OpmVoiceFilter) (*proto.OpmVoice, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmVoice
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.VoiceNo != "" { //字符串
		do = do.Where(t.VoiceNo.Eq(req.VoiceNo))
	}
	if req.VoiceName != "" { //字符串
		do = do.Where(t.VoiceName.Eq(req.VoiceName))
	}
	if req.VoiceCategory != "" { //字符串
		do = do.Where(t.VoiceCategory.Eq(req.VoiceCategory))
	}
	if req.VoiceDoc != "" { //字符串
		do = do.Where(t.VoiceDoc.Eq(req.VoiceDoc))
	}
	if req.VoiceLogo != "" { //字符串
		do = do.Where(t.VoiceLogo.Eq(req.VoiceLogo))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindOpmVoice error : %s", err.Error())
		return nil, err
	}
	res := convert.OpmVoice_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找OpmVoice
func (s *OpmVoiceSvc) FindByIdOpmVoice(req *proto.OpmVoiceFilter) (*proto.OpmVoice, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmVoice
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdOpmVoice error : %s", err.Error())
		return nil, err
	}
	res := convert.OpmVoice_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找OpmVoice,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *OpmVoiceSvc) GetListOpmVoice(req *proto.OpmVoiceListRequest) ([]*proto.OpmVoice, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TOpmVoice
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.VoiceNo != "" { //字符串
			do = do.Where(t.VoiceNo.Like("%" + query.VoiceNo + "%"))
		}
		if query.VoiceName != "" { //字符串
			do = do.Where(t.VoiceName.Like("%" + query.VoiceName + "%"))
		}
		if query.VoiceCategory != "" { //字符串
			do = do.Where(t.VoiceCategory.Like("%" + query.VoiceCategory + "%"))
		}
		if query.VoiceDoc != "" { //字符串
			do = do.Where(t.VoiceDoc.Like("%" + query.VoiceDoc + "%"))
		}
		if query.VoiceLogo != "" { //字符串
			do = do.Where(t.VoiceLogo.Like("%" + query.VoiceLogo + "%"))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TOpmVoice
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListOpmVoice error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.OpmVoice, len(list))
	for i, v := range list {
		result[i] = convert.OpmVoice_db2pb(v)
	}
	return result, total, nil
}
