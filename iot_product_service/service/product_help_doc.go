// Code generated by sgen.exe,2022-08-22 19:32:42. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_product/model"
	"cloud_platform/iot_model/db_product/orm"
	"cloud_platform/iot_product_service/convert"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type ProductHelpDocSvc struct {
	Ctx context.Context
}

// 创建ProductHelpDoc
func (s *ProductHelpDocSvc) CreateProductHelpDoc(req *proto.ProductHelpDoc) (*proto.ProductHelpDoc, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TProductHelpDoc
	do := t.WithContext(context.Background())
	dbObj := convert.ProductHelpDoc_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateProductHelpDoc error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除ProductHelpDoc
func (s *ProductHelpDocSvc) DeleteProductHelpDoc(req *proto.ProductHelpDoc) (*proto.ProductHelpDoc, error) {
	t := orm.Use(iotmodel.GetDB()).TProductHelpDoc
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.TenantId != "" { //字符串
		do = do.Where(t.TenantId.Eq(req.TenantId))
	}
	if req.ProductKey != "" { //字符串
		do = do.Where(t.ProductKey.Eq(req.ProductKey))
	}
	if req.Lang != "" { //字符串
		do = do.Where(t.Lang.Eq(req.Lang))
	}
	if req.Title != "" { //字符串
		do = do.Where(t.Title.Eq(req.Title))
	}
	if req.Content != "" { //字符串
		do = do.Where(t.Content.Eq(req.Content))
	}
	if req.RelationId != 0 { //整数
		do = do.Where(t.RelationId.Eq(req.RelationId))
	}
	if req.SortId != 0 { //整数
		do = do.Where(t.SortId.Eq(req.SortId))
	}
	if req.Status != 0 { //整数
		do = do.Where(t.Status.Eq(req.Status))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteProductHelpDoc error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除ProductHelpDoc
func (s *ProductHelpDocSvc) DeleteByIdProductHelpDoc(req *proto.ProductHelpDoc) (*proto.ProductHelpDoc, error) {
	t := orm.Use(iotmodel.GetDB()).TProductHelpDoc
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdProductHelpDoc error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除ProductHelpDoc
func (s *ProductHelpDocSvc) DeleteByIdsProductHelpDoc(req *proto.ProductHelpDocBatchDeleteRequest) (*proto.ProductHelpDocBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TProductHelpDoc
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsProductHelpDoc error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新ProductHelpDoc
func (s *ProductHelpDocSvc) UpdateProductHelpDoc(req *proto.ProductHelpDoc) (*proto.ProductHelpDoc, error) {
	t := orm.Use(iotmodel.GetDB()).TProductHelpDoc
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.TenantId != "" { //字符串
		updateField = append(updateField, t.TenantId)
	}
	if req.ProductKey != "" { //字符串
		updateField = append(updateField, t.ProductKey)
	}
	if req.Lang != "" { //字符串
		updateField = append(updateField, t.Lang)
	}
	if req.Title != "" { //字符串
		updateField = append(updateField, t.Title)
	}
	if req.Content != "" { //字符串
		updateField = append(updateField, t.Content)
	}
	if req.RelationId != 0 { //整数
		updateField = append(updateField, t.RelationId)
	}
	if req.SortId != 0 { //整数
		updateField = append(updateField, t.SortId)
	}
	if req.Status != 0 { //整数
		updateField = append(updateField, t.Status)
	}
	if req.CreatedBy != 0 { //整数
		updateField = append(updateField, t.CreatedBy)
	}
	if req.UpdatedBy != 0 { //整数
		updateField = append(updateField, t.UpdatedBy)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateProductHelpDoc error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.ProductHelpDoc_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateProductHelpDoc error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段ProductHelpDoc
func (s *ProductHelpDocSvc) UpdateAllProductHelpDoc(req *proto.ProductHelpDoc) (*proto.ProductHelpDoc, error) {
	t := orm.Use(iotmodel.GetDB()).TProductHelpDoc
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.TenantId)
	updateField = append(updateField, t.ProductKey)
	updateField = append(updateField, t.Lang)
	updateField = append(updateField, t.Title)
	updateField = append(updateField, t.Content)
	updateField = append(updateField, t.RelationId)
	updateField = append(updateField, t.SortId)
	updateField = append(updateField, t.Status)
	updateField = append(updateField, t.CreatedBy)
	updateField = append(updateField, t.UpdatedBy)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllProductHelpDoc error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.ProductHelpDoc_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllProductHelpDoc error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *ProductHelpDocSvc) UpdateFieldsProductHelpDoc(req *proto.ProductHelpDocUpdateFieldsRequest) (*proto.ProductHelpDoc, error) {
	t := orm.Use(iotmodel.GetDB()).TProductHelpDoc
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsProductHelpDoc error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryId := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryId = true
	}
	HasRelationId := false
	if req.Data.RelationId != 0 { //整数
		do = do.Where(t.RelationId.Eq(req.Data.RelationId))
		HasRelationId = true
	}
	if req.Data.Lang != "" { //整数
		do = do.Where(t.Lang.Eq(req.Data.Lang))
	}
	if HasPrimaryId && HasRelationId {
		logger.Error("UpdateFieldsProductHelpDoc error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.ProductHelpDoc_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsProductHelpDoc error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找ProductHelpDoc
func (s *ProductHelpDocSvc) FindProductHelpDoc(req *proto.ProductHelpDocFilter) (*proto.ProductHelpDoc, error) {
	t := orm.Use(iotmodel.GetDB()).TProductHelpDoc
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.TenantId != "" { //字符串
		do = do.Where(t.TenantId.Eq(req.TenantId))
	}
	if req.ProductKey != "" { //字符串
		do = do.Where(t.ProductKey.Eq(req.ProductKey))
	}
	if req.Lang != "" { //字符串
		do = do.Where(t.Lang.Eq(req.Lang))
	}
	if req.Title != "" { //字符串
		do = do.Where(t.Title.Eq(req.Title))
	}
	if req.Content != "" { //字符串
		do = do.Where(t.Content.Eq(req.Content))
	}
	if req.RelationId != 0 { //整数
		do = do.Where(t.RelationId.Eq(req.RelationId))
	}
	if req.SortId != 0 { //整数
		do = do.Where(t.SortId.Eq(req.SortId))
	}
	if req.Status != 0 { //整数
		do = do.Where(t.Status.Eq(req.Status))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindProductHelpDoc error : %s", err.Error())
		return nil, err
	}
	res := convert.ProductHelpDoc_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找ProductHelpDoc
func (s *ProductHelpDocSvc) FindByIdProductHelpDoc(req *proto.ProductHelpDocFilter) (*proto.ProductHelpDoc, error) {
	t := orm.Use(iotmodel.GetDB()).TProductHelpDoc
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdProductHelpDoc error : %s", err.Error())
		return nil, err
	}
	res := convert.ProductHelpDoc_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找ProductHelpDoc,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *ProductHelpDocSvc) GetListProductHelpDoc(req *proto.ProductHelpDocListRequest) ([]*proto.ProductHelpDoc, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	q := orm.Use(iotmodel.GetDB())
	t := q.TProductHelpDoc
	tDir := q.TProductHelpConf

	do := t.WithContext(context.Background()).LeftJoin(tDir, tDir.ProductKey.EqCol(t.ProductKey))
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.TenantId != "" { //字符串
			do = do.Where(t.TenantId.Like("%" + query.TenantId + "%"))
		}
		if query.ProductKey != "" { //字符串
			do = do.Where(t.ProductKey.Like("%" + query.ProductKey + "%"))
		}
		if query.Lang != "" { //字符串
			do = do.Where(t.Lang.Like("%" + query.Lang + "%"))
		}
		if query.Title != "" { //字符串
			do = do.Where(t.Title.Like("%" + query.Title + "%"))
		}
		if query.Content != "" { //字符串
			do = do.Where(t.Content.Like("%" + query.Content + "%"))
		}
		if query.RelationId != 0 { //整数
			do = do.Where(t.RelationId.Eq(query.RelationId))
		}
		if query.SortId != 0 { //整数
			do = do.Where(t.SortId.Eq(query.SortId))
		}
		if query.Status != 0 { //整数
			do = do.Where(t.Status.Eq(query.Status), tDir.Status.Eq(query.Status))
		}
		if query.CreatedBy != 0 { //整数
			do = do.Where(t.CreatedBy.Eq(query.CreatedBy))
		}
		if query.UpdatedBy != 0 { //整数
			do = do.Where(t.UpdatedBy.Eq(query.UpdatedBy))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TProductHelpDoc
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListProductHelpDoc error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.ProductHelpDoc, len(list))
	for i, v := range list {
		result[i] = convert.ProductHelpDoc_db2pb(v)
	}
	return result, total, nil
}
