// Code generated by sgen.exe,2023-01-03 17:42:03. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_product/model"
	"cloud_platform/iot_model/db_product/orm"
	"cloud_platform/iot_product_service/convert"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type OpmProductMaterialLanguageSvc struct {
	Ctx context.Context
}

// 创建OpmProductMaterialLanguage
func (s *OpmProductMaterialLanguageSvc) CreateOpmProductMaterialLanguage(req *proto.OpmProductMaterialLanguage) (*proto.OpmProductMaterialLanguage, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TOpmProductMaterialLanguage
	do := t.WithContext(context.Background())
	dbObj := convert.OpmProductMaterialLanguage_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateOpmProductMaterialLanguage error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除OpmProductMaterialLanguage
func (s *OpmProductMaterialLanguageSvc) DeleteOpmProductMaterialLanguage(req *proto.OpmProductMaterialLanguage) (*proto.OpmProductMaterialLanguage, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmProductMaterialLanguage
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.MaterialId != 0 { //整数
		do = do.Where(t.MaterialId.Eq(req.MaterialId))
	}
	if req.Lang != "" { //字符串
		do = do.Where(t.Lang.Eq(req.Lang))
	}
	if req.Name != "" { //字符串
		do = do.Where(t.Name.Eq(req.Name))
	}
	if req.BrandName != "" { //字符串
		do = do.Where(t.BrandName.Eq(req.BrandName))
	}
	if req.FragranceName != "" { //字符串
		do = do.Where(t.FragranceName.Eq(req.FragranceName))
	}
	if req.ProductAddress != "" { //字符串
		do = do.Where(t.ProductAddress.Eq(req.ProductAddress))
	}
	if req.Variety != "" { //字符串
		do = do.Where(t.Variety.Eq(req.Variety))
	}
	if req.Ingredient != "" { //字符串
		do = do.Where(t.Ingredient.Eq(req.Ingredient))
	}
	if req.Effect != "" { //字符串
		do = do.Where(t.Effect.Eq(req.Effect))
	}
	if req.Description != "" { //字符串
		do = do.Where(t.Description.Eq(req.Description))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteOpmProductMaterialLanguage error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除OpmProductMaterialLanguage
func (s *OpmProductMaterialLanguageSvc) DeleteByIdOpmProductMaterialLanguage(req *proto.OpmProductMaterialLanguage) (*proto.OpmProductMaterialLanguage, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmProductMaterialLanguage
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdOpmProductMaterialLanguage error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除OpmProductMaterialLanguage
func (s *OpmProductMaterialLanguageSvc) DeleteByIdsOpmProductMaterialLanguage(req *proto.OpmProductMaterialLanguageBatchDeleteRequest) (*proto.OpmProductMaterialLanguageBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TOpmProductMaterialLanguage
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsOpmProductMaterialLanguage error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新OpmProductMaterialLanguage
func (s *OpmProductMaterialLanguageSvc) UpdateOpmProductMaterialLanguage(req *proto.OpmProductMaterialLanguage) (*proto.OpmProductMaterialLanguage, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmProductMaterialLanguage
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.MaterialId != 0 { //整数
		updateField = append(updateField, t.MaterialId)
	}
	if req.Lang != "" { //字符串
		updateField = append(updateField, t.Lang)
	}
	if req.Name != "" { //字符串
		updateField = append(updateField, t.Name)
	}
	if req.BrandName != "" { //字符串
		updateField = append(updateField, t.BrandName)
	}
	if req.FragranceName != "" { //字符串
		updateField = append(updateField, t.FragranceName)
	}
	if req.ProductAddress != "" { //字符串
		updateField = append(updateField, t.ProductAddress)
	}
	if req.Variety != "" { //字符串
		updateField = append(updateField, t.Variety)
	}
	if req.Ingredient != "" { //字符串
		updateField = append(updateField, t.Ingredient)
	}
	if req.Effect != "" { //字符串
		updateField = append(updateField, t.Effect)
	}
	if req.Description != "" { //字符串
		updateField = append(updateField, t.Description)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateOpmProductMaterialLanguage error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.OpmProductMaterialLanguage_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateOpmProductMaterialLanguage error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段OpmProductMaterialLanguage
func (s *OpmProductMaterialLanguageSvc) UpdateAllOpmProductMaterialLanguage(req *proto.OpmProductMaterialLanguage) (*proto.OpmProductMaterialLanguage, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmProductMaterialLanguage
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.MaterialId)
	updateField = append(updateField, t.Lang)
	updateField = append(updateField, t.Name)
	updateField = append(updateField, t.BrandName)
	updateField = append(updateField, t.FragranceName)
	updateField = append(updateField, t.ProductAddress)
	updateField = append(updateField, t.Variety)
	updateField = append(updateField, t.Ingredient)
	updateField = append(updateField, t.Effect)
	updateField = append(updateField, t.Description)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllOpmProductMaterialLanguage error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OpmProductMaterialLanguage_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllOpmProductMaterialLanguage error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *OpmProductMaterialLanguageSvc) UpdateFieldsOpmProductMaterialLanguage(req *proto.OpmProductMaterialLanguageUpdateFieldsRequest) (*proto.OpmProductMaterialLanguage, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmProductMaterialLanguage
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsOpmProductMaterialLanguage error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsOpmProductMaterialLanguage error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OpmProductMaterialLanguage_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsOpmProductMaterialLanguage error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找OpmProductMaterialLanguage
func (s *OpmProductMaterialLanguageSvc) FindOpmProductMaterialLanguage(req *proto.OpmProductMaterialLanguageFilter) (*proto.OpmProductMaterialLanguage, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmProductMaterialLanguage
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.MaterialId != 0 { //整数
		do = do.Where(t.MaterialId.Eq(req.MaterialId))
	}
	if req.Lang != "" { //字符串
		do = do.Where(t.Lang.Eq(req.Lang))
	}
	if req.Name != "" { //字符串
		do = do.Where(t.Name.Eq(req.Name))
	}
	if req.BrandName != "" { //字符串
		do = do.Where(t.BrandName.Eq(req.BrandName))
	}
	if req.FragranceName != "" { //字符串
		do = do.Where(t.FragranceName.Eq(req.FragranceName))
	}
	if req.ProductAddress != "" { //字符串
		do = do.Where(t.ProductAddress.Eq(req.ProductAddress))
	}
	if req.Variety != "" { //字符串
		do = do.Where(t.Variety.Eq(req.Variety))
	}
	if req.Ingredient != "" { //字符串
		do = do.Where(t.Ingredient.Eq(req.Ingredient))
	}
	if req.Effect != "" { //字符串
		do = do.Where(t.Effect.Eq(req.Effect))
	}
	if req.Description != "" { //字符串
		do = do.Where(t.Description.Eq(req.Description))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindOpmProductMaterialLanguage error : %s", err.Error())
		return nil, err
	}
	res := convert.OpmProductMaterialLanguage_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找OpmProductMaterialLanguage
func (s *OpmProductMaterialLanguageSvc) FindByIdOpmProductMaterialLanguage(req *proto.OpmProductMaterialLanguageFilter) (*proto.OpmProductMaterialLanguage, error) {
	t := orm.Use(iotmodel.GetDB()).TOpmProductMaterialLanguage
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdOpmProductMaterialLanguage error : %s", err.Error())
		return nil, err
	}
	res := convert.OpmProductMaterialLanguage_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找OpmProductMaterialLanguage,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *OpmProductMaterialLanguageSvc) GetListOpmProductMaterialLanguage(req *proto.OpmProductMaterialLanguageListRequest) ([]*proto.OpmProductMaterialLanguage, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TOpmProductMaterialLanguage
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.MaterialId != 0 { //整数
			do = do.Where(t.MaterialId.Eq(query.MaterialId))
		}
		if query.Lang != "" { //字符串
			do = do.Where(t.Lang.Like("%" + query.Lang + "%"))
		}
		if query.Name != "" { //字符串
			do = do.Where(t.Name.Like("%" + query.Name + "%"))
		}
		if query.BrandName != "" { //字符串
			do = do.Where(t.BrandName.Like("%" + query.BrandName + "%"))
		}
		if query.FragranceName != "" { //字符串
			do = do.Where(t.FragranceName.Like("%" + query.FragranceName + "%"))
		}
		if query.ProductAddress != "" { //字符串
			do = do.Where(t.ProductAddress.Like("%" + query.ProductAddress + "%"))
		}
		if query.Variety != "" { //字符串
			do = do.Where(t.Variety.Like("%" + query.Variety + "%"))
		}
		if query.Ingredient != "" { //字符串
			do = do.Where(t.Ingredient.Like("%" + query.Ingredient + "%"))
		}
		if query.Effect != "" { //字符串
			do = do.Where(t.Effect.Like("%" + query.Effect + "%"))
		}
		if query.Description != "" { //字符串
			do = do.Where(t.Description.Like("%" + query.Description + "%"))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TOpmProductMaterialLanguage
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListOpmProductMaterialLanguage error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.OpmProductMaterialLanguage, len(list))
	for i, v := range list {
		result[i] = convert.OpmProductMaterialLanguage_db2pb(v)
	}
	return result, total, nil
}
