// Code generated by sgen.exe,2022-04-20 13:52:29. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_product/model"
	"cloud_platform/iot_model/db_product/orm"
	"cloud_platform/iot_product_service/convert"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type PmFirmwareSettingSvc struct {
	Ctx context.Context
}

// 创建PmFirmwareSetting
func (s *PmFirmwareSettingSvc) CreatePmFirmwareSetting(req *proto.PmFirmwareSetting) (*proto.PmFirmwareSetting, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TPmFirmwareSetting
	do := t.WithContext(context.Background())
	dbObj := convert.PmFirmwareSetting_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreatePmFirmwareSetting error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除PmFirmwareSetting
func (s *PmFirmwareSettingSvc) DeletePmFirmwareSetting(req *proto.PmFirmwareSetting) (*proto.PmFirmwareSetting, error) {
	t := orm.Use(iotmodel.GetDB()).TPmFirmwareSetting
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.ModuleId != "" { //字符串
		do = do.Where(t.ModuleId.Eq(req.ModuleId))
	}
	if req.ProductModel != "" { //字符串
		do = do.Where(t.ProductModel.Eq(req.ProductModel))
	}
	if req.Desc != "" { //字符串
		do = do.Where(t.Desc.Eq(req.Desc))
	}
	if req.Value != "" { //字符串
		do = do.Where(t.Value.Eq(req.Value))
	}
	if req.Remark != "" { //字符串
		do = do.Where(t.Remark.Eq(req.Remark))
	}
	if req.Version != "" { //字符串
		do = do.Where(t.Version.Eq(req.Version))
	}
	if req.UpdatedBy != "" { //字符串
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeletePmFirmwareSetting error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除PmFirmwareSetting
func (s *PmFirmwareSettingSvc) DeleteByIdPmFirmwareSetting(req *proto.PmFirmwareSetting) (*proto.PmFirmwareSetting, error) {
	t := orm.Use(iotmodel.GetDB()).TPmFirmwareSetting
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdPmFirmwareSetting error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除PmFirmwareSetting
func (s *PmFirmwareSettingSvc) DeleteByIdsPmFirmwareSetting(req *proto.PmFirmwareSettingBatchDeleteRequest) (*proto.PmFirmwareSettingBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TPmFirmwareSetting
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsPmFirmwareSetting error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新PmFirmwareSetting
func (s *PmFirmwareSettingSvc) UpdatePmFirmwareSetting(req *proto.PmFirmwareSetting) (*proto.PmFirmwareSetting, error) {
	t := orm.Use(iotmodel.GetDB()).TPmFirmwareSetting
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.ModuleId != "" { //字符串
		updateField = append(updateField, t.ModuleId)
	}
	if req.ProductModel != "" { //字符串
		updateField = append(updateField, t.ProductModel)
	}
	if req.Desc != "" { //字符串
		updateField = append(updateField, t.Desc)
	}
	if req.Value != "" { //字符串
		updateField = append(updateField, t.Value)
	}
	if req.Remark != "" { //字符串
		updateField = append(updateField, t.Remark)
	}
	if req.Version != "" { //字符串
		updateField = append(updateField, t.Version)
	}
	if req.UpdatedBy != "" { //字符串
		updateField = append(updateField, t.UpdatedBy)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdatePmFirmwareSetting error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.PmFirmwareSetting_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdatePmFirmwareSetting error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段PmFirmwareSetting
func (s *PmFirmwareSettingSvc) UpdateAllPmFirmwareSetting(req *proto.PmFirmwareSetting) (*proto.PmFirmwareSetting, error) {
	t := orm.Use(iotmodel.GetDB()).TPmFirmwareSetting
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.ModuleId)
	updateField = append(updateField, t.ProductModel)
	updateField = append(updateField, t.Desc)
	updateField = append(updateField, t.Value)
	updateField = append(updateField, t.Remark)
	updateField = append(updateField, t.Version)
	updateField = append(updateField, t.UpdatedBy)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllPmFirmwareSetting error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.PmFirmwareSetting_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllPmFirmwareSetting error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *PmFirmwareSettingSvc) UpdateFieldsPmFirmwareSetting(req *proto.PmFirmwareSettingUpdateFieldsRequest) (*proto.PmFirmwareSetting, error) {
	t := orm.Use(iotmodel.GetDB()).TPmFirmwareSetting
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsPmFirmwareSetting error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsPmFirmwareSetting error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.PmFirmwareSetting_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsPmFirmwareSetting error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找PmFirmwareSetting
func (s *PmFirmwareSettingSvc) FindPmFirmwareSetting(req *proto.PmFirmwareSettingFilter) (*proto.PmFirmwareSetting, error) {
	t := orm.Use(iotmodel.GetDB()).TPmFirmwareSetting
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.ModuleId != "" { //字符串
		do = do.Where(t.ModuleId.Like("%" + req.ModuleId + "%"))
	}
	if req.ProductModel != "" { //字符串
		do = do.Where(t.ProductModel.Like("%" + req.ProductModel + "%"))
	}
	if req.Desc != "" { //字符串
		do = do.Where(t.Desc.Like("%" + req.Desc + "%"))
	}
	if req.Value != "" { //字符串
		do = do.Where(t.Value.Like("%" + req.Value + "%"))
	}
	if req.Remark != "" { //字符串
		do = do.Where(t.Remark.Like("%" + req.Remark + "%"))
	}
	if req.Version != "" { //字符串
		do = do.Where(t.Version.Like("%" + req.Version + "%"))
	}
	if req.UpdatedBy != "" { //字符串
		do = do.Where(t.UpdatedBy.Like("%" + req.UpdatedBy + "%"))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindPmFirmwareSetting error : %s", err.Error())
		return nil, err
	}
	res := convert.PmFirmwareSetting_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找PmFirmwareSetting
func (s *PmFirmwareSettingSvc) FindByIdPmFirmwareSetting(req *proto.PmFirmwareSettingFilter) (*proto.PmFirmwareSetting, error) {
	t := orm.Use(iotmodel.GetDB()).TPmFirmwareSetting
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdPmFirmwareSetting error : %s", err.Error())
		return nil, err
	}
	res := convert.PmFirmwareSetting_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找PmFirmwareSetting,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *PmFirmwareSettingSvc) GetListPmFirmwareSetting(req *proto.PmFirmwareSettingListRequest) ([]*proto.PmFirmwareSetting, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TPmFirmwareSetting
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.ModuleId != "" { //字符串
			do = do.Where(t.ModuleId.Like("%" + query.ModuleId + "%"))
		}
		if query.ProductModel != "" { //字符串
			do = do.Where(t.ProductModel.Like("%" + query.ProductModel + "%"))
		}
		if query.Desc != "" { //字符串
			do = do.Where(t.Desc.Like("%" + query.Desc + "%"))
		}
		if query.Value != "" { //字符串
			do = do.Where(t.Value.Like("%" + query.Value + "%"))
		}
		if query.Remark != "" { //字符串
			do = do.Where(t.Remark.Like("%" + query.Remark + "%"))
		}
		if query.Version != "" { //字符串
			do = do.Where(t.Version.Like("%" + query.Version + "%"))
		}
		if query.UpdatedBy != "" { //字符串
			do = do.Where(t.UpdatedBy.Like("%" + query.UpdatedBy + "%"))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.CreatedAt
	}
	if req.OrderDesc != "asc" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TPmFirmwareSetting
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.Order(t.CreatedAt.Desc()).FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListPmFirmwareSetting error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.PmFirmwareSetting, len(list))
	for i, v := range list {
		result[i] = convert.PmFirmwareSetting_db2pb(v)
	}
	return result, total, nil
}
