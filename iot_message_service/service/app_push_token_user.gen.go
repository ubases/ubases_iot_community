// Code generated by sgen,2023-08-12 17:29:59. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"cloud_platform/iot_common/iotutil"
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	"cloud_platform/iot_message_service/convert"
	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_message/model"
	"cloud_platform/iot_model/db_message/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type AppPushTokenUserSvc struct {
	Ctx context.Context
}

// 创建AppPushTokenUser
func (s *AppPushTokenUserSvc) CreateAppPushTokenUser(req *proto.AppPushTokenUser) (*proto.AppPushTokenUser, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TAppPushTokenUser
	do := t.WithContext(context.Background())
	dbObj := convert.AppPushTokenUser_pb2db(req)
	//先执行删除
	do.Where(t.AppPushId.Eq(req.AppPushId)).Delete()
	//创建绑定关系
	dbObj.Id = iotutil.GetNextSeqInt64()
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateAppPushTokenUser error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除AppPushTokenUser
func (s *AppPushTokenUserSvc) DeleteAppPushTokenUser(req *proto.AppPushTokenUser) (*proto.AppPushTokenUser, error) {
	t := orm.Use(iotmodel.GetDB()).TAppPushTokenUser
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.UserId != 0 { //整数
		do = do.Where(t.UserId.Eq(req.UserId))
	}
	if req.AppPushId != "" { //字符串
		do = do.Where(t.AppPushId.Eq(req.AppPushId))
	}
	if req.AppKey != "" { //字符串
		do = do.Where(t.AppKey.Eq(req.AppKey))
	}
	if req.TenantId != "" { //字符串
		do = do.Where(t.TenantId.Eq(req.TenantId))
	}
	if req.RegionId != 0 { //整数
		do = do.Where(t.RegionId.Eq(req.RegionId))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteAppPushTokenUser error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除AppPushTokenUser
func (s *AppPushTokenUserSvc) DeleteByIdAppPushTokenUser(req *proto.AppPushTokenUser) (*proto.AppPushTokenUser, error) {
	t := orm.Use(iotmodel.GetDB()).TAppPushTokenUser
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdAppPushTokenUser error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除AppPushTokenUser
func (s *AppPushTokenUserSvc) DeleteByIdsAppPushTokenUser(req *proto.AppPushTokenUserBatchDeleteRequest) (*proto.AppPushTokenUserBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TAppPushTokenUser
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsAppPushTokenUser error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新AppPushTokenUser
func (s *AppPushTokenUserSvc) UpdateAppPushTokenUser(req *proto.AppPushTokenUser) (*proto.AppPushTokenUser, error) {
	t := orm.Use(iotmodel.GetDB()).TAppPushTokenUser
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.UserId != 0 { //整数
		updateField = append(updateField, t.UserId)
	}
	if req.AppPushId != "" { //字符串
		updateField = append(updateField, t.AppPushId)
	}
	if req.AppKey != "" { //字符串
		updateField = append(updateField, t.AppKey)
	}
	if req.TenantId != "" { //字符串
		updateField = append(updateField, t.TenantId)
	}
	if req.RegionId != 0 { //整数
		updateField = append(updateField, t.RegionId)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateAppPushTokenUser error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.AppPushTokenUser_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAppPushTokenUser error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段AppPushTokenUser
func (s *AppPushTokenUserSvc) UpdateAllAppPushTokenUser(req *proto.AppPushTokenUser) (*proto.AppPushTokenUser, error) {
	t := orm.Use(iotmodel.GetDB()).TAppPushTokenUser
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.UserId)
	updateField = append(updateField, t.AppPushId)
	updateField = append(updateField, t.AppKey)
	updateField = append(updateField, t.TenantId)
	updateField = append(updateField, t.RegionId)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllAppPushTokenUser error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.AppPushTokenUser_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllAppPushTokenUser error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *AppPushTokenUserSvc) UpdateFieldsAppPushTokenUser(req *proto.AppPushTokenUserUpdateFieldsRequest) (*proto.AppPushTokenUser, error) {
	t := orm.Use(iotmodel.GetDB()).TAppPushTokenUser
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsAppPushTokenUser error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsAppPushTokenUser error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.AppPushTokenUser_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsAppPushTokenUser error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找AppPushTokenUser
func (s *AppPushTokenUserSvc) FindAppPushTokenUser(req *proto.AppPushTokenUserFilter) (*proto.AppPushTokenUser, error) {
	t := orm.Use(iotmodel.GetDB()).TAppPushTokenUser
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.UserId != 0 { //整数
		do = do.Where(t.UserId.Eq(req.UserId))
	}
	if req.AppPushId != "" { //字符串
		do = do.Where(t.AppPushId.Eq(req.AppPushId))
	}
	if req.AppKey != "" { //字符串
		do = do.Where(t.AppKey.Eq(req.AppKey))
	}
	if req.TenantId != "" { //字符串
		do = do.Where(t.TenantId.Eq(req.TenantId))
	}
	if req.RegionId != 0 { //整数
		do = do.Where(t.RegionId.Eq(req.RegionId))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindAppPushTokenUser error : %s", err.Error())
		return nil, err
	}
	res := convert.AppPushTokenUser_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找AppPushTokenUser
func (s *AppPushTokenUserSvc) FindByIdAppPushTokenUser(req *proto.AppPushTokenUserFilter) (*proto.AppPushTokenUser, error) {
	t := orm.Use(iotmodel.GetDB()).TAppPushTokenUser
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdAppPushTokenUser error : %s", err.Error())
		return nil, err
	}
	res := convert.AppPushTokenUser_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找AppPushTokenUser,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *AppPushTokenUserSvc) GetListAppPushTokenUser(req *proto.AppPushTokenUserListRequest) ([]*proto.AppPushTokenUser, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TAppPushTokenUser
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.UserId != 0 { //整数
			do = do.Where(t.UserId.Eq(query.UserId))
		}
		if query.AppPushId != "" { //字符串
			do = do.Where(t.AppPushId.Like("%" + query.AppPushId + "%"))
		}
		if query.AppKey != "" { //字符串
			do = do.Where(t.AppKey.Like("%" + query.AppKey + "%"))
		}
		if query.TenantId != "" { //字符串
			do = do.Where(t.TenantId.Like("%" + query.TenantId + "%"))
		}
		if query.RegionId != 0 { //整数
			do = do.Where(t.RegionId.Eq(query.RegionId))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TAppPushTokenUser
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListAppPushTokenUser error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.AppPushTokenUser, len(list))
	for i, v := range list {
		result[i] = convert.AppPushTokenUser_db2pb(v)
	}
	return result, total, nil
}
