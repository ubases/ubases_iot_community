// Code generated by sgen.exe,2022-05-17 13:13:13. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	"cloud_platform/iot_basic_service/convert"
	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_config/model"
	"cloud_platform/iot_model/db_config/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type LangTranslateTypeSvc struct {
	Ctx context.Context
}

// 创建LangTranslateType
func (s *LangTranslateTypeSvc) CreateLangTranslateType(req *proto.LangTranslateType) (*proto.LangTranslateType, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TLangTranslateType
	do := t.WithContext(context.Background())
	dbObj := convert.LangTranslateType_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateLangTranslateType error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除LangTranslateType
func (s *LangTranslateTypeSvc) DeleteLangTranslateType(req *proto.LangTranslateType) (*proto.LangTranslateType, error) {
	t := orm.Use(iotmodel.GetDB()).TLangTranslateType
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.Name != "" { //字符串
		do = do.Where(t.Name.Eq(req.Name))
	}
	if req.Code != "" { //字符串
		do = do.Where(t.Code.Eq(req.Code))
	}
	if req.Status != 0 { //整数
		do = do.Where(t.Status.Eq(req.Status))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteLangTranslateType error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除LangTranslateType
func (s *LangTranslateTypeSvc) DeleteByIdLangTranslateType(req *proto.LangTranslateType) (*proto.LangTranslateType, error) {
	t := orm.Use(iotmodel.GetDB()).TLangTranslateType
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdLangTranslateType error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除LangTranslateType
func (s *LangTranslateTypeSvc) DeleteByIdsLangTranslateType(req *proto.LangTranslateTypeBatchDeleteRequest) (*proto.LangTranslateTypeBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TLangTranslateType
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsLangTranslateType error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新LangTranslateType
func (s *LangTranslateTypeSvc) UpdateLangTranslateType(req *proto.LangTranslateType) (*proto.LangTranslateType, error) {
	t := orm.Use(iotmodel.GetDB()).TLangTranslateType
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.Name != "" { //字符串
		updateField = append(updateField, t.Name)
	}
	if req.Code != "" { //字符串
		updateField = append(updateField, t.Code)
	}
	if req.Status != 0 { //整数
		updateField = append(updateField, t.Status)
	}
	if req.CreatedBy != 0 { //整数
		updateField = append(updateField, t.CreatedBy)
	}
	if req.UpdatedBy != 0 { //整数
		updateField = append(updateField, t.UpdatedBy)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateLangTranslateType error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.LangTranslateType_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateLangTranslateType error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段LangTranslateType
func (s *LangTranslateTypeSvc) UpdateAllLangTranslateType(req *proto.LangTranslateType) (*proto.LangTranslateType, error) {
	t := orm.Use(iotmodel.GetDB()).TLangTranslateType
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.Name)
	updateField = append(updateField, t.Code)
	updateField = append(updateField, t.Status)
	updateField = append(updateField, t.CreatedBy)
	updateField = append(updateField, t.UpdatedBy)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllLangTranslateType error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.LangTranslateType_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllLangTranslateType error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *LangTranslateTypeSvc) UpdateFieldsLangTranslateType(req *proto.LangTranslateTypeUpdateFieldsRequest) (*proto.LangTranslateType, error) {
	t := orm.Use(iotmodel.GetDB()).TLangTranslateType
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsLangTranslateType error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsLangTranslateType error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.LangTranslateType_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsLangTranslateType error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找LangTranslateType
func (s *LangTranslateTypeSvc) FindLangTranslateType(req *proto.LangTranslateTypeFilter) (*proto.LangTranslateType, error) {
	t := orm.Use(iotmodel.GetDB()).TLangTranslateType
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.Name != "" { //字符串
		do = do.Where(t.Name.Eq(req.Name))
	}
	if req.Code != "" { //字符串
		do = do.Where(t.Code.Eq(req.Code))
	}
	if req.Status != 0 { //整数
		do = do.Where(t.Status.Eq(req.Status))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindLangTranslateType error : %s", err.Error())
		return nil, err
	}
	res := convert.LangTranslateType_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找LangTranslateType
func (s *LangTranslateTypeSvc) FindByIdLangTranslateType(req *proto.LangTranslateTypeFilter) (*proto.LangTranslateType, error) {
	t := orm.Use(iotmodel.GetDB()).TLangTranslateType
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdLangTranslateType error : %s", err.Error())
		return nil, err
	}
	res := convert.LangTranslateType_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找LangTranslateType,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *LangTranslateTypeSvc) GetListLangTranslateType(req *proto.LangTranslateTypeListRequest) ([]*proto.LangTranslateType, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TLangTranslateType
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.Name != "" { //字符串
			do = do.Where(t.Name.Like("%" + query.Name + "%"))
		}
		if query.Code != "" { //字符串
			do = do.Where(t.Code.Like("%" + query.Code + "%"))
		}
		if query.Status != 0 { //整数
			do = do.Where(t.Status.Eq(query.Status))
		}
		if query.CreatedBy != 0 { //整数
			do = do.Where(t.CreatedBy.Eq(query.CreatedBy))
		}
		if query.UpdatedBy != 0 { //整数
			do = do.Where(t.UpdatedBy.Eq(query.UpdatedBy))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TLangTranslateType
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListLangTranslateType error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.LangTranslateType, len(list))
	for i, v := range list {
		result[i] = convert.LangTranslateType_db2pb(v)
	}
	return result, total, nil
}
