// Code generated by sgen.exe,2022-04-19 09:58:33. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	"cloud_platform/iot_basic_service/convert"
	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_config/model"
	"cloud_platform/iot_model/db_config/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type ConfigLicenseSvc struct {
	Ctx context.Context
}

// 创建ConfigLicense
func (s *ConfigLicenseSvc) CreateConfigLicense(req *proto.ConfigLicense) (*proto.ConfigLicense, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TConfigLicense
	do := t.WithContext(context.Background())
	dbObj := convert.ConfigLicense_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateConfigLicense error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除ConfigLicense
func (s *ConfigLicenseSvc) DeleteConfigLicense(req *proto.ConfigLicense) (*proto.ConfigLicense, error) {
	t := orm.Use(iotmodel.GetDB()).TConfigLicense
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.BelongType != 0 { //整数
		do = do.Where(t.BelongType.Eq(req.BelongType))
	}
	if req.BelongId != 0 { //整数
		do = do.Where(t.BelongId.Eq(req.BelongId))
	}
	if req.MachineId != "" { //字符串
		do = do.Where(t.MachineId.Eq(req.MachineId))
	}
	if req.License != "" { //字符串
		do = do.Where(t.License.Eq(req.License))
	}
	if req.LicenseType != 0 { //整数
		do = do.Where(t.LicenseType.Eq(req.LicenseType))
	}
	if req.AgentNumber != 0 { //整数
		do = do.Where(t.AgentNumber.Eq(req.AgentNumber))
	}
	if req.SurplusagentNumber != 0 { //整数
		do = do.Where(t.SurplusagentNumber.Eq(req.SurplusagentNumber))
	}
	if req.Type != 0 { //整数
		do = do.Where(t.Type.Eq(req.Type))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteConfigLicense error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除ConfigLicense
func (s *ConfigLicenseSvc) DeleteByIdConfigLicense(req *proto.ConfigLicense) (*proto.ConfigLicense, error) {
	t := orm.Use(iotmodel.GetDB()).TConfigLicense
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.BelongType != 0 { //整数
		do = do.Where(t.BelongType.Eq(req.BelongType))
	}
	if req.MachineId != "" { //字符串
		do = do.Where(t.MachineId.Eq(req.MachineId))
	}
	if req.License != "" { //字符串
		do = do.Where(t.License.Eq(req.License))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdConfigLicense error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除ConfigLicense
func (s *ConfigLicenseSvc) DeleteByIdsConfigLicense(req *proto.ConfigLicenseBatchDeleteRequest) (*proto.ConfigLicenseBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TConfigLicense
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		do = do.Where(t.BelongType.Eq(k.BelongType))

		do = do.Where(t.MachineId.Eq(k.MachineId))

		do = do.Where(t.License.Eq(k.License))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsConfigLicense error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新ConfigLicense
func (s *ConfigLicenseSvc) UpdateConfigLicense(req *proto.ConfigLicense) (*proto.ConfigLicense, error) {
	t := orm.Use(iotmodel.GetDB()).TConfigLicense
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.BelongId != 0 { //整数
		updateField = append(updateField, t.BelongId)
	}
	if req.LicenseType != 0 { //整数
		updateField = append(updateField, t.LicenseType)
	}
	if req.AgentNumber != 0 { //整数
		updateField = append(updateField, t.AgentNumber)
	}
	if req.SurplusagentNumber != 0 { //整数
		updateField = append(updateField, t.SurplusagentNumber)
	}
	if req.Type != 0 { //整数
		updateField = append(updateField, t.Type)
	}
	if req.CreatedBy != 0 { //整数
		updateField = append(updateField, t.CreatedBy)
	}
	if req.UpdatedBy != 0 { //整数
		updateField = append(updateField, t.UpdatedBy)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if req.BelongType != 0 { //整数
		do = do.Where(t.BelongType.Eq(req.BelongType))
		HasPrimaryKey = true
	}
	if req.MachineId != "" { //字符串
		do = do.Where(t.MachineId.Eq(req.MachineId))
		HasPrimaryKey = true
	}
	if req.License != "" { //字符串
		do = do.Where(t.License.Eq(req.License))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateConfigLicense error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.ConfigLicense_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateConfigLicense error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段ConfigLicense
func (s *ConfigLicenseSvc) UpdateAllConfigLicense(req *proto.ConfigLicense) (*proto.ConfigLicense, error) {
	t := orm.Use(iotmodel.GetDB()).TConfigLicense
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.BelongId)
	updateField = append(updateField, t.LicenseType)
	updateField = append(updateField, t.AgentNumber)
	updateField = append(updateField, t.SurplusagentNumber)
	updateField = append(updateField, t.Type)
	updateField = append(updateField, t.Timeout)
	updateField = append(updateField, t.CreatedBy)
	updateField = append(updateField, t.UpdatedBy)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if req.BelongType != 0 { //整数
		do = do.Where(t.BelongType.Eq(req.BelongType))
		HasPrimaryKey = true
	}
	if req.MachineId != "" { //字符串
		do = do.Where(t.MachineId.Eq(req.MachineId))
		HasPrimaryKey = true
	}
	if req.License != "" { //字符串
		do = do.Where(t.License.Eq(req.License))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllConfigLicense error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.ConfigLicense_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllConfigLicense error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *ConfigLicenseSvc) UpdateFieldsConfigLicense(req *proto.ConfigLicenseUpdateFieldsRequest) (*proto.ConfigLicense, error) {
	t := orm.Use(iotmodel.GetDB()).TConfigLicense
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if req.Data.BelongType != 0 { //整数
		do = do.Where(t.BelongType.Eq(req.Data.BelongType))
		HasPrimaryKey = true
	}
	if req.Data.MachineId != "" { //字符串
		do = do.Where(t.MachineId.Eq(req.Data.MachineId))
		HasPrimaryKey = true
	}
	if req.Data.License != "" { //字符串
		do = do.Where(t.License.Eq(req.Data.License))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsConfigLicense error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.ConfigLicense_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsConfigLicense error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找ConfigLicense
func (s *ConfigLicenseSvc) FindConfigLicense(req *proto.ConfigLicenseFilter) (*proto.ConfigLicense, error) {
	t := orm.Use(iotmodel.GetDB()).TConfigLicense
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.BelongType != 0 { //整数
		do = do.Where(t.BelongType.Eq(req.BelongType))
	}
	if req.BelongId != 0 { //整数
		do = do.Where(t.BelongId.Eq(req.BelongId))
	}
	if req.MachineId != "" { //字符串
		do = do.Where(t.MachineId.Like("%" + req.MachineId + "%"))
	}
	if req.License != "" { //字符串
		do = do.Where(t.License.Like("%" + req.License + "%"))
	}
	if req.LicenseType != 0 { //整数
		do = do.Where(t.LicenseType.Eq(req.LicenseType))
	}
	if req.AgentNumber != 0 { //整数
		do = do.Where(t.AgentNumber.Eq(req.AgentNumber))
	}
	if req.SurplusagentNumber != 0 { //整数
		do = do.Where(t.SurplusagentNumber.Eq(req.SurplusagentNumber))
	}
	if req.Type != 0 { //整数
		do = do.Where(t.Type.Eq(req.Type))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindConfigLicense error : %s", err.Error())
		return nil, err
	}
	res := convert.ConfigLicense_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找ConfigLicense
func (s *ConfigLicenseSvc) FindByIdConfigLicense(req *proto.ConfigLicenseFilter) (*proto.ConfigLicense, error) {
	t := orm.Use(iotmodel.GetDB()).TConfigLicense
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.BelongType != 0 { //整数
		do = do.Where(t.BelongType.Eq(req.BelongType))
	}
	if req.MachineId != "" { //字符串
		do = do.Where(t.MachineId.Eq(req.MachineId))
	}
	if req.License != "" { //字符串
		do = do.Where(t.License.Eq(req.License))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdConfigLicense error : %s", err.Error())
		return nil, err
	}
	res := convert.ConfigLicense_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找ConfigLicense,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *ConfigLicenseSvc) GetListConfigLicense(req *proto.ConfigLicenseListRequest) ([]*proto.ConfigLicense, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TConfigLicense
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.BelongType != 0 { //整数
			do = do.Where(t.BelongType.Eq(query.BelongType))
		}
		if query.BelongId != 0 { //整数
			do = do.Where(t.BelongId.Eq(query.BelongId))
		}
		if query.MachineId != "" { //字符串
			do = do.Where(t.MachineId.Like("%" + query.MachineId + "%"))
		}
		if query.License != "" { //字符串
			do = do.Where(t.License.Like("%" + query.License + "%"))
		}
		if query.LicenseType != 0 { //整数
			do = do.Where(t.LicenseType.Eq(query.LicenseType))
		}
		if query.AgentNumber != 0 { //整数
			do = do.Where(t.AgentNumber.Eq(query.AgentNumber))
		}
		if query.SurplusagentNumber != 0 { //整数
			do = do.Where(t.SurplusagentNumber.Eq(query.SurplusagentNumber))
		}
		if query.Type != 0 { //整数
			do = do.Where(t.Type.Eq(query.Type))
		}
		if query.CreatedBy != 0 { //整数
			do = do.Where(t.CreatedBy.Eq(query.CreatedBy))
		}
		if query.UpdatedBy != 0 { //整数
			do = do.Where(t.UpdatedBy.Eq(query.UpdatedBy))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
		orderCol = t.BelongType
		orderCol = t.MachineId
		orderCol = t.License
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TConfigLicense
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListConfigLicense error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.ConfigLicense, len(list))
	for i, v := range list {
		result[i] = convert.ConfigLicense_db2pb(v)
	}
	return result, total, nil
}
