// Code generated by sgen.exe,2022-04-19 09:58:35. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	"cloud_platform/iot_basic_service/convert"
	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_config/model"
	"cloud_platform/iot_model/db_config/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type SysRegionSvc struct {
	Ctx context.Context
}

// 创建SysRegion
func (s *SysRegionSvc) CreateSysRegion(req *proto.SysRegion) (*proto.SysRegion, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TSysRegion
	do := t.WithContext(context.Background())
	dbObj := convert.SysRegion_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateSysRegion error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除SysRegion
func (s *SysRegionSvc) DeleteSysRegion(req *proto.SysRegion) (*proto.SysRegion, error) {
	t := orm.Use(iotmodel.GetDB()).TSysRegion
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.Code != "" { //字符串
		do = do.Where(t.Code.Eq(req.Code))
	}
	if req.Name != "" { //字符串
		do = do.Where(t.Name.Eq(req.Name))
	}
	if req.Level != 0 { //整数
		do = do.Where(t.Level.Eq(req.Level))
	}
	if req.ParentId != 0 { //整数
		do = do.Where(t.ParentId.Eq(req.ParentId))
	}
	if req.Sid != 0 { //整数
		do = do.Where(t.Sid.Eq(req.Sid))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteSysRegion error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除SysRegion
func (s *SysRegionSvc) DeleteByIdSysRegion(req *proto.SysRegion) (*proto.SysRegion, error) {
	t := orm.Use(iotmodel.GetDB()).TSysRegion
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdSysRegion error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除SysRegion
func (s *SysRegionSvc) DeleteByIdsSysRegion(req *proto.SysRegionBatchDeleteRequest) (*proto.SysRegionBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TSysRegion
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsSysRegion error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新SysRegion
func (s *SysRegionSvc) UpdateSysRegion(req *proto.SysRegion) (*proto.SysRegion, error) {
	t := orm.Use(iotmodel.GetDB()).TSysRegion
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.Code != "" { //字符串
		updateField = append(updateField, t.Code)
	}
	if req.Name != "" { //字符串
		updateField = append(updateField, t.Name)
	}
	if req.Level != 0 { //整数
		updateField = append(updateField, t.Level)
	}
	if req.ParentId != 0 { //整数
		updateField = append(updateField, t.ParentId)
	}
	if req.Sid != 0 { //整数
		updateField = append(updateField, t.Sid)
	}
	if req.CreatedBy != 0 { //整数
		updateField = append(updateField, t.CreatedBy)
	}
	if req.UpdatedBy != 0 { //整数
		updateField = append(updateField, t.UpdatedBy)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateSysRegion error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.SysRegion_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateSysRegion error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段SysRegion
func (s *SysRegionSvc) UpdateAllSysRegion(req *proto.SysRegion) (*proto.SysRegion, error) {
	t := orm.Use(iotmodel.GetDB()).TSysRegion
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.Code)
	updateField = append(updateField, t.Name)
	updateField = append(updateField, t.Level)
	updateField = append(updateField, t.ParentId)
	updateField = append(updateField, t.Lat)
	updateField = append(updateField, t.Lng)
	updateField = append(updateField, t.Sid)
	updateField = append(updateField, t.CreatedBy)
	updateField = append(updateField, t.UpdatedBy)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllSysRegion error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.SysRegion_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllSysRegion error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *SysRegionSvc) UpdateFieldsSysRegion(req *proto.SysRegionUpdateFieldsRequest) (*proto.SysRegion, error) {
	t := orm.Use(iotmodel.GetDB()).TSysRegion
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsSysRegion error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.SysRegion_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsSysRegion error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找SysRegion
func (s *SysRegionSvc) FindSysRegion(req *proto.SysRegionFilter) (*proto.SysRegion, error) {
	t := orm.Use(iotmodel.GetDB()).TSysRegion
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.Code != "" { //字符串
		do = do.Where(t.Code.Like("%" + req.Code + "%"))
	}
	if req.Name != "" { //字符串
		do = do.Where(t.Name.Like("%" + req.Name + "%"))
	}
	if req.Level != 0 { //整数
		do = do.Where(t.Level.Eq(req.Level))
	}
	if req.ParentId != 0 { //整数
		do = do.Where(t.ParentId.Eq(req.ParentId))
	}
	if req.Sid != 0 { //整数
		do = do.Where(t.Sid.Eq(req.Sid))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindSysRegion error : %s", err.Error())
		return nil, err
	}
	res := convert.SysRegion_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找SysRegion
func (s *SysRegionSvc) FindByIdSysRegion(req *proto.SysRegionFilter) (*proto.SysRegion, error) {
	t := orm.Use(iotmodel.GetDB()).TSysRegion
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdSysRegion error : %s", err.Error())
		return nil, err
	}
	res := convert.SysRegion_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找SysRegion,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *SysRegionSvc) GetListSysRegion(req *proto.SysRegionListRequest) ([]*proto.SysRegion, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TSysRegion
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.Code != "" { //字符串
			do = do.Where(t.Code.Like("%" + query.Code + "%"))
		}
		if query.Name != "" { //字符串
			do = do.Where(t.Name.Like("%" + query.Name + "%"))
		}
		if query.Level != 0 { //整数
			do = do.Where(t.Level.Eq(query.Level))
		}
		if query.ParentId != 0 { //整数
			do = do.Where(t.ParentId.Eq(query.ParentId))
		}
		if query.Sid != 0 { //整数
			do = do.Where(t.Sid.Eq(query.Sid))
		}
		if query.CreatedBy != 0 { //整数
			do = do.Where(t.CreatedBy.Eq(query.CreatedBy))
		}
		if query.UpdatedBy != 0 { //整数
			do = do.Where(t.UpdatedBy.Eq(query.UpdatedBy))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TSysRegion
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListSysRegion error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.SysRegion, len(list))
	for i, v := range list {
		result[i] = convert.SysRegion_db2pb(v)
	}
	return result, total, nil
}
