// Code generated by sgen.exe,2022-04-19 09:58:34. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	"cloud_platform/iot_basic_service/convert"
	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_config/model"
	"cloud_platform/iot_model/db_config/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type ConfigTranslateSvc struct {
	Ctx context.Context
}

// 创建ConfigTranslate
func (s *ConfigTranslateSvc) CreateConfigTranslate(req *proto.ConfigTranslate) (*proto.ConfigTranslate, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TConfigTranslate
	do := t.WithContext(context.Background())
	dbObj := convert.ConfigTranslate_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateConfigTranslate error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除ConfigTranslate
func (s *ConfigTranslateSvc) DeleteConfigTranslate(req *proto.ConfigTranslate) (*proto.ConfigTranslate, error) {
	t := orm.Use(iotmodel.GetDB()).TConfigTranslate
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.Code != "" { //字符串
		do = do.Where(t.Code.Eq(req.Code))
	}
	if req.En != "" { //字符串
		do = do.Where(t.En.Eq(req.En))
	}
	if req.Zh != "" { //字符串
		do = do.Where(t.Zh.Eq(req.Zh))
	}
	if req.Jp != "" { //字符串
		do = do.Where(t.Jp.Eq(req.Jp))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteConfigTranslate error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除ConfigTranslate
func (s *ConfigTranslateSvc) DeleteByIdConfigTranslate(req *proto.ConfigTranslate) (*proto.ConfigTranslate, error) {
	t := orm.Use(iotmodel.GetDB()).TConfigTranslate
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdConfigTranslate error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除ConfigTranslate
func (s *ConfigTranslateSvc) DeleteByIdsConfigTranslate(req *proto.ConfigTranslateBatchDeleteRequest) (*proto.ConfigTranslateBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TConfigTranslate
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsConfigTranslate error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新ConfigTranslate
func (s *ConfigTranslateSvc) UpdateConfigTranslate(req *proto.ConfigTranslate) (*proto.ConfigTranslate, error) {
	t := orm.Use(iotmodel.GetDB()).TConfigTranslate
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.Code != "" { //字符串
		updateField = append(updateField, t.Code)
	}
	if req.En != "" { //字符串
		updateField = append(updateField, t.En)
	}
	if req.Zh != "" { //字符串
		updateField = append(updateField, t.Zh)
	}
	if req.Jp != "" { //字符串
		updateField = append(updateField, t.Jp)
	}
	if req.CreatedBy != 0 { //整数
		updateField = append(updateField, t.CreatedBy)
	}
	if req.UpdatedBy != 0 { //整数
		updateField = append(updateField, t.UpdatedBy)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateConfigTranslate error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.ConfigTranslate_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateConfigTranslate error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段ConfigTranslate
func (s *ConfigTranslateSvc) UpdateAllConfigTranslate(req *proto.ConfigTranslate) (*proto.ConfigTranslate, error) {
	t := orm.Use(iotmodel.GetDB()).TConfigTranslate
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.Code)
	updateField = append(updateField, t.En)
	updateField = append(updateField, t.Zh)
	updateField = append(updateField, t.Jp)
	updateField = append(updateField, t.CreatedBy)
	updateField = append(updateField, t.UpdatedBy)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllConfigTranslate error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.ConfigTranslate_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllConfigTranslate error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *ConfigTranslateSvc) UpdateFieldsConfigTranslate(req *proto.ConfigTranslateUpdateFieldsRequest) (*proto.ConfigTranslate, error) {
	t := orm.Use(iotmodel.GetDB()).TConfigTranslate
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsConfigTranslate error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.ConfigTranslate_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsConfigTranslate error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找ConfigTranslate
func (s *ConfigTranslateSvc) FindConfigTranslate(req *proto.ConfigTranslateFilter) (*proto.ConfigTranslate, error) {
	t := orm.Use(iotmodel.GetDB()).TConfigTranslate
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.Code != "" { //字符串
		do = do.Where(t.Code.Like("%" + req.Code + "%"))
	}
	if req.En != "" { //字符串
		do = do.Where(t.En.Like("%" + req.En + "%"))
	}
	if req.Zh != "" { //字符串
		do = do.Where(t.Zh.Like("%" + req.Zh + "%"))
	}
	if req.Jp != "" { //字符串
		do = do.Where(t.Jp.Like("%" + req.Jp + "%"))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindConfigTranslate error : %s", err.Error())
		return nil, err
	}
	res := convert.ConfigTranslate_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找ConfigTranslate
func (s *ConfigTranslateSvc) FindByIdConfigTranslate(req *proto.ConfigTranslateFilter) (*proto.ConfigTranslate, error) {
	t := orm.Use(iotmodel.GetDB()).TConfigTranslate
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdConfigTranslate error : %s", err.Error())
		return nil, err
	}
	res := convert.ConfigTranslate_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找ConfigTranslate,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *ConfigTranslateSvc) GetListConfigTranslate(req *proto.ConfigTranslateListRequest) ([]*proto.ConfigTranslate, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TConfigTranslate
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.Code != "" { //字符串
			do = do.Where(t.Code.Like("%" + query.Code + "%"))
		}
		if query.En != "" { //字符串
			do = do.Where(t.En.Like("%" + query.En + "%"))
		}
		if query.Zh != "" { //字符串
			do = do.Where(t.Zh.Like("%" + query.Zh + "%"))
		}
		if query.Jp != "" { //字符串
			do = do.Where(t.Jp.Like("%" + query.Jp + "%"))
		}
		if query.CreatedBy != 0 { //整数
			do = do.Where(t.CreatedBy.Eq(query.CreatedBy))
		}
		if query.UpdatedBy != 0 { //整数
			do = do.Where(t.UpdatedBy.Eq(query.UpdatedBy))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TConfigTranslate
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListConfigTranslate error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.ConfigTranslate, len(list))
	for i, v := range list {
		result[i] = convert.ConfigTranslate_db2pb(v)
	}
	return result, total, nil
}
