// Code generated by sgen.exe,2022-05-31 10:08:59. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	"cloud_platform/iot_basic_service/convert"
	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_config/model"
	"cloud_platform/iot_model/db_config/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type ConfigPlatformSvc struct {
	Ctx context.Context
}

// 创建ConfigPlatform
func (s *ConfigPlatformSvc) CreateConfigPlatform(req *proto.ConfigPlatform) (*proto.ConfigPlatform, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TConfigPlatform
	do := t.WithContext(context.Background())
	dbObj := convert.ConfigPlatform_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateConfigPlatform error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除ConfigPlatform
func (s *ConfigPlatformSvc) DeleteConfigPlatform(req *proto.ConfigPlatform) (*proto.ConfigPlatform, error) {
	t := orm.Use(iotmodel.GetDB()).TConfigPlatform
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.Code != "" { //字符串
		do = do.Where(t.Code.Eq(req.Code))
	}
	if req.Value != "" { //字符串
		do = do.Where(t.Value.Eq(req.Value))
	}
	if req.Remark != "" { //字符串
		do = do.Where(t.Remark.Eq(req.Remark))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteConfigPlatform error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除ConfigPlatform
func (s *ConfigPlatformSvc) DeleteByIdConfigPlatform(req *proto.ConfigPlatform) (*proto.ConfigPlatform, error) {
	t := orm.Use(iotmodel.GetDB()).TConfigPlatform
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdConfigPlatform error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除ConfigPlatform
func (s *ConfigPlatformSvc) DeleteByIdsConfigPlatform(req *proto.ConfigPlatformBatchDeleteRequest) (*proto.ConfigPlatformBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TConfigPlatform
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsConfigPlatform error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新ConfigPlatform
func (s *ConfigPlatformSvc) UpdateConfigPlatform(req *proto.ConfigPlatform) (*proto.ConfigPlatform, error) {
	t := orm.Use(iotmodel.GetDB()).TConfigPlatform
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.Code != "" { //字符串
		updateField = append(updateField, t.Code)
	}
	if req.Value != "" { //字符串
		updateField = append(updateField, t.Value)
	}
	if req.Remark != "" { //字符串
		updateField = append(updateField, t.Remark)
	}
	if req.CreatedBy != 0 { //整数
		updateField = append(updateField, t.CreatedBy)
	}
	if req.UpdatedBy != 0 { //整数
		updateField = append(updateField, t.UpdatedBy)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateConfigPlatform error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.ConfigPlatform_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateConfigPlatform error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段ConfigPlatform
func (s *ConfigPlatformSvc) UpdateAllConfigPlatform(req *proto.ConfigPlatform) (*proto.ConfigPlatform, error) {
	t := orm.Use(iotmodel.GetDB()).TConfigPlatform
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.Code)
	updateField = append(updateField, t.Value)
	updateField = append(updateField, t.Remark)
	updateField = append(updateField, t.CreatedBy)
	updateField = append(updateField, t.UpdatedBy)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllConfigPlatform error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.ConfigPlatform_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllConfigPlatform error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *ConfigPlatformSvc) UpdateFieldsConfigPlatform(req *proto.ConfigPlatformUpdateFieldsRequest) (*proto.ConfigPlatform, error) {
	t := orm.Use(iotmodel.GetDB()).TConfigPlatform
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsConfigPlatform error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsConfigPlatform error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.ConfigPlatform_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsConfigPlatform error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找ConfigPlatform
func (s *ConfigPlatformSvc) FindConfigPlatform(req *proto.ConfigPlatformFilter) (*proto.ConfigPlatform, error) {
	t := orm.Use(iotmodel.GetDB()).TConfigPlatform
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.Code != "" { //字符串
		do = do.Where(t.Code.Eq(req.Code))
	}
	if req.Value != "" { //字符串
		do = do.Where(t.Value.Eq(req.Value))
	}
	if req.Remark != "" { //字符串
		do = do.Where(t.Remark.Eq(req.Remark))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindConfigPlatform error : %s", err.Error())
		return nil, err
	}
	res := convert.ConfigPlatform_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找ConfigPlatform
func (s *ConfigPlatformSvc) FindByIdConfigPlatform(req *proto.ConfigPlatformFilter) (*proto.ConfigPlatform, error) {
	t := orm.Use(iotmodel.GetDB()).TConfigPlatform
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdConfigPlatform error : %s", err.Error())
		return nil, err
	}
	res := convert.ConfigPlatform_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找ConfigPlatform,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *ConfigPlatformSvc) GetListConfigPlatform(req *proto.ConfigPlatformListRequest) ([]*proto.ConfigPlatform, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TConfigPlatform
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.Code != "" { //字符串
			do = do.Where(t.Code.Like("%" + query.Code + "%"))
		}
		if query.Value != "" { //字符串
			do = do.Where(t.Value.Like("%" + query.Value + "%"))
		}
		if query.Remark != "" { //字符串
			do = do.Where(t.Remark.Like("%" + query.Remark + "%"))
		}
		if query.CreatedBy != 0 { //整数
			do = do.Where(t.CreatedBy.Eq(query.CreatedBy))
		}
		if query.UpdatedBy != 0 { //整数
			do = do.Where(t.UpdatedBy.Eq(query.UpdatedBy))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TConfigPlatform
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListConfigPlatform error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.ConfigPlatform, len(list))
	for i, v := range list {
		result[i] = convert.ConfigPlatform_db2pb(v)
	}
	return result, total, nil
}
