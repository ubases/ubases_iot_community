// Code generated by sgen.exe,2022-04-21 13:46:12. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	"cloud_platform/iot_device_service/convert"
	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_device/model"
	"cloud_platform/iot_model/db_device/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type IotOtaWhiteGroupSvc struct {
	Ctx context.Context
}

// 创建IotOtaWhiteGroup
func (s *IotOtaWhiteGroupSvc) CreateIotOtaWhiteGroup(req *proto.IotOtaWhiteGroup) (*proto.IotOtaWhiteGroup, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TIotOtaWhiteGroup
	do := t.WithContext(context.Background())
	dbObj := convert.IotOtaWhiteGroup_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateIotOtaWhiteGroup error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除IotOtaWhiteGroup
func (s *IotOtaWhiteGroupSvc) DeleteIotOtaWhiteGroup(req *proto.IotOtaWhiteGroup) (*proto.IotOtaWhiteGroup, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaWhiteGroup
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.GroupName != "" { //字符串
		do = do.Where(t.GroupName.Eq(req.GroupName))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteIotOtaWhiteGroup error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除IotOtaWhiteGroup
func (s *IotOtaWhiteGroupSvc) DeleteByIdIotOtaWhiteGroup(req *proto.IotOtaWhiteGroup) (*proto.IotOtaWhiteGroup, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaWhiteGroup
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdIotOtaWhiteGroup error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除IotOtaWhiteGroup
func (s *IotOtaWhiteGroupSvc) DeleteByIdsIotOtaWhiteGroup(req *proto.IotOtaWhiteGroupBatchDeleteRequest) (*proto.IotOtaWhiteGroupBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TIotOtaWhiteGroup
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsIotOtaWhiteGroup error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新IotOtaWhiteGroup
func (s *IotOtaWhiteGroupSvc) UpdateIotOtaWhiteGroup(req *proto.IotOtaWhiteGroup) (*proto.IotOtaWhiteGroup, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaWhiteGroup
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.GroupName != "" { //字符串
		updateField = append(updateField, t.GroupName)
	}
	if req.CreatedBy != 0 { //整数
		updateField = append(updateField, t.CreatedBy)
	}
	if req.UpdatedBy != 0 { //整数
		updateField = append(updateField, t.UpdatedBy)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateIotOtaWhiteGroup error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.IotOtaWhiteGroup_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateIotOtaWhiteGroup error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段IotOtaWhiteGroup
func (s *IotOtaWhiteGroupSvc) UpdateAllIotOtaWhiteGroup(req *proto.IotOtaWhiteGroup) (*proto.IotOtaWhiteGroup, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaWhiteGroup
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.GroupName)
	updateField = append(updateField, t.CreatedBy)
	updateField = append(updateField, t.UpdatedBy)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllIotOtaWhiteGroup error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.IotOtaWhiteGroup_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllIotOtaWhiteGroup error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *IotOtaWhiteGroupSvc) UpdateFieldsIotOtaWhiteGroup(req *proto.IotOtaWhiteGroupUpdateFieldsRequest) (*proto.IotOtaWhiteGroup, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaWhiteGroup
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsIotOtaWhiteGroup error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsIotOtaWhiteGroup error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.IotOtaWhiteGroup_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsIotOtaWhiteGroup error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找IotOtaWhiteGroup
func (s *IotOtaWhiteGroupSvc) FindIotOtaWhiteGroup(req *proto.IotOtaWhiteGroupFilter) (*proto.IotOtaWhiteGroup, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaWhiteGroup
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.GroupName != "" { //字符串
		do = do.Where(t.GroupName.Like("%" + req.GroupName + "%"))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindIotOtaWhiteGroup error : %s", err.Error())
		return nil, err
	}
	res := convert.IotOtaWhiteGroup_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找IotOtaWhiteGroup
func (s *IotOtaWhiteGroupSvc) FindByIdIotOtaWhiteGroup(req *proto.IotOtaWhiteGroupFilter) (*proto.IotOtaWhiteGroup, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaWhiteGroup
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdIotOtaWhiteGroup error : %s", err.Error())
		return nil, err
	}
	res := convert.IotOtaWhiteGroup_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找IotOtaWhiteGroup,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *IotOtaWhiteGroupSvc) GetListIotOtaWhiteGroup(req *proto.IotOtaWhiteGroupListRequest) ([]*proto.IotOtaWhiteGroup, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TIotOtaWhiteGroup
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.GroupName != "" { //字符串
			do = do.Where(t.GroupName.Like("%" + query.GroupName + "%"))
		}
		if query.CreatedBy != 0 { //整数
			do = do.Where(t.CreatedBy.Eq(query.CreatedBy))
		}
		if query.UpdatedBy != 0 { //整数
			do = do.Where(t.UpdatedBy.Eq(query.UpdatedBy))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TIotOtaWhiteGroup
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListIotOtaWhiteGroup error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.IotOtaWhiteGroup, len(list))
	for i, v := range list {
		result[i] = convert.IotOtaWhiteGroup_db2pb(v)
	}
	return result, total, nil
}
