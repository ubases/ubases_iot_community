// Code generated by sgen.exe,2022-12-23 15:25:01. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	"cloud_platform/iot_device_service/convert"
	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_device/model"
	"cloud_platform/iot_model/db_device/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type IotDeviceTimerSvc struct {
	Ctx context.Context
}

// 创建IotDeviceTimer
func (s *IotDeviceTimerSvc) CreateIotDeviceTimer(req *proto.IotDeviceTimer) (*proto.IotDeviceTimer, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TIotDeviceTimer
	do := t.WithContext(context.Background())
	dbObj := convert.IotDeviceTimer_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateIotDeviceTimer error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除IotDeviceTimer
func (s *IotDeviceTimerSvc) DeleteIotDeviceTimer(req *proto.IotDeviceTimer) (*proto.IotDeviceTimer, error) {
	t := orm.Use(iotmodel.GetDB()).TIotDeviceTimer
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.Hour != "" { //字符串
		do = do.Where(t.Hour.Eq(req.Hour))
	}
	if req.Minute != "" { //字符串
		do = do.Where(t.Minute.Eq(req.Minute))
	}
	if req.DaysMode != 0 { //整数
		do = do.Where(t.DaysMode.Eq(req.DaysMode))
	}
	if req.Weeks != "" { //字符串
		do = do.Where(t.Weeks.Eq(req.Weeks))
	}
	if req.Remark != "" { //字符串
		do = do.Where(t.Remark.Eq(req.Remark))
	}
	if req.FuncKey != "" { //字符串
		do = do.Where(t.FuncKey.Eq(req.FuncKey))
	}
	if req.FuncValue != "" { //字符串
		do = do.Where(t.FuncValue.Eq(req.FuncValue))
	}
	if req.UserId != 0 { //整数
		do = do.Where(t.UserId.Eq(req.UserId))
	}
	if req.DeviceId != "" { //字符串
		do = do.Where(t.DeviceId.Eq(req.DeviceId))
	}
	if req.Enabled != 0 { //整数
		do = do.Where(t.Enabled.Eq(req.Enabled))
	}
	if req.Cron != "" { //字符串
		do = do.Where(t.Cron.Eq(req.Cron))
	}
	if req.TaskId != "" { //字符串
		do = do.Where(t.TaskId.Eq(req.TaskId))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	if req.Functions != "" { //字符串
		do = do.Where(t.Functions.Eq(req.Functions))
	}
	if req.Name != "" { //字符串
		do = do.Where(t.Name.Eq(req.Name))
	}
	if req.EndHour != "" { //字符串
		do = do.Where(t.EndHour.Eq(req.EndHour))
	}
	if req.EndMinute != "" { //字符串
		do = do.Where(t.EndMinute.Eq(req.EndMinute))
	}
	if req.EndCron != "" { //字符串
		do = do.Where(t.EndCron.Eq(req.EndCron))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteIotDeviceTimer error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除IotDeviceTimer
func (s *IotDeviceTimerSvc) DeleteByIdIotDeviceTimer(req *proto.IotDeviceTimer) (*proto.IotDeviceTimer, error) {
	t := orm.Use(iotmodel.GetDB()).TIotDeviceTimer
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdIotDeviceTimer error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除IotDeviceTimer
func (s *IotDeviceTimerSvc) DeleteByIdsIotDeviceTimer(req *proto.IotDeviceTimerBatchDeleteRequest) (*proto.IotDeviceTimerBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TIotDeviceTimer
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsIotDeviceTimer error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新IotDeviceTimer
func (s *IotDeviceTimerSvc) UpdateIotDeviceTimer(req *proto.IotDeviceTimer) (*proto.IotDeviceTimer, error) {
	t := orm.Use(iotmodel.GetDB()).TIotDeviceTimer
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.Hour != "" { //字符串
		updateField = append(updateField, t.Hour)
	}
	if req.Minute != "" { //字符串
		updateField = append(updateField, t.Minute)
	}
	if req.DaysMode >= 0 { //整数
		updateField = append(updateField, t.DaysMode)
	}
	// if req.Weeks != "" {//字符串
	updateField = append(updateField, t.Weeks)
	// }
	if req.Remark != "" { //字符串
		updateField = append(updateField, t.Remark)
	}
	if req.FuncKey != "" { //字符串
		updateField = append(updateField, t.FuncKey)
	}
	if req.FuncValue != "" { //字符串
		updateField = append(updateField, t.FuncValue)
	}
	if req.UserId != 0 { //整数
		updateField = append(updateField, t.UserId)
	}
	if req.DeviceId != "" { //字符串
		updateField = append(updateField, t.DeviceId)
	}
	if req.Enabled != 0 { //整数
		updateField = append(updateField, t.Enabled)
	}
	if req.Cron != "" { //字符串
		updateField = append(updateField, t.Cron)
	}
	if req.TaskId != "" { //字符串
		updateField = append(updateField, t.TaskId)
	}
	if req.CreatedBy != 0 { //整数
		updateField = append(updateField, t.CreatedBy)
	}
	if req.UpdatedBy != 0 { //整数
		updateField = append(updateField, t.UpdatedBy)
	}
	if req.Functions != "" { //字符串
		updateField = append(updateField, t.Functions)
	}
	if req.Name != "" { //字符串
		updateField = append(updateField, t.Name)
	}
	if req.EndHour != "" { //字符串
		updateField = append(updateField, t.EndHour)
	}
	if req.EndMinute != "" { //字符串
		updateField = append(updateField, t.EndMinute)
	}
	if req.EndCron != "" { //字符串
		updateField = append(updateField, t.EndCron)
	}
	if req.FirstTime != nil {
		updateField = append(updateField, t.FirstTime)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateIotDeviceTimer error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.IotDeviceTimer_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateIotDeviceTimer error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段IotDeviceTimer
func (s *IotDeviceTimerSvc) UpdateAllIotDeviceTimer(req *proto.IotDeviceTimer) (*proto.IotDeviceTimer, error) {
	t := orm.Use(iotmodel.GetDB()).TIotDeviceTimer
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.Hour)
	updateField = append(updateField, t.Minute)
	updateField = append(updateField, t.DaysMode)
	updateField = append(updateField, t.Weeks)
	updateField = append(updateField, t.Remark)
	updateField = append(updateField, t.FuncKey)
	updateField = append(updateField, t.FuncValue)
	updateField = append(updateField, t.UserId)
	updateField = append(updateField, t.DeviceId)
	updateField = append(updateField, t.Enabled)
	updateField = append(updateField, t.FirstTime)
	updateField = append(updateField, t.Cron)
	updateField = append(updateField, t.TaskId)
	updateField = append(updateField, t.CreatedBy)
	updateField = append(updateField, t.UpdatedBy)
	updateField = append(updateField, t.Functions)
	updateField = append(updateField, t.Name)
	updateField = append(updateField, t.EndHour)
	updateField = append(updateField, t.EndMinute)
	updateField = append(updateField, t.EndCron)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllIotDeviceTimer error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.IotDeviceTimer_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllIotDeviceTimer error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *IotDeviceTimerSvc) UpdateFieldsIotDeviceTimer(req *proto.IotDeviceTimerUpdateFieldsRequest) (*proto.IotDeviceTimer, error) {
	t := orm.Use(iotmodel.GetDB()).TIotDeviceTimer
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsIotDeviceTimer error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsIotDeviceTimer error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.IotDeviceTimer_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsIotDeviceTimer error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找IotDeviceTimer
func (s *IotDeviceTimerSvc) FindIotDeviceTimer(req *proto.IotDeviceTimerFilter) (*proto.IotDeviceTimer, error) {
	t := orm.Use(iotmodel.GetDB()).TIotDeviceTimer
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.Hour != "" { //字符串
		do = do.Where(t.Hour.Eq(req.Hour))
	}
	if req.Minute != "" { //字符串
		do = do.Where(t.Minute.Eq(req.Minute))
	}
	if req.DaysMode != 0 { //整数
		do = do.Where(t.DaysMode.Eq(req.DaysMode))
	}
	if req.Weeks != "" { //字符串
		do = do.Where(t.Weeks.Eq(req.Weeks))
	}
	if req.Remark != "" { //字符串
		do = do.Where(t.Remark.Eq(req.Remark))
	}
	if req.FuncKey != "" { //字符串
		do = do.Where(t.FuncKey.Eq(req.FuncKey))
	}
	if req.FuncValue != "" { //字符串
		do = do.Where(t.FuncValue.Eq(req.FuncValue))
	}
	if req.UserId != 0 { //整数
		do = do.Where(t.UserId.Eq(req.UserId))
	}
	if req.DeviceId != "" { //字符串
		do = do.Where(t.DeviceId.Eq(req.DeviceId))
	}
	if req.Enabled != 0 { //整数
		do = do.Where(t.Enabled.Eq(req.Enabled))
	}
	if req.Cron != "" { //字符串
		do = do.Where(t.Cron.Eq(req.Cron))
	}
	if req.TaskId != "" { //字符串
		do = do.Where(t.TaskId.Eq(req.TaskId))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	if req.Functions != "" { //字符串
		do = do.Where(t.Functions.Eq(req.Functions))
	}
	if req.Name != "" { //字符串
		do = do.Where(t.Name.Eq(req.Name))
	}
	if req.EndHour != "" { //字符串
		do = do.Where(t.EndHour.Eq(req.EndHour))
	}
	if req.EndMinute != "" { //字符串
		do = do.Where(t.EndMinute.Eq(req.EndMinute))
	}
	if req.EndCron != "" { //字符串
		do = do.Where(t.EndCron.Eq(req.EndCron))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindIotDeviceTimer error : %s", err.Error())
		return nil, err
	}
	res := convert.IotDeviceTimer_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找IotDeviceTimer
func (s *IotDeviceTimerSvc) FindByIdIotDeviceTimer(req *proto.IotDeviceTimerFilter) (*proto.IotDeviceTimer, error) {
	t := orm.Use(iotmodel.GetDB()).TIotDeviceTimer
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdIotDeviceTimer error : %s", err.Error())
		return nil, err
	}
	res := convert.IotDeviceTimer_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找IotDeviceTimer,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *IotDeviceTimerSvc) GetListIotDeviceTimer(req *proto.IotDeviceTimerListRequest) ([]*proto.IotDeviceTimer, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TIotDeviceTimer
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.Hour != "" { //字符串
			do = do.Where(t.Hour.Like("%" + query.Hour + "%"))
		}
		if query.Minute != "" { //字符串
			do = do.Where(t.Minute.Like("%" + query.Minute + "%"))
		}
		if query.DaysMode != 0 { //整数
			do = do.Where(t.DaysMode.Eq(query.DaysMode))
		}
		if query.Weeks != "" { //字符串
			do = do.Where(t.Weeks.Like("%" + query.Weeks + "%"))
		}
		if query.Remark != "" { //字符串
			do = do.Where(t.Remark.Like("%" + query.Remark + "%"))
		}
		if query.FuncKey != "" { //字符串
			do = do.Where(t.FuncKey.Like("%" + query.FuncKey + "%"))
		}
		if query.FuncValue != "" { //字符串
			do = do.Where(t.FuncValue.Like("%" + query.FuncValue + "%"))
		}
		if query.UserId != 0 { //整数
			do = do.Where(t.UserId.Eq(query.UserId))
		}
		if query.DeviceId != "" { //字符串
			do = do.Where(t.DeviceId.Eq(query.DeviceId))
		}
		if query.Enabled != 0 { //整数
			do = do.Where(t.Enabled.Eq(query.Enabled))
		}
		if query.Cron != "" { //字符串
			do = do.Where(t.Cron.Like("%" + query.Cron + "%"))
		}
		if query.TaskId != "" { //字符串
			do = do.Where(t.TaskId.Eq(query.TaskId))
		}
		if query.CreatedBy != 0 { //整数
			do = do.Where(t.CreatedBy.Eq(query.CreatedBy))
		}
		if query.UpdatedBy != 0 { //整数
			do = do.Where(t.UpdatedBy.Eq(query.UpdatedBy))
		}
		if query.Functions != "" { //字符串
			do = do.Where(t.Functions.Like("%" + query.Functions + "%"))
		}
		if query.Name != "" { //字符串
			do = do.Where(t.Name.Like("%" + query.Name + "%"))
		}
		if query.EndHour != "" { //字符串
			do = do.Where(t.EndHour.Like("%" + query.EndHour + "%"))
		}
		if query.EndMinute != "" { //字符串
			do = do.Where(t.EndMinute.Like("%" + query.EndMinute + "%"))
		}
		if query.EndCron != "" { //字符串
			do = do.Where(t.EndCron.Like("%" + query.EndCron + "%"))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TIotDeviceTimer
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListIotDeviceTimer error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.IotDeviceTimer, len(list))
	for i, v := range list {
		result[i] = convert.IotDeviceTimer_db2pb(v)
	}
	return result, total, nil
}
