// Code generated by sgen.exe,2022-04-21 13:46:12. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	"cloud_platform/iot_device_service/convert"
	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_device/model"
	"cloud_platform/iot_model/db_device/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type IotOtaVersionSvc struct {
	Ctx context.Context
}

// 创建IotOtaVersion
func (s *IotOtaVersionSvc) CreateIotOtaVersion(req *proto.IotOtaVersion) (*proto.IotOtaVersion, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TIotOtaVersion
	do := t.WithContext(context.Background())
	dbObj := convert.IotOtaVersion_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateIotOtaVersion error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除IotOtaVersion
func (s *IotOtaVersionSvc) DeleteIotOtaVersion(req *proto.IotOtaVersion) (*proto.IotOtaVersion, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaVersion
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.DeviceVersion != "" { //字符串
		do = do.Where(t.DeviceVersion.Eq(req.DeviceVersion))
	}
	if req.McuPkgId != 0 { //整数
		do = do.Where(t.McuPkgId.Eq(req.McuPkgId))
	}
	if req.EcuPkgId != 0 { //整数
		do = do.Where(t.EcuPkgId.Eq(req.EcuPkgId))
	}
	if req.UpgradeMode != 0 { //整数
		do = do.Where(t.UpgradeMode.Eq(req.UpgradeMode))
	}
	if req.Status != 0 { //整数
		do = do.Where(t.Status.Eq(req.Status))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteIotOtaVersion error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除IotOtaVersion
func (s *IotOtaVersionSvc) DeleteByIdIotOtaVersion(req *proto.IotOtaVersion) (*proto.IotOtaVersion, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaVersion
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdIotOtaVersion error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除IotOtaVersion
func (s *IotOtaVersionSvc) DeleteByIdsIotOtaVersion(req *proto.IotOtaVersionBatchDeleteRequest) (*proto.IotOtaVersionBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TIotOtaVersion
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsIotOtaVersion error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新IotOtaVersion
func (s *IotOtaVersionSvc) UpdateIotOtaVersion(req *proto.IotOtaVersion) (*proto.IotOtaVersion, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaVersion
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.DeviceVersion != "" { //字符串
		updateField = append(updateField, t.DeviceVersion)
	}
	if req.McuPkgId != 0 { //整数
		updateField = append(updateField, t.McuPkgId)
	}
	if req.EcuPkgId != 0 { //整数
		updateField = append(updateField, t.EcuPkgId)
	}
	if req.UpgradeMode != 0 { //整数
		updateField = append(updateField, t.UpgradeMode)
	}
	if req.Status != 0 { //整数
		updateField = append(updateField, t.Status)
	}
	if req.CreatedBy != 0 { //整数
		updateField = append(updateField, t.CreatedBy)
	}
	if req.UpdatedBy != 0 { //整数
		updateField = append(updateField, t.UpdatedBy)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateIotOtaVersion error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.IotOtaVersion_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateIotOtaVersion error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段IotOtaVersion
func (s *IotOtaVersionSvc) UpdateAllIotOtaVersion(req *proto.IotOtaVersion) (*proto.IotOtaVersion, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaVersion
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.DeviceVersion)
	updateField = append(updateField, t.McuPkgId)
	updateField = append(updateField, t.EcuPkgId)
	updateField = append(updateField, t.UpgradeMode)
	updateField = append(updateField, t.Status)
	updateField = append(updateField, t.CreatedBy)
	updateField = append(updateField, t.UpdatedBy)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllIotOtaVersion error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.IotOtaVersion_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllIotOtaVersion error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *IotOtaVersionSvc) UpdateFieldsIotOtaVersion(req *proto.IotOtaVersionUpdateFieldsRequest) (*proto.IotOtaVersion, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaVersion
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsIotOtaVersion error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsIotOtaVersion error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.IotOtaVersion_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsIotOtaVersion error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找IotOtaVersion
func (s *IotOtaVersionSvc) FindIotOtaVersion(req *proto.IotOtaVersionFilter) (*proto.IotOtaVersion, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaVersion
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.DeviceVersion != "" { //字符串
		do = do.Where(t.DeviceVersion.Like("%" + req.DeviceVersion + "%"))
	}
	if req.McuPkgId != 0 { //整数
		do = do.Where(t.McuPkgId.Eq(req.McuPkgId))
	}
	if req.EcuPkgId != 0 { //整数
		do = do.Where(t.EcuPkgId.Eq(req.EcuPkgId))
	}
	if req.UpgradeMode != 0 { //整数
		do = do.Where(t.UpgradeMode.Eq(req.UpgradeMode))
	}
	if req.Status != 0 { //整数
		do = do.Where(t.Status.Eq(req.Status))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindIotOtaVersion error : %s", err.Error())
		return nil, err
	}
	res := convert.IotOtaVersion_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找IotOtaVersion
func (s *IotOtaVersionSvc) FindByIdIotOtaVersion(req *proto.IotOtaVersionFilter) (*proto.IotOtaVersion, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaVersion
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdIotOtaVersion error : %s", err.Error())
		return nil, err
	}
	res := convert.IotOtaVersion_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找IotOtaVersion,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *IotOtaVersionSvc) GetListIotOtaVersion(req *proto.IotOtaVersionListRequest) ([]*proto.IotOtaVersion, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TIotOtaVersion
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.DeviceVersion != "" { //字符串
			do = do.Where(t.DeviceVersion.Like("%" + query.DeviceVersion + "%"))
		}
		if query.McuPkgId != 0 { //整数
			do = do.Where(t.McuPkgId.Eq(query.McuPkgId))
		}
		if query.EcuPkgId != 0 { //整数
			do = do.Where(t.EcuPkgId.Eq(query.EcuPkgId))
		}
		if query.UpgradeMode != 0 { //整数
			do = do.Where(t.UpgradeMode.Eq(query.UpgradeMode))
		}
		if query.Status != 0 { //整数
			do = do.Where(t.Status.Eq(query.Status))
		}
		if query.CreatedBy != 0 { //整数
			do = do.Where(t.CreatedBy.Eq(query.CreatedBy))
		}
		if query.UpdatedBy != 0 { //整数
			do = do.Where(t.UpdatedBy.Eq(query.UpdatedBy))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TIotOtaVersion
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListIotOtaVersion error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.IotOtaVersion, len(list))
	for i, v := range list {
		result[i] = convert.IotOtaVersion_db2pb(v)
	}
	return result, total, nil
}
