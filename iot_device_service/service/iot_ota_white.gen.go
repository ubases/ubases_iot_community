// Code generated by sgen.exe,2022-04-21 13:46:12. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	"cloud_platform/iot_device_service/convert"
	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_device/model"
	"cloud_platform/iot_model/db_device/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type IotOtaWhiteSvc struct {
	Ctx context.Context
}

// 创建IotOtaWhite
func (s *IotOtaWhiteSvc) CreateIotOtaWhite(req *proto.IotOtaWhite) (*proto.IotOtaWhite, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TIotOtaWhite
	do := t.WithContext(context.Background())
	dbObj := convert.IotOtaWhite_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateIotOtaWhite error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除IotOtaWhite
func (s *IotOtaWhiteSvc) DeleteIotOtaWhite(req *proto.IotOtaWhite) (*proto.IotOtaWhite, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaWhite
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.PkgId != 0 { //整数
		do = do.Where(t.PkgId.Eq(req.PkgId))
	}
	if req.ProductId != 0 { //整数
		do = do.Where(t.ProductId.Eq(req.ProductId))
	}
	if req.GroupId != 0 { //整数
		do = do.Where(t.GroupId.Eq(req.GroupId))
	}
	if req.Did != "" { //字符串
		do = do.Where(t.Did.Eq(req.Did))
	}
	if req.DeviceId != 0 { //整数
		do = do.Where(t.DeviceId.Eq(req.DeviceId))
	}
	if req.BelongType != "" { //字符串
		do = do.Where(t.BelongType.Eq(req.BelongType))
	}
	if req.BelongId != 0 { //整数
		do = do.Where(t.BelongId.Eq(req.BelongId))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteIotOtaWhite error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除IotOtaWhite
func (s *IotOtaWhiteSvc) DeleteByIdIotOtaWhite(req *proto.IotOtaWhite) (*proto.IotOtaWhite, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaWhite
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdIotOtaWhite error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除IotOtaWhite
func (s *IotOtaWhiteSvc) DeleteByIdsIotOtaWhite(req *proto.IotOtaWhiteBatchDeleteRequest) (*proto.IotOtaWhiteBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TIotOtaWhite
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsIotOtaWhite error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新IotOtaWhite
func (s *IotOtaWhiteSvc) UpdateIotOtaWhite(req *proto.IotOtaWhite) (*proto.IotOtaWhite, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaWhite
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.PkgId != 0 { //整数
		updateField = append(updateField, t.PkgId)
	}
	if req.ProductId != 0 { //整数
		updateField = append(updateField, t.ProductId)
	}
	if req.GroupId != 0 { //整数
		updateField = append(updateField, t.GroupId)
	}
	if req.Did != "" { //字符串
		updateField = append(updateField, t.Did)
	}
	if req.DeviceId != 0 { //整数
		updateField = append(updateField, t.DeviceId)
	}
	if req.BelongType != "" { //字符串
		updateField = append(updateField, t.BelongType)
	}
	if req.BelongId != 0 { //整数
		updateField = append(updateField, t.BelongId)
	}
	if req.CreatedBy != 0 { //整数
		updateField = append(updateField, t.CreatedBy)
	}
	if req.UpdatedBy != 0 { //整数
		updateField = append(updateField, t.UpdatedBy)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateIotOtaWhite error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.IotOtaWhite_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateIotOtaWhite error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段IotOtaWhite
func (s *IotOtaWhiteSvc) UpdateAllIotOtaWhite(req *proto.IotOtaWhite) (*proto.IotOtaWhite, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaWhite
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.PkgId)
	updateField = append(updateField, t.ProductId)
	updateField = append(updateField, t.GroupId)
	updateField = append(updateField, t.Did)
	updateField = append(updateField, t.DeviceId)
	updateField = append(updateField, t.BelongType)
	updateField = append(updateField, t.BelongId)
	updateField = append(updateField, t.CreatedBy)
	updateField = append(updateField, t.UpdatedBy)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllIotOtaWhite error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.IotOtaWhite_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllIotOtaWhite error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *IotOtaWhiteSvc) UpdateFieldsIotOtaWhite(req *proto.IotOtaWhiteUpdateFieldsRequest) (*proto.IotOtaWhite, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaWhite
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsIotOtaWhite error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsIotOtaWhite error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.IotOtaWhite_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsIotOtaWhite error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找IotOtaWhite
func (s *IotOtaWhiteSvc) FindIotOtaWhite(req *proto.IotOtaWhiteFilter) (*proto.IotOtaWhite, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaWhite
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.PkgId != 0 { //整数
		do = do.Where(t.PkgId.Eq(req.PkgId))
	}
	if req.ProductId != 0 { //整数
		do = do.Where(t.ProductId.Eq(req.ProductId))
	}
	if req.GroupId != 0 { //整数
		do = do.Where(t.GroupId.Eq(req.GroupId))
	}
	if req.Did != "" { //字符串
		do = do.Where(t.Did.Like("%" + req.Did + "%"))
	}
	if req.DeviceId != 0 { //整数
		do = do.Where(t.DeviceId.Eq(req.DeviceId))
	}
	if req.BelongType != "" { //字符串
		do = do.Where(t.BelongType.Like("%" + req.BelongType + "%"))
	}
	if req.BelongId != 0 { //整数
		do = do.Where(t.BelongId.Eq(req.BelongId))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindIotOtaWhite error : %s", err.Error())
		return nil, err
	}
	res := convert.IotOtaWhite_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找IotOtaWhite
func (s *IotOtaWhiteSvc) FindByIdIotOtaWhite(req *proto.IotOtaWhiteFilter) (*proto.IotOtaWhite, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaWhite
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdIotOtaWhite error : %s", err.Error())
		return nil, err
	}
	res := convert.IotOtaWhite_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找IotOtaWhite,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *IotOtaWhiteSvc) GetListIotOtaWhite(req *proto.IotOtaWhiteListRequest) ([]*proto.IotOtaWhite, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TIotOtaWhite
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.PkgId != 0 { //整数
			do = do.Where(t.PkgId.Eq(query.PkgId))
		}
		if query.ProductId != 0 { //整数
			do = do.Where(t.ProductId.Eq(query.ProductId))
		}
		if query.GroupId != 0 { //整数
			do = do.Where(t.GroupId.Eq(query.GroupId))
		}
		if query.Did != "" { //字符串
			do = do.Where(t.Did.Like("%" + query.Did + "%"))
		}
		if query.DeviceId != 0 { //整数
			do = do.Where(t.DeviceId.Eq(query.DeviceId))
		}
		if query.BelongType != "" { //字符串
			do = do.Where(t.BelongType.Like("%" + query.BelongType + "%"))
		}
		if query.BelongId != 0 { //整数
			do = do.Where(t.BelongId.Eq(query.BelongId))
		}
		if query.CreatedBy != 0 { //整数
			do = do.Where(t.CreatedBy.Eq(query.CreatedBy))
		}
		if query.UpdatedBy != 0 { //整数
			do = do.Where(t.UpdatedBy.Eq(query.UpdatedBy))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TIotOtaWhite
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListIotOtaWhite error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.IotOtaWhite, len(list))
	for i, v := range list {
		result[i] = convert.IotOtaWhite_db2pb(v)
	}
	return result, total, nil
}
