// Code generated by sgen.exe,2022-04-21 13:46:12. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	"cloud_platform/iot_device_service/convert"
	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_device/model"
	"cloud_platform/iot_model/db_device/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type IotOtaVersionPublishSvc struct {
	Ctx context.Context
}

// 创建IotOtaVersionPublish
func (s *IotOtaVersionPublishSvc) CreateIotOtaVersionPublish(req *proto.IotOtaVersionPublish) (*proto.IotOtaVersionPublish, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TIotOtaVersionPublish
	do := t.WithContext(context.Background())
	dbObj := convert.IotOtaVersionPublish_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateIotOtaVersionPublish error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除IotOtaVersionPublish
func (s *IotOtaVersionPublishSvc) DeleteIotOtaVersionPublish(req *proto.IotOtaVersionPublish) (*proto.IotOtaVersionPublish, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaVersionPublish
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.VersionId != 0 { //整数
		do = do.Where(t.VersionId.Eq(req.VersionId))
	}
	if req.PublishMode != 0 { //整数
		do = do.Where(t.PublishMode.Eq(req.PublishMode))
	}
	if req.Status != 0 { //整数
		do = do.Where(t.Status.Eq(req.Status))
	}
	if req.IsGray != 0 { //整数
		do = do.Where(t.IsGray.Eq(req.IsGray))
	}
	if req.UpdateDesc != "" { //字符串
		do = do.Where(t.UpdateDesc.Eq(req.UpdateDesc))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteIotOtaVersionPublish error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除IotOtaVersionPublish
func (s *IotOtaVersionPublishSvc) DeleteByIdIotOtaVersionPublish(req *proto.IotOtaVersionPublish) (*proto.IotOtaVersionPublish, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaVersionPublish
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdIotOtaVersionPublish error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除IotOtaVersionPublish
func (s *IotOtaVersionPublishSvc) DeleteByIdsIotOtaVersionPublish(req *proto.IotOtaVersionPublishBatchDeleteRequest) (*proto.IotOtaVersionPublishBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TIotOtaVersionPublish
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsIotOtaVersionPublish error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新IotOtaVersionPublish
func (s *IotOtaVersionPublishSvc) UpdateIotOtaVersionPublish(req *proto.IotOtaVersionPublish) (*proto.IotOtaVersionPublish, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaVersionPublish
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.VersionId != 0 { //整数
		updateField = append(updateField, t.VersionId)
	}
	if req.PublishMode != 0 { //整数
		updateField = append(updateField, t.PublishMode)
	}
	if req.Status != 0 { //整数
		updateField = append(updateField, t.Status)
	}
	if req.IsGray != 0 { //整数
		updateField = append(updateField, t.IsGray)
	}
	if req.UpdateDesc != "" { //字符串
		updateField = append(updateField, t.UpdateDesc)
	}
	if req.CreatedBy != 0 { //整数
		updateField = append(updateField, t.CreatedBy)
	}
	if req.UpdatedBy != 0 { //整数
		updateField = append(updateField, t.UpdatedBy)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateIotOtaVersionPublish error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.IotOtaVersionPublish_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateIotOtaVersionPublish error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段IotOtaVersionPublish
func (s *IotOtaVersionPublishSvc) UpdateAllIotOtaVersionPublish(req *proto.IotOtaVersionPublish) (*proto.IotOtaVersionPublish, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaVersionPublish
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.VersionId)
	updateField = append(updateField, t.PublishMode)
	updateField = append(updateField, t.ScheduleTime)
	updateField = append(updateField, t.PublishTime)
	updateField = append(updateField, t.Status)
	updateField = append(updateField, t.IsGray)
	updateField = append(updateField, t.UpdateDesc)
	updateField = append(updateField, t.CreatedBy)
	updateField = append(updateField, t.UpdatedBy)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllIotOtaVersionPublish error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.IotOtaVersionPublish_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllIotOtaVersionPublish error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *IotOtaVersionPublishSvc) UpdateFieldsIotOtaVersionPublish(req *proto.IotOtaVersionPublishUpdateFieldsRequest) (*proto.IotOtaVersionPublish, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaVersionPublish
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsIotOtaVersionPublish error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsIotOtaVersionPublish error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.IotOtaVersionPublish_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsIotOtaVersionPublish error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找IotOtaVersionPublish
func (s *IotOtaVersionPublishSvc) FindIotOtaVersionPublish(req *proto.IotOtaVersionPublishFilter) (*proto.IotOtaVersionPublish, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaVersionPublish
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.VersionId != 0 { //整数
		do = do.Where(t.VersionId.Eq(req.VersionId))
	}
	if req.PublishMode != 0 { //整数
		do = do.Where(t.PublishMode.Eq(req.PublishMode))
	}
	if req.Status != 0 { //整数
		do = do.Where(t.Status.Eq(req.Status))
	}
	if req.IsGray != 0 { //整数
		do = do.Where(t.IsGray.Eq(req.IsGray))
	}
	if req.UpdateDesc != "" { //字符串
		do = do.Where(t.UpdateDesc.Like("%" + req.UpdateDesc + "%"))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindIotOtaVersionPublish error : %s", err.Error())
		return nil, err
	}
	res := convert.IotOtaVersionPublish_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找IotOtaVersionPublish
func (s *IotOtaVersionPublishSvc) FindByIdIotOtaVersionPublish(req *proto.IotOtaVersionPublishFilter) (*proto.IotOtaVersionPublish, error) {
	t := orm.Use(iotmodel.GetDB()).TIotOtaVersionPublish
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdIotOtaVersionPublish error : %s", err.Error())
		return nil, err
	}
	res := convert.IotOtaVersionPublish_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找IotOtaVersionPublish,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *IotOtaVersionPublishSvc) GetListIotOtaVersionPublish(req *proto.IotOtaVersionPublishListRequest) ([]*proto.IotOtaVersionPublish, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TIotOtaVersionPublish
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.VersionId != 0 { //整数
			do = do.Where(t.VersionId.Eq(query.VersionId))
		}
		if query.PublishMode != 0 { //整数
			do = do.Where(t.PublishMode.Eq(query.PublishMode))
		}
		if query.Status != 0 { //整数
			do = do.Where(t.Status.Eq(query.Status))
		}
		if query.IsGray != 0 { //整数
			do = do.Where(t.IsGray.Eq(query.IsGray))
		}
		if query.UpdateDesc != "" { //字符串
			do = do.Where(t.UpdateDesc.Like("%" + query.UpdateDesc + "%"))
		}
		if query.CreatedBy != 0 { //整数
			do = do.Where(t.CreatedBy.Eq(query.CreatedBy))
		}
		if query.UpdatedBy != 0 { //整数
			do = do.Where(t.UpdatedBy.Eq(query.UpdatedBy))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TIotOtaVersionPublish
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListIotOtaVersionPublish error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.IotOtaVersionPublish, len(list))
	for i, v := range list {
		result[i] = convert.IotOtaVersionPublish_db2pb(v)
	}
	return result, total, nil
}
