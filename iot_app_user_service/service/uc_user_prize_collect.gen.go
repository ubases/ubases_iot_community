// Code generated by sgen.exe,2022-12-21 09:34:52. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	"cloud_platform/iot_app_user_service/convert"
	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_app/model"
	"cloud_platform/iot_model/db_app/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type UcUserPrizeCollectSvc struct {
	Ctx context.Context
}

// 创建UcUserPrizeCollect
func (s *UcUserPrizeCollectSvc) CreateUcUserPrizeCollect(req *proto.UcUserPrizeCollect) (*proto.UcUserPrizeCollect, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TUcUserPrizeCollect
	do := t.WithContext(context.Background())
	dbObj := convert.UcUserPrizeCollect_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateUcUserPrizeCollect error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除UcUserPrizeCollect
func (s *UcUserPrizeCollectSvc) DeleteUcUserPrizeCollect(req *proto.UcUserPrizeCollect) (*proto.UcUserPrizeCollect, error) {
	t := orm.Use(iotmodel.GetDB()).TUcUserPrizeCollect
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.TenantId != "" { //字符串
		do = do.Where(t.TenantId.Eq(req.TenantId))
	}
	if req.UserId != 0 { //整数
		do = do.Where(t.UserId.Eq(req.UserId))
	}
	if req.Account != "" { //字符串
		do = do.Where(t.Account.Eq(req.Account))
	}
	if req.Title != "" { //字符串
		do = do.Where(t.Title.Eq(req.Title))
	}
	if req.AppName != "" { //字符串
		do = do.Where(t.AppName.Eq(req.AppName))
	}
	if req.AppKey != "" { //字符串
		do = do.Where(t.AppKey.Eq(req.AppKey))
	}
	if req.Content != "" { //字符串
		do = do.Where(t.Content.Eq(req.Content))
	}
	if req.ImageUrlList != "" { //字符串
		do = do.Where(t.ImageUrlList.Eq(req.ImageUrlList))
	}
	if req.VideoUrlList != "" { //字符串
		do = do.Where(t.VideoUrlList.Eq(req.VideoUrlList))
	}
	if req.Contact != "" { //字符串
		do = do.Where(t.Contact.Eq(req.Contact))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteUcUserPrizeCollect error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除UcUserPrizeCollect
func (s *UcUserPrizeCollectSvc) DeleteByIdUcUserPrizeCollect(req *proto.UcUserPrizeCollect) (*proto.UcUserPrizeCollect, error) {
	t := orm.Use(iotmodel.GetDB()).TUcUserPrizeCollect
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdUcUserPrizeCollect error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除UcUserPrizeCollect
func (s *UcUserPrizeCollectSvc) DeleteByIdsUcUserPrizeCollect(req *proto.UcUserPrizeCollectBatchDeleteRequest) (*proto.UcUserPrizeCollectBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TUcUserPrizeCollect
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsUcUserPrizeCollect error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新UcUserPrizeCollect
func (s *UcUserPrizeCollectSvc) UpdateUcUserPrizeCollect(req *proto.UcUserPrizeCollect) (*proto.UcUserPrizeCollect, error) {
	t := orm.Use(iotmodel.GetDB()).TUcUserPrizeCollect
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.TenantId != "" { //字符串
		updateField = append(updateField, t.TenantId)
	}
	if req.UserId != 0 { //整数
		updateField = append(updateField, t.UserId)
	}
	if req.Account != "" { //字符串
		updateField = append(updateField, t.Account)
	}
	if req.Title != "" { //字符串
		updateField = append(updateField, t.Title)
	}
	if req.AppName != "" { //字符串
		updateField = append(updateField, t.AppName)
	}
	if req.AppKey != "" { //字符串
		updateField = append(updateField, t.AppKey)
	}
	if req.Content != "" { //字符串
		updateField = append(updateField, t.Content)
	}
	if req.ImageUrlList != "" { //字符串
		updateField = append(updateField, t.ImageUrlList)
	}
	if req.VideoUrlList != "" { //字符串
		updateField = append(updateField, t.VideoUrlList)
	}
	if req.Contact != "" { //字符串
		updateField = append(updateField, t.Contact)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateUcUserPrizeCollect error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.UcUserPrizeCollect_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateUcUserPrizeCollect error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段UcUserPrizeCollect
func (s *UcUserPrizeCollectSvc) UpdateAllUcUserPrizeCollect(req *proto.UcUserPrizeCollect) (*proto.UcUserPrizeCollect, error) {
	t := orm.Use(iotmodel.GetDB()).TUcUserPrizeCollect
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.TenantId)
	updateField = append(updateField, t.UserId)
	updateField = append(updateField, t.Account)
	updateField = append(updateField, t.Title)
	updateField = append(updateField, t.AppName)
	updateField = append(updateField, t.AppKey)
	updateField = append(updateField, t.Content)
	updateField = append(updateField, t.ImageUrlList)
	updateField = append(updateField, t.VideoUrlList)
	updateField = append(updateField, t.Contact)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllUcUserPrizeCollect error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.UcUserPrizeCollect_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllUcUserPrizeCollect error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *UcUserPrizeCollectSvc) UpdateFieldsUcUserPrizeCollect(req *proto.UcUserPrizeCollectUpdateFieldsRequest) (*proto.UcUserPrizeCollect, error) {
	t := orm.Use(iotmodel.GetDB()).TUcUserPrizeCollect
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsUcUserPrizeCollect error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsUcUserPrizeCollect error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.UcUserPrizeCollect_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsUcUserPrizeCollect error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找UcUserPrizeCollect
func (s *UcUserPrizeCollectSvc) FindUcUserPrizeCollect(req *proto.UcUserPrizeCollectFilter) (*proto.UcUserPrizeCollect, error) {
	t := orm.Use(iotmodel.GetDB()).TUcUserPrizeCollect
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.TenantId != "" { //字符串
		do = do.Where(t.TenantId.Eq(req.TenantId))
	}
	if req.UserId != 0 { //整数
		do = do.Where(t.UserId.Eq(req.UserId))
	}
	if req.Account != "" { //字符串
		do = do.Where(t.Account.Eq(req.Account))
	}
	if req.Title != "" { //字符串
		do = do.Where(t.Title.Eq(req.Title))
	}
	if req.AppName != "" { //字符串
		do = do.Where(t.AppName.Eq(req.AppName))
	}
	if req.AppKey != "" { //字符串
		do = do.Where(t.AppKey.Eq(req.AppKey))
	}
	if req.Content != "" { //字符串
		do = do.Where(t.Content.Eq(req.Content))
	}
	if req.ImageUrlList != "" { //字符串
		do = do.Where(t.ImageUrlList.Eq(req.ImageUrlList))
	}
	if req.VideoUrlList != "" { //字符串
		do = do.Where(t.VideoUrlList.Eq(req.VideoUrlList))
	}
	if req.Contact != "" { //字符串
		do = do.Where(t.Contact.Eq(req.Contact))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindUcUserPrizeCollect error : %s", err.Error())
		return nil, err
	}
	res := convert.UcUserPrizeCollect_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找UcUserPrizeCollect
func (s *UcUserPrizeCollectSvc) FindByIdUcUserPrizeCollect(req *proto.UcUserPrizeCollectFilter) (*proto.UcUserPrizeCollect, error) {
	t := orm.Use(iotmodel.GetDB()).TUcUserPrizeCollect
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdUcUserPrizeCollect error : %s", err.Error())
		return nil, err
	}
	res := convert.UcUserPrizeCollect_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找UcUserPrizeCollect,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *UcUserPrizeCollectSvc) GetListUcUserPrizeCollect(req *proto.UcUserPrizeCollectListRequest) ([]*proto.UcUserPrizeCollect, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TUcUserPrizeCollect
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.TenantId != "" { //字符串
			do = do.Where(t.TenantId.Like("%" + query.TenantId + "%"))
		}
		if query.UserId != 0 { //整数
			do = do.Where(t.UserId.Eq(query.UserId))
		}
		if query.Account != "" { //字符串
			do = do.Where(t.Account.Like("%" + query.Account + "%"))
		}
		if query.Title != "" { //字符串
			do = do.Where(t.Title.Like("%" + query.Title + "%"))
		}
		if query.AppName != "" { //字符串
			do = do.Where(t.AppName.Like("%" + query.AppName + "%"))
		}
		if query.AppKey != "" { //字符串
			do = do.Where(t.AppKey.Like("%" + query.AppKey + "%"))
		}
		if query.Content != "" { //字符串
			do = do.Where(t.Content.Like("%" + query.Content + "%"))
		}
		if query.ImageUrlList != "" { //字符串
			do = do.Where(t.ImageUrlList.Like("%" + query.ImageUrlList + "%"))
		}
		if query.VideoUrlList != "" { //字符串
			do = do.Where(t.VideoUrlList.Like("%" + query.VideoUrlList + "%"))
		}
		if query.Contact != "" { //字符串
			do = do.Where(t.Contact.Like("%" + query.Contact + "%"))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TUcUserPrizeCollect
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListUcUserPrizeCollect error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.UcUserPrizeCollect, len(list))
	for i, v := range list {
		result[i] = convert.UcUserPrizeCollect_db2pb(v)
	}
	return result, total, nil
}
