// Code generated by sgen.exe,2022-09-26 11:41:05. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	"cloud_platform/iot_app_user_service/convert"
	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_app/model"
	"cloud_platform/iot_model/db_app/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type UcAppleidInfoSvc struct {
	Ctx context.Context
}

// 创建UcAppleidInfo
func (s *UcAppleidInfoSvc) CreateUcAppleidInfo(req *proto.UcAppleidInfo) (*proto.UcAppleidInfo, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TUcAppleidInfo
	do := t.WithContext(context.Background())
	dbObj := convert.UcAppleidInfo_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateUcAppleidInfo error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除UcAppleidInfo
func (s *UcAppleidInfoSvc) DeleteUcAppleidInfo(req *proto.UcAppleidInfo) (*proto.UcAppleidInfo, error) {
	t := orm.Use(iotmodel.GetDB()).TUcAppleidInfo
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.ThirdUserId != "" { //字符串
		do = do.Where(t.ThirdUserId.Eq(req.ThirdUserId))
	}
	if req.Nickname != "" { //字符串
		do = do.Where(t.Nickname.Eq(req.Nickname))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteUcAppleidInfo error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除UcAppleidInfo
func (s *UcAppleidInfoSvc) DeleteByIdUcAppleidInfo(req *proto.UcAppleidInfo) (*proto.UcAppleidInfo, error) {
	t := orm.Use(iotmodel.GetDB()).TUcAppleidInfo
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdUcAppleidInfo error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除UcAppleidInfo
func (s *UcAppleidInfoSvc) DeleteByIdsUcAppleidInfo(req *proto.UcAppleidInfoBatchDeleteRequest) (*proto.UcAppleidInfoBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TUcAppleidInfo
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsUcAppleidInfo error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新UcAppleidInfo
func (s *UcAppleidInfoSvc) UpdateUcAppleidInfo(req *proto.UcAppleidInfo) (*proto.UcAppleidInfo, error) {
	t := orm.Use(iotmodel.GetDB()).TUcAppleidInfo
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.ThirdUserId != "" { //字符串
		updateField = append(updateField, t.ThirdUserId)
	}
	if req.Nickname != "" { //字符串
		updateField = append(updateField, t.Nickname)
	}
	if req.CreatedBy != 0 { //整数
		updateField = append(updateField, t.CreatedBy)
	}
	if req.UpdatedBy != 0 { //整数
		updateField = append(updateField, t.UpdatedBy)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	//HasPrimaryKey := false
	do = do.Where(t.ThirdUserId.Eq(req.ThirdUserId))
	//if req.Id != 0 {//整数
	//    do = do.Where(t.Id.Eq(req.Id))
	//    HasPrimaryKey = true
	//}

	//if !HasPrimaryKey {
	//    logger.Error("UpdateUcAppleidInfo error : Missing condition")
	//    return nil,errors.New("Missing condition")
	//}

	dbObj := convert.UcAppleidInfo_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateUcAppleidInfo error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段UcAppleidInfo
func (s *UcAppleidInfoSvc) UpdateAllUcAppleidInfo(req *proto.UcAppleidInfo) (*proto.UcAppleidInfo, error) {
	t := orm.Use(iotmodel.GetDB()).TUcAppleidInfo
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.ThirdUserId)
	updateField = append(updateField, t.Nickname)
	updateField = append(updateField, t.CreatedBy)
	updateField = append(updateField, t.UpdatedBy)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllUcAppleidInfo error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.UcAppleidInfo_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllUcAppleidInfo error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *UcAppleidInfoSvc) UpdateFieldsUcAppleidInfo(req *proto.UcAppleidInfoUpdateFieldsRequest) (*proto.UcAppleidInfo, error) {
	t := orm.Use(iotmodel.GetDB()).TUcAppleidInfo
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsUcAppleidInfo error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsUcAppleidInfo error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.UcAppleidInfo_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsUcAppleidInfo error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找UcAppleidInfo
func (s *UcAppleidInfoSvc) FindUcAppleidInfo(req *proto.UcAppleidInfoFilter) (*proto.UcAppleidInfo, error) {
	t := orm.Use(iotmodel.GetDB()).TUcAppleidInfo
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.ThirdUserId != "" { //字符串
		do = do.Where(t.ThirdUserId.Eq(req.ThirdUserId))
	}
	if req.Nickname != "" { //字符串
		do = do.Where(t.Nickname.Eq(req.Nickname))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindUcAppleidInfo error : %s", err.Error())
		return nil, err
	}
	res := convert.UcAppleidInfo_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找UcAppleidInfo
func (s *UcAppleidInfoSvc) FindByIdUcAppleidInfo(req *proto.UcAppleidInfoFilter) (*proto.UcAppleidInfo, error) {
	t := orm.Use(iotmodel.GetDB()).TUcAppleidInfo
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdUcAppleidInfo error : %s", err.Error())
		return nil, err
	}
	res := convert.UcAppleidInfo_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找UcAppleidInfo,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *UcAppleidInfoSvc) GetListUcAppleidInfo(req *proto.UcAppleidInfoListRequest) ([]*proto.UcAppleidInfo, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TUcAppleidInfo
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.ThirdUserId != "" { //字符串
			do = do.Where(t.ThirdUserId.Like("%" + query.ThirdUserId + "%"))
		}
		if query.Nickname != "" { //字符串
			do = do.Where(t.Nickname.Like("%" + query.Nickname + "%"))
		}
		if query.CreatedBy != 0 { //整数
			do = do.Where(t.CreatedBy.Eq(query.CreatedBy))
		}
		if query.UpdatedBy != 0 { //整数
			do = do.Where(t.UpdatedBy.Eq(query.UpdatedBy))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TUcAppleidInfo
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListUcAppleidInfo error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.UcAppleidInfo, len(list))
	for i, v := range list {
		result[i] = convert.UcAppleidInfo_db2pb(v)
	}
	return result, total, nil
}
