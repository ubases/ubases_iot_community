// Code generated by sgen.exe,2022-04-18 19:12:10. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_system/model"
	"cloud_platform/iot_model/db_system/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
	"cloud_platform/iot_system_service/convert"
)

type SysUserPostSvc struct {
	Ctx context.Context
}

// 创建SysUserPost
func (s *SysUserPostSvc) CreateSysUserPost(req *proto.SysUserPost) (*proto.SysUserPost, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TSysUserPost
	do := t.WithContext(context.Background())
	dbObj := convert.SysUserPost_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateSysUserPost error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除SysUserPost
func (s *SysUserPostSvc) DeleteSysUserPost(req *proto.SysUserPost) (*proto.SysUserPost, error) {
	t := orm.Use(iotmodel.GetDB()).TSysUserPost
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.UserId != 0 { //整数
		do = do.Where(t.UserId.Eq(req.UserId))
	}
	if req.PostId != 0 { //整数
		do = do.Where(t.PostId.Eq(req.PostId))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteSysUserPost error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除SysUserPost
func (s *SysUserPostSvc) DeleteByIdSysUserPost(req *proto.SysUserPost) (*proto.SysUserPost, error) {
	t := orm.Use(iotmodel.GetDB()).TSysUserPost
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.UserId != 0 { //整数
		do = do.Where(t.UserId.Eq(req.UserId))
	}
	if req.PostId != 0 { //整数
		do = do.Where(t.PostId.Eq(req.PostId))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdSysUserPost error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除SysUserPost
func (s *SysUserPostSvc) DeleteByIdsSysUserPost(req *proto.SysUserPostBatchDeleteRequest) (*proto.SysUserPostBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TSysUserPost
		do := t.WithContext(context.Background())

		do = do.Where(t.UserId.Eq(k.UserId))

		do = do.Where(t.PostId.Eq(k.PostId))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsSysUserPost error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新SysUserPost
func (s *SysUserPostSvc) UpdateSysUserPost(req *proto.SysUserPost) (*proto.SysUserPost, error) {
	t := orm.Use(iotmodel.GetDB()).TSysUserPost
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.UserId != 0 { //整数
		do = do.Where(t.UserId.Eq(req.UserId))
		HasPrimaryKey = true
	}
	if req.PostId != 0 { //整数
		do = do.Where(t.PostId.Eq(req.PostId))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateSysUserPost error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.SysUserPost_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateSysUserPost error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段SysUserPost
func (s *SysUserPostSvc) UpdateAllSysUserPost(req *proto.SysUserPost) (*proto.SysUserPost, error) {
	t := orm.Use(iotmodel.GetDB()).TSysUserPost
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.UserId != 0 { //整数
		do = do.Where(t.UserId.Eq(req.UserId))
		HasPrimaryKey = true
	}
	if req.PostId != 0 { //整数
		do = do.Where(t.PostId.Eq(req.PostId))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllSysUserPost error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.SysUserPost_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllSysUserPost error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *SysUserPostSvc) UpdateFieldsSysUserPost(req *proto.SysUserPostUpdateFieldsRequest) (*proto.SysUserPost, error) {
	t := orm.Use(iotmodel.GetDB()).TSysUserPost
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFields error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)
	//主键条件
	HasPrimaryKey := false
	if req.Data.UserId != 0 { //整数
		do = do.Where(t.UserId.Eq(req.Data.UserId))
		HasPrimaryKey = true
	}
	if req.Data.PostId != 0 { //整数
		do = do.Where(t.PostId.Eq(req.Data.PostId))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsSysUserPost error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.SysUserPost_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsSysUserPost error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找SysUserPost
func (s *SysUserPostSvc) FindSysUserPost(req *proto.SysUserPostFilter) (*proto.SysUserPost, error) {
	t := orm.Use(iotmodel.GetDB()).TSysUserPost
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.UserId != 0 { //整数
		do = do.Where(t.UserId.Eq(req.UserId))
	}
	if req.PostId != 0 { //整数
		do = do.Where(t.PostId.Eq(req.PostId))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindSysUserPost error : %s", err.Error())
		return nil, err
	}
	res := convert.SysUserPost_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找SysUserPost
func (s *SysUserPostSvc) FindByIdSysUserPost(req *proto.SysUserPostFilter) (*proto.SysUserPost, error) {
	t := orm.Use(iotmodel.GetDB()).TSysUserPost
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.UserId != 0 { //整数
		do = do.Where(t.UserId.Eq(req.UserId))
	}
	if req.PostId != 0 { //整数
		do = do.Where(t.PostId.Eq(req.PostId))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdSysUserPost error : %s", err.Error())
		return nil, err
	}
	res := convert.SysUserPost_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找SysUserPost,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *SysUserPostSvc) GetListSysUserPost(req *proto.SysUserPostListRequest) ([]*proto.SysUserPost, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TSysUserPost
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.UserId != 0 { //整数
			do = do.Where(t.UserId.Eq(query.UserId))
		}
		if query.PostId != 0 { //整数
			do = do.Where(t.PostId.Eq(query.PostId))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.UserId
		orderCol = t.PostId
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TSysUserPost
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListSysUserPost error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.SysUserPost, len(list))
	for i, v := range list {
		result[i] = convert.SysUserPost_db2pb(v)
	}
	return result, total, nil
}
