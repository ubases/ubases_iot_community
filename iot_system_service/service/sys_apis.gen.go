// Code generated by sgen.exe,2022-04-18 19:12:07. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_system/model"
	"cloud_platform/iot_model/db_system/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
	"cloud_platform/iot_system_service/convert"
)

type SysApisSvc struct {
	Ctx context.Context
}

// 创建SysApis
func (s *SysApisSvc) CreateSysApis(req *proto.SysApis) (*proto.SysApis, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TSysApis
	do := t.WithContext(context.Background())
	dbObj := convert.SysApis_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateSysApis error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除SysApis
func (s *SysApisSvc) DeleteSysApis(req *proto.SysApis) (*proto.SysApis, error) {
	t := orm.Use(iotmodel.GetDB()).TSysApis
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.Path != "" { //字符串
		do = do.Where(t.Path.Eq(req.Path))
	}
	if req.Description != "" { //字符串
		do = do.Where(t.Description.Eq(req.Description))
	}
	if req.ApiGroup != "" { //字符串
		do = do.Where(t.ApiGroup.Eq(req.ApiGroup))
	}
	if req.Method != "" { //字符串
		do = do.Where(t.Method.Eq(req.Method))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteSysApis error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除SysApis
func (s *SysApisSvc) DeleteByIdSysApis(req *proto.SysApis) (*proto.SysApis, error) {
	t := orm.Use(iotmodel.GetDB()).TSysApis
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdSysApis error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除SysApis
func (s *SysApisSvc) DeleteByIdsSysApis(req *proto.SysApisBatchDeleteRequest) (*proto.SysApisBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TSysApis
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsSysApis error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新SysApis
func (s *SysApisSvc) UpdateSysApis(req *proto.SysApis) (*proto.SysApis, error) {
	t := orm.Use(iotmodel.GetDB()).TSysApis
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.Path != "" { //字符串
		updateField = append(updateField, t.Path)
	}
	if req.Description != "" { //字符串
		updateField = append(updateField, t.Description)
	}
	if req.ApiGroup != "" { //字符串
		updateField = append(updateField, t.ApiGroup)
	}
	if req.Method != "" { //字符串
		updateField = append(updateField, t.Method)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateSysApis error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.SysApis_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateSysApis error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段SysApis
func (s *SysApisSvc) UpdateAllSysApis(req *proto.SysApis) (*proto.SysApis, error) {
	t := orm.Use(iotmodel.GetDB()).TSysApis
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.Path)
	updateField = append(updateField, t.Description)
	updateField = append(updateField, t.ApiGroup)
	updateField = append(updateField, t.Method)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllSysApis error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.SysApis_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllSysApis error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *SysApisSvc) UpdateFieldsSysApis(req *proto.SysApisUpdateFieldsRequest) (*proto.SysApis, error) {
	t := orm.Use(iotmodel.GetDB()).TSysApis
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFields error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)
	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsSysApis error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.SysApis_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsSysApis error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找SysApis
func (s *SysApisSvc) FindSysApis(req *proto.SysApisFilter) (*proto.SysApis, error) {
	t := orm.Use(iotmodel.GetDB()).TSysApis
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.Path != "" { //字符串
		do = do.Where(t.Path.Like("%" + req.Path + "%"))
	}
	if req.Description != "" { //字符串
		do = do.Where(t.Description.Like("%" + req.Description + "%"))
	}
	if req.ApiGroup != "" { //字符串
		do = do.Where(t.ApiGroup.Like("%" + req.ApiGroup + "%"))
	}
	if req.Method != "" { //字符串
		do = do.Where(t.Method.Like("%" + req.Method + "%"))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindSysApis error : %s", err.Error())
		return nil, err
	}
	res := convert.SysApis_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找SysApis
func (s *SysApisSvc) FindByIdSysApis(req *proto.SysApisFilter) (*proto.SysApis, error) {
	t := orm.Use(iotmodel.GetDB()).TSysApis
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdSysApis error : %s", err.Error())
		return nil, err
	}
	res := convert.SysApis_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找SysApis,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *SysApisSvc) GetListSysApis(req *proto.SysApisListRequest) ([]*proto.SysApis, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TSysApis
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.Path != "" { //字符串
			do = do.Where(t.Path.Like("%" + query.Path + "%"))
		}
		if query.Description != "" { //字符串
			do = do.Where(t.Description.Like("%" + query.Description + "%"))
		}
		if query.ApiGroup != "" { //字符串
			do = do.Where(t.ApiGroup.Like("%" + query.ApiGroup + "%"))
		}
		if query.Method != "" { //字符串
			do = do.Where(t.Method.Like("%" + query.Method + "%"))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TSysApis
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListSysApis error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.SysApis, len(list))
	for i, v := range list {
		result[i] = convert.SysApis_db2pb(v)
	}
	return result, total, nil
}
