// Code generated by sgen.exe,2022-04-18 19:12:11. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_system/model"
	"cloud_platform/iot_model/db_system/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
	"cloud_platform/iot_system_service/convert"
)

type SysWebSetSvc struct {
	Ctx context.Context
}

// 创建SysWebSet
func (s *SysWebSetSvc) CreateSysWebSet(req *proto.SysWebSet) (*proto.SysWebSet, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TSysWebSet
	do := t.WithContext(context.Background())
	dbObj := convert.SysWebSet_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateSysWebSet error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除SysWebSet
func (s *SysWebSetSvc) DeleteSysWebSet(req *proto.SysWebSet) (*proto.SysWebSet, error) {
	t := orm.Use(iotmodel.GetDB()).TSysWebSet
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.WebId != 0 { //整数
		do = do.Where(t.WebId.Eq(req.WebId))
	}
	if req.WebContent != "" { //字符串
		do = do.Where(t.WebContent.Eq(req.WebContent))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteSysWebSet error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除SysWebSet
func (s *SysWebSetSvc) DeleteByIdSysWebSet(req *proto.SysWebSet) (*proto.SysWebSet, error) {
	t := orm.Use(iotmodel.GetDB()).TSysWebSet
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.WebId != 0 { //整数
		do = do.Where(t.WebId.Eq(req.WebId))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdSysWebSet error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除SysWebSet
func (s *SysWebSetSvc) DeleteByIdsSysWebSet(req *proto.SysWebSetBatchDeleteRequest) (*proto.SysWebSetBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TSysWebSet
		do := t.WithContext(context.Background())

		do = do.Where(t.WebId.Eq(k.WebId))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsSysWebSet error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新SysWebSet
func (s *SysWebSetSvc) UpdateSysWebSet(req *proto.SysWebSet) (*proto.SysWebSet, error) {
	t := orm.Use(iotmodel.GetDB()).TSysWebSet
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.WebContent != "" { //字符串
		updateField = append(updateField, t.WebContent)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.WebId != 0 { //整数
		do = do.Where(t.WebId.Eq(req.WebId))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateSysWebSet error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.SysWebSet_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateSysWebSet error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段SysWebSet
func (s *SysWebSetSvc) UpdateAllSysWebSet(req *proto.SysWebSet) (*proto.SysWebSet, error) {
	t := orm.Use(iotmodel.GetDB()).TSysWebSet
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.WebContent)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.WebId != 0 { //整数
		do = do.Where(t.WebId.Eq(req.WebId))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllSysWebSet error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.SysWebSet_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllSysWebSet error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *SysWebSetSvc) UpdateFieldsSysWebSet(req *proto.SysWebSetUpdateFieldsRequest) (*proto.SysWebSet, error) {
	t := orm.Use(iotmodel.GetDB()).TSysWebSet
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFields error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)
	//主键条件
	HasPrimaryKey := false
	if req.Data.WebId != 0 { //整数
		do = do.Where(t.WebId.Eq(req.Data.WebId))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsSysWebSet error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.SysWebSet_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsSysWebSet error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找SysWebSet
func (s *SysWebSetSvc) FindSysWebSet(req *proto.SysWebSetFilter) (*proto.SysWebSet, error) {
	t := orm.Use(iotmodel.GetDB()).TSysWebSet
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.WebId != 0 { //整数
		do = do.Where(t.WebId.Eq(req.WebId))
	}
	if req.WebContent != "" { //字符串
		do = do.Where(t.WebContent.Like("%" + req.WebContent + "%"))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindSysWebSet error : %s", err.Error())
		return nil, err
	}
	res := convert.SysWebSet_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找SysWebSet
func (s *SysWebSetSvc) FindByIdSysWebSet(req *proto.SysWebSetFilter) (*proto.SysWebSet, error) {
	t := orm.Use(iotmodel.GetDB()).TSysWebSet
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.WebId != 0 { //整数
		do = do.Where(t.WebId.Eq(req.WebId))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdSysWebSet error : %s", err.Error())
		return nil, err
	}
	res := convert.SysWebSet_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找SysWebSet,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *SysWebSetSvc) GetListSysWebSet(req *proto.SysWebSetListRequest) ([]*proto.SysWebSet, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TSysWebSet
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.WebId != 0 { //整数
			do = do.Where(t.WebId.Eq(query.WebId))
		}
		if query.WebContent != "" { //字符串
			do = do.Where(t.WebContent.Like("%" + query.WebContent + "%"))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.WebId
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TSysWebSet
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListSysWebSet error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.SysWebSet, len(list))
	for i, v := range list {
		result[i] = convert.SysWebSet_db2pb(v)
	}
	return result, total, nil
}
