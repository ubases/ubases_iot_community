// Code generated by sgen.exe,2022-04-18 19:12:08. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_system/model"
	"cloud_platform/iot_model/db_system/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
	"cloud_platform/iot_system_service/convert"
)

type SysDictDataSvc struct {
	Ctx context.Context
}

// 创建SysDictData
func (s *SysDictDataSvc) CreateSysDictData(req *proto.SysDictData) (*proto.SysDictData, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TSysDictData
	do := t.WithContext(context.Background())
	dbObj := convert.SysDictData_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateSysDictData error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除SysDictData
func (s *SysDictDataSvc) DeleteSysDictData(req *proto.SysDictData) (*proto.SysDictData, error) {
	t := orm.Use(iotmodel.GetDB()).TSysDictData
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.DictCode != 0 { //整数
		do = do.Where(t.DictCode.Eq(req.DictCode))
	}
	if req.DictSort != 0 { //整数
		do = do.Where(t.DictSort.Eq(req.DictSort))
	}
	if req.DictLabel != "" { //字符串
		do = do.Where(t.DictLabel.Eq(req.DictLabel))
	}
	if req.DictValue != "" { //字符串
		do = do.Where(t.DictValue.Eq(req.DictValue))
	}
	if req.DictType != "" { //字符串
		do = do.Where(t.DictType.Eq(req.DictType))
	}
	if req.CssClass != "" { //字符串
		do = do.Where(t.CssClass.Eq(req.CssClass))
	}
	if req.ListClass != "" { //字符串
		do = do.Where(t.ListClass.Eq(req.ListClass))
	}
	if req.IsDefault != 0 { //整数
		do = do.Where(t.IsDefault.Eq(req.IsDefault))
	}
	if req.Status != 0 { //整数
		do = do.Where(t.Status.Eq(req.Status))
	}
	if req.CreateBy != 0 { //整数
		do = do.Where(t.CreateBy.Eq(req.CreateBy))
	}
	if req.UpdateBy != 0 { //整数
		do = do.Where(t.UpdateBy.Eq(req.UpdateBy))
	}
	if req.Remark != "" { //字符串
		do = do.Where(t.Remark.Eq(req.Remark))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteSysDictData error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除SysDictData
func (s *SysDictDataSvc) DeleteByIdSysDictData(req *proto.SysDictData) (*proto.SysDictData, error) {
	t := orm.Use(iotmodel.GetDB()).TSysDictData
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.DictCode != 0 { //整数
		do = do.Where(t.DictCode.Eq(req.DictCode))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdSysDictData error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除SysDictData
func (s *SysDictDataSvc) DeleteByIdsSysDictData(req *proto.SysDictDataBatchDeleteRequest) (*proto.SysDictDataBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TSysDictData
		do := t.WithContext(context.Background())

		do = do.Where(t.DictCode.Eq(k.DictCode))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsSysDictData error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新SysDictData
func (s *SysDictDataSvc) UpdateSysDictData(req *proto.SysDictData) (*proto.SysDictData, error) {
	t := orm.Use(iotmodel.GetDB()).TSysDictData
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.DictSort != 0 { //整数
		updateField = append(updateField, t.DictSort)
	}
	if req.DictLabel != "" { //字符串
		updateField = append(updateField, t.DictLabel)
	}
	if req.DictValue != "" { //字符串
		updateField = append(updateField, t.DictValue)
	}
	if req.DictType != "" { //字符串
		updateField = append(updateField, t.DictType)
	}
	if req.CssClass != "" { //字符串
		updateField = append(updateField, t.CssClass)
	}
	if req.ListClass != "" { //字符串
		updateField = append(updateField, t.ListClass)
	}
	if req.IsDefault != 0 { //整数
		updateField = append(updateField, t.IsDefault)
	}
	if req.Status != 0 { //整数
		updateField = append(updateField, t.Status)
	}
	if req.CreateBy != 0 { //整数
		updateField = append(updateField, t.CreateBy)
	}
	if req.UpdateBy != 0 { //整数
		updateField = append(updateField, t.UpdateBy)
	}
	if req.Remark != "" { //字符串
		updateField = append(updateField, t.Remark)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.DictCode != 0 { //整数
		do = do.Where(t.DictCode.Eq(req.DictCode))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateSysDictData error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.SysDictData_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateSysDictData error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段SysDictData
func (s *SysDictDataSvc) UpdateAllSysDictData(req *proto.SysDictData) (*proto.SysDictData, error) {
	t := orm.Use(iotmodel.GetDB()).TSysDictData
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.DictSort)
	updateField = append(updateField, t.DictLabel)
	updateField = append(updateField, t.DictValue)
	updateField = append(updateField, t.DictType)
	updateField = append(updateField, t.CssClass)
	updateField = append(updateField, t.ListClass)
	updateField = append(updateField, t.IsDefault)
	updateField = append(updateField, t.Status)
	updateField = append(updateField, t.CreateBy)
	updateField = append(updateField, t.UpdateBy)
	updateField = append(updateField, t.Remark)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.DictCode != 0 { //整数
		do = do.Where(t.DictCode.Eq(req.DictCode))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllSysDictData error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.SysDictData_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllSysDictData error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *SysDictDataSvc) UpdateFieldsSysDictData(req *proto.SysDictDataUpdateFieldsRequest) (*proto.SysDictData, error) {
	t := orm.Use(iotmodel.GetDB()).TSysDictData
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFields error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)
	//主键条件
	HasPrimaryKey := false
	if req.Data.DictCode != 0 { //整数
		do = do.Where(t.DictCode.Eq(req.Data.DictCode))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsSysDictData error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.SysDictData_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsSysDictData error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找SysDictData
func (s *SysDictDataSvc) FindSysDictData(req *proto.SysDictDataFilter) (*proto.SysDictData, error) {
	t := orm.Use(iotmodel.GetDB()).TSysDictData
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.DictCode != 0 { //整数
		do = do.Where(t.DictCode.Eq(req.DictCode))
	}
	if req.DictSort != 0 { //整数
		do = do.Where(t.DictSort.Eq(req.DictSort))
	}
	if req.DictLabel != "" { //字符串
		do = do.Where(t.DictLabel.Like("%" + req.DictLabel + "%"))
	}
	if req.DictValue != "" { //字符串
		do = do.Where(t.DictValue.Like("%" + req.DictValue + "%"))
	}
	if req.DictType != "" { //字符串
		do = do.Where(t.DictType.Like("%" + req.DictType + "%"))
	}
	if req.CssClass != "" { //字符串
		do = do.Where(t.CssClass.Like("%" + req.CssClass + "%"))
	}
	if req.ListClass != "" { //字符串
		do = do.Where(t.ListClass.Like("%" + req.ListClass + "%"))
	}
	if req.IsDefault != 0 { //整数
		do = do.Where(t.IsDefault.Eq(req.IsDefault))
	}
	if req.Status != 0 { //整数
		do = do.Where(t.Status.Eq(req.Status))
	}
	if req.CreateBy != 0 { //整数
		do = do.Where(t.CreateBy.Eq(req.CreateBy))
	}
	if req.UpdateBy != 0 { //整数
		do = do.Where(t.UpdateBy.Eq(req.UpdateBy))
	}
	if req.Remark != "" { //字符串
		do = do.Where(t.Remark.Like("%" + req.Remark + "%"))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindSysDictData error : %s", err.Error())
		return nil, err
	}
	res := convert.SysDictData_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找SysDictData
func (s *SysDictDataSvc) FindByIdSysDictData(req *proto.SysDictDataFilter) (*proto.SysDictData, error) {
	t := orm.Use(iotmodel.GetDB()).TSysDictData
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.DictCode != 0 { //整数
		do = do.Where(t.DictCode.Eq(req.DictCode))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdSysDictData error : %s", err.Error())
		return nil, err
	}
	res := convert.SysDictData_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找SysDictData,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *SysDictDataSvc) GetListSysDictData(req *proto.SysDictDataListRequest) ([]*proto.SysDictData, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TSysDictData
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.DictCode != 0 { //整数
			do = do.Where(t.DictCode.Eq(query.DictCode))
		}
		if query.DictSort != 0 { //整数
			do = do.Where(t.DictSort.Eq(query.DictSort))
		}
		if query.DictLabel != "" { //字符串
			do = do.Where(t.DictLabel.Like("%" + query.DictLabel + "%"))
		}
		if query.DictValue != "" { //字符串
			do = do.Where(t.DictValue.Like("%" + query.DictValue + "%"))
		}
		if query.DictType != "" { //字符串
			do = do.Where(t.DictType.Like("%" + query.DictType + "%"))
		}
		if query.CssClass != "" { //字符串
			do = do.Where(t.CssClass.Like("%" + query.CssClass + "%"))
		}
		if query.ListClass != "" { //字符串
			do = do.Where(t.ListClass.Like("%" + query.ListClass + "%"))
		}
		if query.IsDefault != 0 { //整数
			do = do.Where(t.IsDefault.Eq(query.IsDefault))
		}
		if query.Status != 0 { //整数
			do = do.Where(t.Status.Eq(query.Status))
		}
		if query.CreateBy != 0 { //整数
			do = do.Where(t.CreateBy.Eq(query.CreateBy))
		}
		if query.UpdateBy != 0 { //整数
			do = do.Where(t.UpdateBy.Eq(query.UpdateBy))
		}
		if query.Remark != "" { //字符串
			do = do.Where(t.Remark.Like("%" + query.Remark + "%"))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.DictCode
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TSysDictData
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListSysDictData error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.SysDictData, len(list))
	for i, v := range list {
		result[i] = convert.SysDictData_db2pb(v)
	}
	return result, total, nil
}
