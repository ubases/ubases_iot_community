// Code generated by sgen.exe,2022-07-25 09:29:04. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_system/model"
	"cloud_platform/iot_model/db_system/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
	"cloud_platform/iot_system_service/convert"
)

type SysAppEntrySetingSvc struct {
	Ctx context.Context
}

// 创建SysAppEntrySeting
func (s *SysAppEntrySetingSvc) CreateSysAppEntrySeting(req *proto.SysAppEntrySeting) (*proto.SysAppEntrySeting, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TSysAppEntrySeting
	do := t.WithContext(context.Background())
	dbObj := convert.SysAppEntrySeting_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateSysAppEntrySeting error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除SysAppEntrySeting
func (s *SysAppEntrySetingSvc) DeleteSysAppEntrySeting(req *proto.SysAppEntrySeting) (*proto.SysAppEntrySeting, error) {
	t := orm.Use(iotmodel.GetDB()).TSysAppEntrySeting
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.DirId != 0 { //整数
		do = do.Where(t.DirId.Eq(req.DirId))
	}
	if req.Sort != 0 { //整数
		do = do.Where(t.Sort.Eq(req.Sort))
	}
	if req.IsEnable != 0 { //整数
		do = do.Where(t.IsEnable.Eq(req.IsEnable))
	}
	if req.IsNormal != 0 { //整数
		do = do.Where(t.IsNormal.Eq(req.IsNormal))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteSysAppEntrySeting error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除SysAppEntrySeting
func (s *SysAppEntrySetingSvc) DeleteByIdSysAppEntrySeting(req *proto.SysAppEntrySeting) (*proto.SysAppEntrySeting, error) {
	t := orm.Use(iotmodel.GetDB()).TSysAppEntrySeting
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdSysAppEntrySeting error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除SysAppEntrySeting
func (s *SysAppEntrySetingSvc) DeleteByIdsSysAppEntrySeting(req *proto.SysAppEntrySetingBatchDeleteRequest) (*proto.SysAppEntrySetingBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TSysAppEntrySeting
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsSysAppEntrySeting error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新SysAppEntrySeting
func (s *SysAppEntrySetingSvc) UpdateSysAppEntrySeting(req *proto.SysAppEntrySeting) (*proto.SysAppEntrySeting, error) {
	t := orm.Use(iotmodel.GetDB()).TSysAppEntrySeting
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.DirId != 0 { //整数
		updateField = append(updateField, t.DirId)
	}
	if req.Sort != 0 { //整数
		updateField = append(updateField, t.Sort)
	}
	if req.IsEnable != 0 { //整数
		updateField = append(updateField, t.IsEnable)
	}
	if req.IsNormal != 0 { //整数
		updateField = append(updateField, t.IsNormal)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateSysAppEntrySeting error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.SysAppEntrySeting_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateSysAppEntrySeting error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段SysAppEntrySeting
func (s *SysAppEntrySetingSvc) UpdateAllSysAppEntrySeting(req *proto.SysAppEntrySeting) (*proto.SysAppEntrySeting, error) {
	t := orm.Use(iotmodel.GetDB()).TSysAppEntrySeting
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.DirId)
	updateField = append(updateField, t.Sort)
	updateField = append(updateField, t.IsEnable)
	updateField = append(updateField, t.IsNormal)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllSysAppEntrySeting error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.SysAppEntrySeting_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllSysAppEntrySeting error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *SysAppEntrySetingSvc) UpdateFieldsSysAppEntrySeting(req *proto.SysAppEntrySetingUpdateFieldsRequest) (*proto.SysAppEntrySeting, error) {
	t := orm.Use(iotmodel.GetDB()).TSysAppEntrySeting
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsSysAppEntrySeting error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsSysAppEntrySeting error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.SysAppEntrySeting_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsSysAppEntrySeting error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找SysAppEntrySeting
func (s *SysAppEntrySetingSvc) FindSysAppEntrySeting(req *proto.SysAppEntrySetingFilter) (*proto.SysAppEntrySeting, error) {
	t := orm.Use(iotmodel.GetDB()).TSysAppEntrySeting
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.DirId != 0 { //整数
		do = do.Where(t.DirId.Eq(req.DirId))
	}
	if req.Sort != 0 { //整数
		do = do.Where(t.Sort.Eq(req.Sort))
	}
	if req.IsEnable != 0 { //整数
		do = do.Where(t.IsEnable.Eq(req.IsEnable))
	}
	if req.IsNormal != 0 { //整数
		do = do.Where(t.IsNormal.Eq(req.IsNormal))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindSysAppEntrySeting error : %s", err.Error())
		return nil, err
	}
	res := convert.SysAppEntrySeting_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找SysAppEntrySeting
func (s *SysAppEntrySetingSvc) FindByIdSysAppEntrySeting(req *proto.SysAppEntrySetingFilter) (*proto.SysAppEntrySeting, error) {
	t := orm.Use(iotmodel.GetDB()).TSysAppEntrySeting
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdSysAppEntrySeting error : %s", err.Error())
		return nil, err
	}
	res := convert.SysAppEntrySeting_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找SysAppEntrySeting,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *SysAppEntrySetingSvc) GetListSysAppEntrySeting(req *proto.SysAppEntrySetingListRequest) ([]*proto.SysAppEntrySeting, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TSysAppEntrySeting
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.DirId != 0 { //整数
			do = do.Where(t.DirId.Eq(query.DirId))
		}
		if query.Sort != 0 { //整数
			do = do.Where(t.Sort.Eq(query.Sort))
		}
		if query.IsEnable != 0 { //整数
			do = do.Where(t.IsEnable.Eq(query.IsEnable))
		}
		if query.IsNormal != 0 { //整数
			do = do.Where(t.IsNormal.Eq(query.IsNormal))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TSysAppEntrySeting
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListSysAppEntrySeting error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.SysAppEntrySeting, len(list))
	for i, v := range list {
		result[i] = convert.SysAppEntrySeting_db2pb(v)
	}
	return result, total, nil
}

// 创建CreateSysAppEntrySetingBatch
func (s *SysAppEntrySetingSvc) CreateSysAppEntrySetingBatch(req *proto.SysAppEntrySetingBatchRequest) (*proto.SysAppEntrySetingBatchRequest, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TSysAppEntrySeting
	do := t.WithContext(context.Background())
	reqBatch := make([]*model.TSysAppEntrySeting, 0)
	for i := range req.SysAppEntrySetings {
		reqBatch = append(reqBatch, convert.SysAppEntrySeting_pb2db(req.SysAppEntrySetings[i]))
	}
	err := do.CreateInBatches(reqBatch, len(reqBatch))
	if err != nil {
		logger.Errorf("CreateSysAppEntrySetingBatch error : %s", err.Error())
		return nil, err
	}
	return req, err
}
