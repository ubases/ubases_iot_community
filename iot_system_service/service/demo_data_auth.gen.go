// Code generated by sgen.exe,2022-04-18 19:12:06. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_system/model"
	"cloud_platform/iot_model/db_system/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
	"cloud_platform/iot_system_service/convert"
)

type DemoDataAuthSvc struct {
	Ctx context.Context
}

// 创建DemoDataAuth
func (s *DemoDataAuthSvc) CreateDemoDataAuth(req *proto.DemoDataAuth) (*proto.DemoDataAuth, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TDemoDataAuth
	do := t.WithContext(context.Background())
	dbObj := convert.DemoDataAuth_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateDemoDataAuth error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除DemoDataAuth
func (s *DemoDataAuthSvc) DeleteDemoDataAuth(req *proto.DemoDataAuth) (*proto.DemoDataAuth, error) {
	t := orm.Use(iotmodel.GetDB()).TDemoDataAuth
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.Title != "" { //字符串
		do = do.Where(t.Title.Eq(req.Title))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteDemoDataAuth error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除DemoDataAuth
func (s *DemoDataAuthSvc) DeleteByIdDemoDataAuth(req *proto.DemoDataAuth) (*proto.DemoDataAuth, error) {
	t := orm.Use(iotmodel.GetDB()).TDemoDataAuth
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdDemoDataAuth error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除DemoDataAuth
func (s *DemoDataAuthSvc) DeleteByIdsDemoDataAuth(req *proto.DemoDataAuthBatchDeleteRequest) (*proto.DemoDataAuthBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TDemoDataAuth
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsDemoDataAuth error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新DemoDataAuth
func (s *DemoDataAuthSvc) UpdateDemoDataAuth(req *proto.DemoDataAuth) (*proto.DemoDataAuth, error) {
	t := orm.Use(iotmodel.GetDB()).TDemoDataAuth
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.Title != "" { //字符串
		updateField = append(updateField, t.Title)
	}
	if req.CreatedBy != 0 { //整数
		updateField = append(updateField, t.CreatedBy)
	}
	if req.UpdatedBy != 0 { //整数
		updateField = append(updateField, t.UpdatedBy)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateDemoDataAuth error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.DemoDataAuth_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateDemoDataAuth error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段DemoDataAuth
func (s *DemoDataAuthSvc) UpdateAllDemoDataAuth(req *proto.DemoDataAuth) (*proto.DemoDataAuth, error) {
	t := orm.Use(iotmodel.GetDB()).TDemoDataAuth
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.Title)
	updateField = append(updateField, t.CreatedBy)
	updateField = append(updateField, t.UpdatedBy)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllDemoDataAuth error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.DemoDataAuth_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllDemoDataAuth error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *DemoDataAuthSvc) UpdateFieldsDemoDataAuth(req *proto.DemoDataAuthUpdateFieldsRequest) (*proto.DemoDataAuth, error) {
	t := orm.Use(iotmodel.GetDB()).TDemoDataAuth
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFields error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)
	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsDemoDataAuth error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.DemoDataAuth_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsDemoDataAuth error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找DemoDataAuth
func (s *DemoDataAuthSvc) FindDemoDataAuth(req *proto.DemoDataAuthFilter) (*proto.DemoDataAuth, error) {
	t := orm.Use(iotmodel.GetDB()).TDemoDataAuth
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.Title != "" { //字符串
		do = do.Where(t.Title.Like("%" + req.Title + "%"))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindDemoDataAuth error : %s", err.Error())
		return nil, err
	}
	res := convert.DemoDataAuth_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找DemoDataAuth
func (s *DemoDataAuthSvc) FindByIdDemoDataAuth(req *proto.DemoDataAuthFilter) (*proto.DemoDataAuth, error) {
	t := orm.Use(iotmodel.GetDB()).TDemoDataAuth
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdDemoDataAuth error : %s", err.Error())
		return nil, err
	}
	res := convert.DemoDataAuth_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找DemoDataAuth,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *DemoDataAuthSvc) GetListDemoDataAuth(req *proto.DemoDataAuthListRequest) ([]*proto.DemoDataAuth, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TDemoDataAuth
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.Title != "" { //字符串
			do = do.Where(t.Title.Like("%" + query.Title + "%"))
		}
		if query.CreatedBy != 0 { //整数
			do = do.Where(t.CreatedBy.Eq(query.CreatedBy))
		}
		if query.UpdatedBy != 0 { //整数
			do = do.Where(t.UpdatedBy.Eq(query.UpdatedBy))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TDemoDataAuth
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListDemoDataAuth error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.DemoDataAuth, len(list))
	for i, v := range list {
		result[i] = convert.DemoDataAuth_db2pb(v)
	}
	return result, total, nil
}
