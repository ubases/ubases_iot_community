// Code generated by sgen.exe,2022-06-02 11:15:12. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	"cloud_platform/iot_app_oem_service/convert"
	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_app_oem/model"
	"cloud_platform/iot_model/db_app_oem/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type OemAppPushCertSvc struct {
	Ctx context.Context
}

// 创建OemAppPushCert
func (s *OemAppPushCertSvc) CreateOemAppPushCert(req *proto.OemAppPushCert) (*proto.OemAppPushCert, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TOemAppPushCert
	do := t.WithContext(context.Background())
	dbObj := convert.OemAppPushCert_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateOemAppPushCert error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除OemAppPushCert
func (s *OemAppPushCertSvc) DeleteOemAppPushCert(req *proto.OemAppPushCert) (*proto.OemAppPushCert, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppPushCert
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.AppId != 0 { //整数
		do = do.Where(t.AppId.Eq(req.AppId))
	}
	if req.Version != "" { //字符串
		do = do.Where(t.Version.Eq(req.Version))
	}
	if req.Apns != "" { //字符串
		do = do.Where(t.Apns.Eq(req.Apns))
	}
	if req.Jpush != "" { //字符串
		do = do.Where(t.Jpush.Eq(req.Jpush))
	}
	if req.Fcm != "" { //字符串
		do = do.Where(t.Fcm.Eq(req.Fcm))
	}
	if req.Huawei != "" { //字符串
		do = do.Where(t.Huawei.Eq(req.Huawei))
	}
	if req.Xiaomi != "" { //字符串
		do = do.Where(t.Xiaomi.Eq(req.Xiaomi))
	}
	if req.Vivo != "" { //字符串
		do = do.Where(t.Vivo.Eq(req.Vivo))
	}
	if req.Oppo != "" { //字符串
		do = do.Where(t.Oppo.Eq(req.Oppo))
	}
	if req.Honor != "" { //字符串
		do = do.Where(t.Honor.Eq(req.Honor))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteOemAppPushCert error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除OemAppPushCert
func (s *OemAppPushCertSvc) DeleteByIdOemAppPushCert(req *proto.OemAppPushCert) (*proto.OemAppPushCert, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppPushCert
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdOemAppPushCert error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除OemAppPushCert
func (s *OemAppPushCertSvc) DeleteByIdsOemAppPushCert(req *proto.OemAppPushCertBatchDeleteRequest) (*proto.OemAppPushCertBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TOemAppPushCert
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsOemAppPushCert error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新OemAppPushCert
func (s *OemAppPushCertSvc) UpdateOemAppPushCert(req *proto.OemAppPushCert) (*proto.OemAppPushCert, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppPushCert
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.AppId != 0 { //整数
		updateField = append(updateField, t.AppId)
	}
	if req.Version != "" { //字符串
		updateField = append(updateField, t.Version)
	}
	if req.Apns != "" { //字符串
		updateField = append(updateField, t.Apns)
	}
	if req.Jpush != "" { //字符串
		updateField = append(updateField, t.Jpush)
	}
	if req.Fcm != "" { //字符串
		updateField = append(updateField, t.Fcm)
	}
	if req.Huawei != "" { //字符串
		updateField = append(updateField, t.Huawei)
	}
	if req.Xiaomi != "" { //字符串
		updateField = append(updateField, t.Xiaomi)
	}
	if req.Vivo != "" { //字符串
		updateField = append(updateField, t.Vivo)
	}
	if req.Oppo != "" { //字符串
		updateField = append(updateField, t.Oppo)
	}
	if req.Honor != "" { //字符串
		updateField = append(updateField, t.Honor)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateOemAppPushCert error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.OemAppPushCert_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateOemAppPushCert error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段OemAppPushCert
func (s *OemAppPushCertSvc) UpdateAllOemAppPushCert(req *proto.OemAppPushCert) (*proto.OemAppPushCert, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppPushCert
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.AppId)
	updateField = append(updateField, t.Version)
	updateField = append(updateField, t.Apns)
	updateField = append(updateField, t.Jpush)
	updateField = append(updateField, t.Fcm)
	updateField = append(updateField, t.Huawei)
	updateField = append(updateField, t.Xiaomi)
	updateField = append(updateField, t.Vivo)
	updateField = append(updateField, t.Oppo)
	updateField = append(updateField, t.Honor)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllOemAppPushCert error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OemAppPushCert_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllOemAppPushCert error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *OemAppPushCertSvc) UpdateFieldsOemAppPushCert(req *proto.OemAppPushCertUpdateFieldsRequest) (*proto.OemAppPushCert, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppPushCert
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsOemAppPushCert error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsOemAppPushCert error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OemAppPushCert_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsOemAppPushCert error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找OemAppPushCert
func (s *OemAppPushCertSvc) FindOemAppPushCert(req *proto.OemAppPushCertFilter) (*proto.OemAppPushCert, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppPushCert
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.AppId != 0 { //整数
		do = do.Where(t.AppId.Eq(req.AppId))
	}
	if req.Version != "" { //字符串
		do = do.Where(t.Version.Eq(req.Version))
	}
	if req.Apns != "" { //字符串
		do = do.Where(t.Apns.Eq(req.Apns))
	}
	if req.Jpush != "" { //字符串
		do = do.Where(t.Jpush.Eq(req.Jpush))
	}
	if req.Fcm != "" { //字符串
		do = do.Where(t.Fcm.Eq(req.Fcm))
	}
	if req.Huawei != "" { //字符串
		do = do.Where(t.Huawei.Eq(req.Huawei))
	}
	if req.Xiaomi != "" { //字符串
		do = do.Where(t.Xiaomi.Eq(req.Xiaomi))
	}
	if req.Vivo != "" { //字符串
		do = do.Where(t.Vivo.Eq(req.Vivo))
	}
	if req.Oppo != "" { //字符串
		do = do.Where(t.Oppo.Eq(req.Oppo))
	}
	if req.Honor != "" { //字符串
		do = do.Where(t.Honor.Eq(req.Honor))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindOemAppPushCert error : %s", err.Error())
		return nil, err
	}
	res := convert.OemAppPushCert_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找OemAppPushCert
func (s *OemAppPushCertSvc) FindByIdOemAppPushCert(req *proto.OemAppPushCertFilter) (*proto.OemAppPushCert, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppPushCert
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdOemAppPushCert error : %s", err.Error())
		return nil, err
	}
	res := convert.OemAppPushCert_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找OemAppPushCert,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *OemAppPushCertSvc) GetListOemAppPushCert(req *proto.OemAppPushCertListRequest) ([]*proto.OemAppPushCert, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TOemAppPushCert
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.AppId != 0 { //整数
			do = do.Where(t.AppId.Eq(query.AppId))
		}
		if query.Version != "" { //字符串
			do = do.Where(t.Version.Like("%" + query.Version + "%"))
		}
		if query.Apns != "" { //字符串
			do = do.Where(t.Apns.Like("%" + query.Apns + "%"))
		}
		if query.Jpush != "" { //字符串
			do = do.Where(t.Jpush.Like("%" + query.Jpush + "%"))
		}
		if query.Fcm != "" { //字符串
			do = do.Where(t.Fcm.Like("%" + query.Fcm + "%"))
		}
		if query.Huawei != "" { //字符串
			do = do.Where(t.Huawei.Like("%" + query.Huawei + "%"))
		}
		if query.Xiaomi != "" { //字符串
			do = do.Where(t.Xiaomi.Like("%" + query.Xiaomi + "%"))
		}
		if query.Vivo != "" { //字符串
			do = do.Where(t.Vivo.Like("%" + query.Vivo + "%"))
		}
		if query.Oppo != "" { //字符串
			do = do.Where(t.Oppo.Like("%" + query.Oppo + "%"))
		}
		if query.Honor != "" { //字符串
			do = do.Where(t.Honor.Like("%" + query.Honor + "%"))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TOemAppPushCert
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListOemAppPushCert error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.OemAppPushCert, len(list))
	for i, v := range list {
		result[i] = convert.OemAppPushCert_db2pb(v)
	}
	return result, total, nil
}
