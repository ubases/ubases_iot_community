// Code generated by sgen.exe,2022-10-24 08:40:56. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"cloud_platform/iot_common/iotutil"
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	"cloud_platform/iot_app_oem_service/convert"
	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_app_oem/model"
	"cloud_platform/iot_model/db_app_oem/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type OemAppTemplateThirdPartySvc struct {
	Ctx context.Context
}

// CopyOemAppTemplateThirdParty
func (s *OemAppTemplateThirdPartySvc) CopyOemAppTemplateThirdParty(tx *orm.Query, appTemplateId, newAppTemplateId int64) error {
	// fixme 请在这里校验参数
	t := tx.TOemAppTemplateThirdParty
	do := t.WithContext(context.Background())

	list, err := do.Where(t.AppTemplateId.Eq(appTemplateId)).Find()
	if err != nil {
		return err
	}
	saveList := make([]*model.TOemAppTemplateThirdParty, 0)
	for _, function := range list {
		function.Id = iotutil.GetNextSeqInt64()
		function.AppTemplateId = newAppTemplateId
		saveList = append(saveList, function)
	}
	if len(saveList) == 0 {
		return nil
	}
	err = do.Create(saveList...)
	if err != nil {
		return err
	}
	return err
}

// 创建OemAppTemplateThirdParty
func (s *OemAppTemplateThirdPartySvc) CreateOemAppTemplateThirdParty(req *proto.OemAppTemplateThirdParty) (*proto.OemAppTemplateThirdParty, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateThirdParty
	do := t.WithContext(context.Background())
	req.Id = iotutil.GetNextSeqInt64()
	dbObj := convert.OemAppTemplateThirdParty_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateOemAppTemplateThirdParty error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除OemAppTemplateThirdParty
func (s *OemAppTemplateThirdPartySvc) DeleteOemAppTemplateThirdParty(req *proto.OemAppTemplateThirdParty) (*proto.OemAppTemplateThirdParty, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateThirdParty
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.Type != 0 { //整数
		do = do.Where(t.Type.Eq(req.Type))
	}
	if req.Name != "" { //字符串
		do = do.Where(t.Name.Eq(req.Name))
	}
	if req.NameEn != "" { //字符串
		do = do.Where(t.NameEn.Eq(req.NameEn))
	}
	if req.Code != "" { //字符串
		do = do.Where(t.Code.Eq(req.Code))
	}
	if req.Sort != 0 { //整数
		do = do.Where(t.Sort.Eq(req.Sort))
	}
	if req.Icon != "" { //字符串
		do = do.Where(t.Icon.Eq(req.Icon))
	}
	if req.AppTemplateId != 0 { //整数
		do = do.Where(t.AppTemplateId.Eq(req.AppTemplateId))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteOemAppTemplateThirdParty error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除OemAppTemplateThirdParty
func (s *OemAppTemplateThirdPartySvc) DeleteByIdOemAppTemplateThirdParty(req *proto.OemAppTemplateThirdParty) (*proto.OemAppTemplateThirdParty, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateThirdParty
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdOemAppTemplateThirdParty error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除OemAppTemplateThirdParty
func (s *OemAppTemplateThirdPartySvc) DeleteByIdsOemAppTemplateThirdParty(req *proto.OemAppTemplateThirdPartyBatchDeleteRequest) (*proto.OemAppTemplateThirdPartyBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TOemAppTemplateThirdParty
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsOemAppTemplateThirdParty error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新OemAppTemplateThirdParty
func (s *OemAppTemplateThirdPartySvc) UpdateOemAppTemplateThirdParty(req *proto.OemAppTemplateThirdParty) (*proto.OemAppTemplateThirdParty, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateThirdParty
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.Type != 0 { //整数
		updateField = append(updateField, t.Type)
	}
	if req.Name != "" { //字符串
		updateField = append(updateField, t.Name)
	}
	if req.NameEn != "" { //字符串
		updateField = append(updateField, t.NameEn)
	}
	if req.Code != "" { //字符串
		updateField = append(updateField, t.Code)
	}
	if req.Sort != 0 { //整数
		updateField = append(updateField, t.Sort)
	}
	if req.Icon != "" { //字符串
		updateField = append(updateField, t.Icon)
	}
	if req.AppTemplateId != 0 { //整数
		updateField = append(updateField, t.AppTemplateId)
	}
	if req.CreatedBy != 0 { //整数
		updateField = append(updateField, t.CreatedBy)
	}
	if req.UpdatedBy != 0 { //整数
		updateField = append(updateField, t.UpdatedBy)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateOemAppTemplateThirdParty error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.OemAppTemplateThirdParty_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateOemAppTemplateThirdParty error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段OemAppTemplateThirdParty
func (s *OemAppTemplateThirdPartySvc) UpdateAllOemAppTemplateThirdParty(req *proto.OemAppTemplateThirdParty) (*proto.OemAppTemplateThirdParty, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateThirdParty
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.Type)
	updateField = append(updateField, t.Name)
	updateField = append(updateField, t.NameEn)
	updateField = append(updateField, t.Code)
	updateField = append(updateField, t.Sort)
	updateField = append(updateField, t.Icon)
	updateField = append(updateField, t.AppTemplateId)
	updateField = append(updateField, t.CreatedBy)
	updateField = append(updateField, t.UpdatedBy)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllOemAppTemplateThirdParty error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OemAppTemplateThirdParty_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllOemAppTemplateThirdParty error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *OemAppTemplateThirdPartySvc) UpdateFieldsOemAppTemplateThirdParty(req *proto.OemAppTemplateThirdPartyUpdateFieldsRequest) (*proto.OemAppTemplateThirdParty, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateThirdParty
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsOemAppTemplateThirdParty error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsOemAppTemplateThirdParty error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OemAppTemplateThirdParty_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsOemAppTemplateThirdParty error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找OemAppTemplateThirdParty
func (s *OemAppTemplateThirdPartySvc) FindOemAppTemplateThirdParty(req *proto.OemAppTemplateThirdPartyFilter) (*proto.OemAppTemplateThirdParty, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateThirdParty
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.Type != 0 { //整数
		do = do.Where(t.Type.Eq(req.Type))
	}
	if req.Name != "" { //字符串
		do = do.Where(t.Name.Eq(req.Name))
	}
	if req.NameEn != "" { //字符串
		do = do.Where(t.NameEn.Eq(req.NameEn))
	}
	if req.Code != "" { //字符串
		do = do.Where(t.Code.Eq(req.Code))
	}
	if req.Sort != 0 { //整数
		do = do.Where(t.Sort.Eq(req.Sort))
	}
	if req.Icon != "" { //字符串
		do = do.Where(t.Icon.Eq(req.Icon))
	}
	if req.AppTemplateId != 0 { //整数
		do = do.Where(t.AppTemplateId.Eq(req.AppTemplateId))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindOemAppTemplateThirdParty error : %s", err.Error())
		return nil, err
	}
	res := convert.OemAppTemplateThirdParty_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找OemAppTemplateThirdParty
func (s *OemAppTemplateThirdPartySvc) FindByIdOemAppTemplateThirdParty(req *proto.OemAppTemplateThirdPartyFilter) (*proto.OemAppTemplateThirdParty, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateThirdParty
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdOemAppTemplateThirdParty error : %s", err.Error())
		return nil, err
	}
	res := convert.OemAppTemplateThirdParty_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找OemAppTemplateThirdParty,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *OemAppTemplateThirdPartySvc) GetListOemAppTemplateThirdParty(req *proto.OemAppTemplateThirdPartyListRequest) ([]*proto.OemAppTemplateThirdParty, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateThirdParty
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.Type != 0 { //整数
			do = do.Where(t.Type.Eq(query.Type))
		}
		if query.Name != "" { //字符串
			do = do.Where(t.Name.Like("%" + query.Name + "%"))
		}
		if query.NameEn != "" { //字符串
			do = do.Where(t.NameEn.Like("%" + query.NameEn + "%"))
		}
		if query.Code != "" { //字符串
			do = do.Where(t.Code.Like("%" + query.Code + "%"))
		}
		if query.Sort != 0 { //整数
			do = do.Where(t.Sort.Eq(query.Sort))
		}
		if query.Icon != "" { //字符串
			do = do.Where(t.Icon.Like("%" + query.Icon + "%"))
		}
		if query.AppTemplateId != 0 { //整数
			do = do.Where(t.AppTemplateId.Eq(query.AppTemplateId))
		}
		if query.CreatedBy != 0 { //整数
			do = do.Where(t.CreatedBy.Eq(query.CreatedBy))
		}
		if query.UpdatedBy != 0 { //整数
			do = do.Where(t.UpdatedBy.Eq(query.UpdatedBy))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TOemAppTemplateThirdParty
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListOemAppTemplateThirdParty error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.OemAppTemplateThirdParty, len(list))
	for i, v := range list {
		result[i] = convert.OemAppTemplateThirdParty_db2pb(v)
	}
	return result, total, nil
}
