// Code generated by sgen.exe,2022-07-14 15:09:59. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	"cloud_platform/iot_app_oem_service/convert"
	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_app_oem/model"
	"cloud_platform/iot_model/db_app_oem/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type OemAppDocRelationSvc struct {
	Ctx context.Context
}

// 创建OemAppDocRelation
func (s *OemAppDocRelationSvc) CreateOemAppDocRelation(req *proto.OemAppDocRelation) (*proto.OemAppDocRelation, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TOemAppDocRelation
	do := t.WithContext(context.Background())
	dbObj := convert.OemAppDocRelation_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateOemAppDocRelation error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除OemAppDocRelation
func (s *OemAppDocRelationSvc) DeleteOemAppDocRelation(req *proto.OemAppDocRelation) (*proto.OemAppDocRelation, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppDocRelation
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.DocId != 0 { //整数
		do = do.Where(t.DocId.Eq(req.DocId))
	}
	if req.AppId != 0 { //整数
		do = do.Where(t.AppId.Eq(req.AppId))
	}
	if req.AppKey != "" { //字符串
		do = do.Where(t.AppKey.Eq(req.AppKey))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteOemAppDocRelation error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除OemAppDocRelation
func (s *OemAppDocRelationSvc) DeleteByIdOemAppDocRelation(req *proto.OemAppDocRelation) (*proto.OemAppDocRelation, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppDocRelation
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdOemAppDocRelation error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除OemAppDocRelation
func (s *OemAppDocRelationSvc) DeleteByIdsOemAppDocRelation(req *proto.OemAppDocRelationBatchDeleteRequest) (*proto.OemAppDocRelationBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TOemAppDocRelation
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsOemAppDocRelation error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新OemAppDocRelation
func (s *OemAppDocRelationSvc) UpdateOemAppDocRelation(req *proto.OemAppDocRelation) (*proto.OemAppDocRelation, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppDocRelation
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.DocId != 0 { //整数
		updateField = append(updateField, t.DocId)
	}
	if req.AppId != 0 { //整数
		updateField = append(updateField, t.AppId)
	}
	if req.AppKey != "" { //字符串
		updateField = append(updateField, t.AppKey)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateOemAppDocRelation error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.OemAppDocRelation_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateOemAppDocRelation error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段OemAppDocRelation
func (s *OemAppDocRelationSvc) UpdateAllOemAppDocRelation(req *proto.OemAppDocRelation) (*proto.OemAppDocRelation, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppDocRelation
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.DocId)
	updateField = append(updateField, t.AppId)
	updateField = append(updateField, t.AppKey)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllOemAppDocRelation error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OemAppDocRelation_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllOemAppDocRelation error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *OemAppDocRelationSvc) UpdateFieldsOemAppDocRelation(req *proto.OemAppDocRelationUpdateFieldsRequest) (*proto.OemAppDocRelation, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppDocRelation
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsOemAppDocRelation error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsOemAppDocRelation error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OemAppDocRelation_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsOemAppDocRelation error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找OemAppDocRelation
func (s *OemAppDocRelationSvc) FindOemAppDocRelation(req *proto.OemAppDocRelationFilter) (*proto.OemAppDocRelation, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppDocRelation
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.DocId != 0 { //整数
		do = do.Where(t.DocId.Eq(req.DocId))
	}
	if req.AppId != 0 { //整数
		do = do.Where(t.AppId.Eq(req.AppId))
	}
	if req.AppKey != "" { //字符串
		do = do.Where(t.AppKey.Eq(req.AppKey))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindOemAppDocRelation error : %s", err.Error())
		return nil, err
	}
	res := convert.OemAppDocRelation_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找OemAppDocRelation
func (s *OemAppDocRelationSvc) FindByIdOemAppDocRelation(req *proto.OemAppDocRelationFilter) (*proto.OemAppDocRelation, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppDocRelation
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdOemAppDocRelation error : %s", err.Error())
		return nil, err
	}
	res := convert.OemAppDocRelation_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找OemAppDocRelation,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *OemAppDocRelationSvc) GetListOemAppDocRelation(req *proto.OemAppDocRelationListRequest) ([]*proto.OemAppDocRelation, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TOemAppDocRelation
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.DocId != 0 { //整数
			do = do.Where(t.DocId.Eq(query.DocId))
		}
		if query.AppId != 0 { //整数
			do = do.Where(t.AppId.Eq(query.AppId))
		}
		if query.AppKey != "" { //字符串
			do = do.Where(t.AppKey.Like("%" + query.AppKey + "%"))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TOemAppDocRelation
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListOemAppDocRelation error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.OemAppDocRelation, len(list))
	for i, v := range list {
		result[i] = convert.OemAppDocRelation_db2pb(v)
	}
	return result, total, nil
}
