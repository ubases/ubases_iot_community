// Code generated by sgen.exe,2022-10-24 08:40:53. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"cloud_platform/iot_common/iotutil"
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	"cloud_platform/iot_app_oem_service/convert"
	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_app_oem/model"
	"cloud_platform/iot_model/db_app_oem/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type OemAppAssistReleaseMarketSvc struct {
	Ctx context.Context
}

// 创建OemAppAssistReleaseMarket
func (s *OemAppAssistReleaseMarketSvc) CreateOemAppAssistReleaseMarket(req *proto.OemAppAssistReleaseMarket) (*proto.OemAppAssistReleaseMarket, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TOemAppAssistReleaseMarket
	do := t.WithContext(context.Background())
	req.Id = iotutil.GetNextSeqInt64()
	dbObj := convert.OemAppAssistReleaseMarket_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateOemAppAssistReleaseMarket error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除OemAppAssistReleaseMarket
func (s *OemAppAssistReleaseMarketSvc) DeleteOemAppAssistReleaseMarket(req *proto.OemAppAssistReleaseMarket) (*proto.OemAppAssistReleaseMarket, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppAssistReleaseMarket
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.MarketId != 0 { //整数
		do = do.Where(t.MarketId.Eq(req.MarketId))
	}
	if req.ReleaseStatus != 0 {
		do = do.Where(t.ReleaseStatus.Eq(req.ReleaseStatus))
	}
	if req.AssistReleaseId != 0 {
		do = do.Where(t.AssistReleaseId.Eq(req.AssistReleaseId))
	}
	if req.IsCheck != 0 {
		do = do.Where(t.IsCheck.Eq(req.IsCheck))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteOemAppAssistReleaseMarket error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除OemAppAssistReleaseMarket
func (s *OemAppAssistReleaseMarketSvc) DeleteByIdOemAppAssistReleaseMarket(req *proto.OemAppAssistReleaseMarket) (*proto.OemAppAssistReleaseMarket, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppAssistReleaseMarket
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdOemAppAssistReleaseMarket error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除OemAppAssistReleaseMarket
func (s *OemAppAssistReleaseMarketSvc) DeleteByIdsOemAppAssistReleaseMarket(req *proto.OemAppAssistReleaseMarketBatchDeleteRequest) (*proto.OemAppAssistReleaseMarketBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TOemAppAssistReleaseMarket
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsOemAppAssistReleaseMarket error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新OemAppAssistReleaseMarket
func (s *OemAppAssistReleaseMarketSvc) UpdateOemAppAssistReleaseMarket(req *proto.OemAppAssistReleaseMarket) (*proto.OemAppAssistReleaseMarket, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppAssistReleaseMarket
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.MarketId != 0 { //整数
		updateField = append(updateField, t.MarketId)
	}
	if req.ReleaseStatus != 0 {
		updateField = append(updateField, t.ReleaseStatus)
	}
	if req.FinishTime != nil { //字符串
		updateField = append(updateField, t.FinishTime)
	}
	if req.AssistReleaseId != 0 {
		updateField = append(updateField, t.AssistReleaseId)
	}
	if req.IsCheck != 0 {
		updateField = append(updateField, t.IsCheck)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateOemAppAssistReleaseMarket error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.OemAppAssistReleaseMarket_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateOemAppAssistReleaseMarket error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段OemAppAssistReleaseMarket
func (s *OemAppAssistReleaseMarketSvc) UpdateAllOemAppAssistReleaseMarket(req *proto.OemAppAssistReleaseMarket) (*proto.OemAppAssistReleaseMarket, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppAssistReleaseMarket
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.MarketId)
	updateField = append(updateField, t.ReleaseStatus)
	updateField = append(updateField, t.FinishTime)
	updateField = append(updateField, t.AssistReleaseId)
	updateField = append(updateField, t.IsCheck)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllOemAppAssistReleaseMarket error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OemAppAssistReleaseMarket_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllOemAppAssistReleaseMarket error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *OemAppAssistReleaseMarketSvc) UpdateFieldsOemAppAssistReleaseMarket(req *proto.OemAppAssistReleaseMarketUpdateFieldsRequest) (*proto.OemAppAssistReleaseMarket, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppAssistReleaseMarket
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsOemAppAssistReleaseMarket error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsOemAppAssistReleaseMarket error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OemAppAssistReleaseMarket_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsOemAppAssistReleaseMarket error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找OemAppAssistReleaseMarket
func (s *OemAppAssistReleaseMarketSvc) FindOemAppAssistReleaseMarket(req *proto.OemAppAssistReleaseMarketFilter) (*proto.OemAppAssistReleaseMarket, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppAssistReleaseMarket
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.MarketId != 0 { //整数
		do = do.Where(t.MarketId.Eq(req.MarketId))
	}
	if req.ReleaseStatus != 0 {
		do = do.Where(t.ReleaseStatus.Eq(req.ReleaseStatus))
	}
	if req.FinishTime != nil {
		do = do.Where(t.FinishTime.Eq(req.FinishTime.AsTime()))
	}
	if req.AssistReleaseId != 0 {
		do = do.Where(t.AssistReleaseId.Eq(req.AssistReleaseId))
	}
	if req.IsCheck != 0 {
		do = do.Where(t.IsCheck.Eq(req.IsCheck))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindOemAppAssistReleaseMarket error : %s", err.Error())
		return nil, err
	}
	res := convert.OemAppAssistReleaseMarket_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找OemAppAssistReleaseMarket
func (s *OemAppAssistReleaseMarketSvc) FindByIdOemAppAssistReleaseMarket(req *proto.OemAppAssistReleaseMarketFilter) (*proto.OemAppAssistReleaseMarket, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppAssistReleaseMarket
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdOemAppAssistReleaseMarket error : %s", err.Error())
		return nil, err
	}
	res := convert.OemAppAssistReleaseMarket_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找OemAppAssistReleaseMarket,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *OemAppAssistReleaseMarketSvc) GetListOemAppAssistReleaseMarket(req *proto.OemAppAssistReleaseMarketListRequest) ([]*proto.OemAppAssistReleaseMarket, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TOemAppAssistReleaseMarket
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.MarketId != 0 { //整数
			do = do.Where(t.MarketId.Eq(query.MarketId))
		}
		if query.ReleaseStatus != 0 {
			do = do.Where(t.ReleaseStatus.Eq(query.ReleaseStatus))
		}
		if query.FinishTime != nil { //字符串
			do = do.Where(t.FinishTime.Eq(query.FinishTime.AsTime()))
		}
		if query.AssistReleaseId != 0 {
			do = do.Where(t.AssistReleaseId.Eq(query.AssistReleaseId))
		}
		if query.IsCheck != 0 { //字符串
			do = do.Where(t.IsCheck.Eq(query.IsCheck))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TOemAppAssistReleaseMarket
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListOemAppAssistReleaseMarket error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.OemAppAssistReleaseMarket, len(list))
	for i, v := range list {
		result[i] = convert.OemAppAssistReleaseMarket_db2pb(v)
	}
	return result, total, nil
}
