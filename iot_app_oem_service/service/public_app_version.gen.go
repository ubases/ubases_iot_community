// Code generated by sgen.exe,2022-10-24 08:40:57. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"cloud_platform/iot_common/iotutil"
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	"cloud_platform/iot_app_oem_service/convert"
	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_app_oem/model"
	"cloud_platform/iot_model/db_app_oem/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type PublicAppVersionSvc struct {
	Ctx context.Context
}

// 创建PublicAppVersion
func (s *PublicAppVersionSvc) CreatePublicAppVersion(req *proto.PublicAppVersion) (*proto.PublicAppVersion, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TPublicAppVersion
	do := t.WithContext(context.Background())
	req.Id = iotutil.GetNextSeqInt64()
	dbObj := convert.PublicAppVersion_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreatePublicAppVersion error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除PublicAppVersion
func (s *PublicAppVersionSvc) DeletePublicAppVersion(req *proto.PublicAppVersion) (*proto.PublicAppVersion, error) {
	t := orm.Use(iotmodel.GetDB()).TPublicAppVersion
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.AppKey != "" { //字符串
		do = do.Where(t.AppKey.Eq(req.AppKey))
	}
	if req.ReleaseMarket != 0 { //整数
		do = do.Where(t.ReleaseMarket.Eq(req.ReleaseMarket))
	}
	if req.Version != "" { //字符串
		do = do.Where(t.Version.Eq(req.Version))
	}
	if req.AppType != 0 { //整数
		do = do.Where(t.AppType.Eq(req.AppType))
	}
	if req.Status != 0 { //整数
		do = do.Where(t.Status.Eq(req.Status))
	}
	if req.Remark != "" { //字符串
		do = do.Where(t.Remark.Eq(req.Remark))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeletePublicAppVersion error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除PublicAppVersion
func (s *PublicAppVersionSvc) DeleteByIdPublicAppVersion(req *proto.PublicAppVersion) (*proto.PublicAppVersion, error) {
	t := orm.Use(iotmodel.GetDB()).TPublicAppVersion
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdPublicAppVersion error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除PublicAppVersion
func (s *PublicAppVersionSvc) DeleteByIdsPublicAppVersion(req *proto.PublicAppVersionBatchDeleteRequest) (*proto.PublicAppVersionBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TPublicAppVersion
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsPublicAppVersion error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新PublicAppVersion
func (s *PublicAppVersionSvc) UpdatePublicAppVersion(req *proto.PublicAppVersion) (*proto.PublicAppVersion, error) {
	t := orm.Use(iotmodel.GetDB()).TPublicAppVersion
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.AppKey != "" { //字符串
		updateField = append(updateField, t.AppKey)
	}
	if req.ReleaseMarket != 0 { //整数
		updateField = append(updateField, t.ReleaseMarket)
	}
	if req.Version != "" { //字符串
		updateField = append(updateField, t.Version)
	}
	if req.AppType != 0 { //整数
		updateField = append(updateField, t.AppType)
	}
	if req.Status != 0 { //整数
		updateField = append(updateField, t.Status)
	}
	if req.Remark != "" { //字符串
		updateField = append(updateField, t.Remark)
	}
	if req.CreatedBy != 0 { //整数
		updateField = append(updateField, t.CreatedBy)
	}
	if req.UpdatedBy != 0 { //整数
		updateField = append(updateField, t.UpdatedBy)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdatePublicAppVersion error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.PublicAppVersion_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdatePublicAppVersion error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段PublicAppVersion
func (s *PublicAppVersionSvc) UpdateAllPublicAppVersion(req *proto.PublicAppVersion) (*proto.PublicAppVersion, error) {
	t := orm.Use(iotmodel.GetDB()).TPublicAppVersion
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.AppKey)
	updateField = append(updateField, t.ReleaseTime)
	updateField = append(updateField, t.ReleaseMarket)
	updateField = append(updateField, t.Version)
	updateField = append(updateField, t.AppType)
	updateField = append(updateField, t.Status)
	updateField = append(updateField, t.Remark)
	updateField = append(updateField, t.CreatedBy)
	updateField = append(updateField, t.UpdatedBy)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllPublicAppVersion error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.PublicAppVersion_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllPublicAppVersion error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *PublicAppVersionSvc) UpdateFieldsPublicAppVersion(req *proto.PublicAppVersionUpdateFieldsRequest) (*proto.PublicAppVersion, error) {
	t := orm.Use(iotmodel.GetDB()).TPublicAppVersion
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsPublicAppVersion error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsPublicAppVersion error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.PublicAppVersion_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsPublicAppVersion error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找PublicAppVersion
func (s *PublicAppVersionSvc) FindPublicAppVersion(req *proto.PublicAppVersionFilter) (*proto.PublicAppVersion, error) {
	t := orm.Use(iotmodel.GetDB()).TPublicAppVersion
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.AppKey != "" { //字符串
		do = do.Where(t.AppKey.Eq(req.AppKey))
	}
	if req.ReleaseMarket != 0 { //整数
		do = do.Where(t.ReleaseMarket.Eq(req.ReleaseMarket))
	}
	if req.Version != "" { //字符串
		do = do.Where(t.Version.Eq(req.Version))
	}
	if req.AppType != 0 { //整数
		do = do.Where(t.AppType.Eq(req.AppType))
	}
	if req.Status != 0 { //整数
		do = do.Where(t.Status.Eq(req.Status))
	}
	if req.Remark != "" { //字符串
		do = do.Where(t.Remark.Eq(req.Remark))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindPublicAppVersion error : %s", err.Error())
		return nil, err
	}
	res := convert.PublicAppVersion_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找PublicAppVersion
func (s *PublicAppVersionSvc) FindByIdPublicAppVersion(req *proto.PublicAppVersionFilter) (*proto.PublicAppVersion, error) {
	t := orm.Use(iotmodel.GetDB()).TPublicAppVersion
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdPublicAppVersion error : %s", err.Error())
		return nil, err
	}
	res := convert.PublicAppVersion_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找PublicAppVersion,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *PublicAppVersionSvc) GetListPublicAppVersion(req *proto.PublicAppVersionListRequest) ([]*proto.PublicAppVersion, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TPublicAppVersion
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.AppKey != "" { //字符串
			do = do.Where(t.AppKey.Like("%" + query.AppKey + "%"))
		}
		if query.ReleaseMarket != 0 { //整数
			do = do.Where(t.ReleaseMarket.Eq(query.ReleaseMarket))
		}
		if query.Version != "" { //字符串
			do = do.Where(t.Version.Like("%" + query.Version + "%"))
		}
		if query.AppType != 0 { //整数
			do = do.Where(t.AppType.Eq(query.AppType))
		}
		if query.Status != 0 { //整数
			do = do.Where(t.Status.Eq(query.Status))
		}
		if query.Remark != "" { //字符串
			do = do.Where(t.Remark.Like("%" + query.Remark + "%"))
		}
		if query.CreatedBy != 0 { //整数
			do = do.Where(t.CreatedBy.Eq(query.CreatedBy))
		}
		if query.UpdatedBy != 0 { //整数
			do = do.Where(t.UpdatedBy.Eq(query.UpdatedBy))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TPublicAppVersion
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListPublicAppVersion error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.PublicAppVersion, len(list))
	for i, v := range list {
		result[i] = convert.PublicAppVersion_db2pb(v)
	}
	return result, total, nil
}
