// Code generated by sgen.exe,2022-10-24 08:40:56. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"cloud_platform/iot_common/iotutil"
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	"cloud_platform/iot_app_oem_service/convert"
	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_app_oem/model"
	"cloud_platform/iot_model/db_app_oem/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type OemAppTemplateFunctionSvc struct {
	Ctx context.Context
}

// CopyOemAppTemplateFunction
func (s *OemAppTemplateFunctionSvc) CopyOemAppTemplateFunction(tx *orm.Query, appTemplateId, newAppTemplateId int64) error {
	// fixme 请在这里校验参数
	t := tx.TOemAppTemplateFunction
	do := t.WithContext(context.Background())

	list, err := do.Where(t.AppTemplateId.Eq(appTemplateId)).Find()
	if err != nil {
		return err
	}
	saveList := make([]*model.TOemAppTemplateFunction, 0)
	for _, function := range list {
		function.Id = iotutil.GetNextSeqInt64()
		function.AppTemplateId = newAppTemplateId
		saveList = append(saveList, function)
	}
	if len(saveList) == 0 {
		return nil
	}
	err = do.Create(saveList...)
	if err != nil {
		return err
	}
	return err
}

// 创建OemAppTemplateFunction
func (s *OemAppTemplateFunctionSvc) CreateOemAppTemplateFunction(req *proto.OemAppTemplateFunction) (*proto.OemAppTemplateFunction, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateFunction
	do := t.WithContext(context.Background())
	req.Id = iotutil.GetNextSeqInt64()
	dbObj := convert.OemAppTemplateFunction_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateOemAppTemplateFunction error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除OemAppTemplateFunction
func (s *OemAppTemplateFunctionSvc) DeleteOemAppTemplateFunction(req *proto.OemAppTemplateFunction) (*proto.OemAppTemplateFunction, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateFunction
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.Name != "" { //字符串
		do = do.Where(t.Name.Eq(req.Name))
	}
	if req.NameEn != "" { //字符串
		do = do.Where(t.NameEn.Eq(req.NameEn))
	}
	if req.Code != "" { //字符串
		do = do.Where(t.Code.Eq(req.Code))
	}
	if req.Sort != 0 { //整数
		do = do.Where(t.Sort.Eq(req.Sort))
	}
	if req.AppTemplateId != 0 { //整数
		do = do.Where(t.AppTemplateId.Eq(req.AppTemplateId))
	}
	if req.DefaultValueMode != 0 { //整数
		do = do.Where(t.DefaultValueMode.Eq(req.DefaultValueMode))
	}
	if req.DefaultValue != "" { //字符串
		do = do.Where(t.DefaultValue.Eq(req.DefaultValue))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteOemAppTemplateFunction error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除OemAppTemplateFunction
func (s *OemAppTemplateFunctionSvc) DeleteByIdOemAppTemplateFunction(req *proto.OemAppTemplateFunction) (*proto.OemAppTemplateFunction, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateFunction
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdOemAppTemplateFunction error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除OemAppTemplateFunction
func (s *OemAppTemplateFunctionSvc) DeleteByIdsOemAppTemplateFunction(req *proto.OemAppTemplateFunctionBatchDeleteRequest) (*proto.OemAppTemplateFunctionBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TOemAppTemplateFunction
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsOemAppTemplateFunction error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新OemAppTemplateFunction
func (s *OemAppTemplateFunctionSvc) UpdateOemAppTemplateFunction(req *proto.OemAppTemplateFunction) (*proto.OemAppTemplateFunction, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateFunction
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.Name != "" { //字符串
		updateField = append(updateField, t.Name)
	}
	if req.NameEn != "" { //字符串
		updateField = append(updateField, t.NameEn)
	}
	if req.Code != "" { //字符串
		updateField = append(updateField, t.Code)
	}
	if req.Sort != 0 { //整数
		updateField = append(updateField, t.Sort)
	}
	if req.AppTemplateId != 0 { //整数
		updateField = append(updateField, t.AppTemplateId)
	}
	if req.DefaultValueMode != 0 { //整数
		updateField = append(updateField, t.DefaultValueMode)
	}
	if req.DefaultValue != "" { //字符串
		updateField = append(updateField, t.DefaultValue)
	}
	if req.CreatedBy != 0 { //整数
		updateField = append(updateField, t.CreatedBy)
	}
	if req.UpdatedBy != 0 { //整数
		updateField = append(updateField, t.UpdatedBy)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateOemAppTemplateFunction error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.OemAppTemplateFunction_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateOemAppTemplateFunction error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段OemAppTemplateFunction
func (s *OemAppTemplateFunctionSvc) UpdateAllOemAppTemplateFunction(req *proto.OemAppTemplateFunction) (*proto.OemAppTemplateFunction, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateFunction
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.Name)
	updateField = append(updateField, t.NameEn)
	updateField = append(updateField, t.Code)
	updateField = append(updateField, t.Sort)
	updateField = append(updateField, t.AppTemplateId)
	updateField = append(updateField, t.DefaultValueMode)
	updateField = append(updateField, t.DefaultValue)
	updateField = append(updateField, t.CreatedBy)
	updateField = append(updateField, t.UpdatedBy)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllOemAppTemplateFunction error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OemAppTemplateFunction_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllOemAppTemplateFunction error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *OemAppTemplateFunctionSvc) UpdateFieldsOemAppTemplateFunction(req *proto.OemAppTemplateFunctionUpdateFieldsRequest) (*proto.OemAppTemplateFunction, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateFunction
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsOemAppTemplateFunction error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsOemAppTemplateFunction error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OemAppTemplateFunction_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsOemAppTemplateFunction error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找OemAppTemplateFunction
func (s *OemAppTemplateFunctionSvc) FindOemAppTemplateFunction(req *proto.OemAppTemplateFunctionFilter) (*proto.OemAppTemplateFunction, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateFunction
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.Name != "" { //字符串
		do = do.Where(t.Name.Eq(req.Name))
	}
	if req.NameEn != "" { //字符串
		do = do.Where(t.NameEn.Eq(req.NameEn))
	}
	if req.Code != "" { //字符串
		do = do.Where(t.Code.Eq(req.Code))
	}
	if req.Sort != 0 { //整数
		do = do.Where(t.Sort.Eq(req.Sort))
	}
	if req.AppTemplateId != 0 { //整数
		do = do.Where(t.AppTemplateId.Eq(req.AppTemplateId))
	}
	if req.DefaultValueMode != 0 { //整数
		do = do.Where(t.DefaultValueMode.Eq(req.DefaultValueMode))
	}
	if req.DefaultValue != "" { //字符串
		do = do.Where(t.DefaultValue.Eq(req.DefaultValue))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindOemAppTemplateFunction error : %s", err.Error())
		return nil, err
	}
	res := convert.OemAppTemplateFunction_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找OemAppTemplateFunction
func (s *OemAppTemplateFunctionSvc) FindByIdOemAppTemplateFunction(req *proto.OemAppTemplateFunctionFilter) (*proto.OemAppTemplateFunction, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateFunction
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdOemAppTemplateFunction error : %s", err.Error())
		return nil, err
	}
	res := convert.OemAppTemplateFunction_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找OemAppTemplateFunction,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *OemAppTemplateFunctionSvc) GetListOemAppTemplateFunction(req *proto.OemAppTemplateFunctionListRequest) ([]*proto.OemAppTemplateFunction, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateFunction
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.Name != "" { //字符串
			do = do.Where(t.Name.Like("%" + query.Name + "%"))
		}
		if query.NameEn != "" { //字符串
			do = do.Where(t.NameEn.Like("%" + query.NameEn + "%"))
		}
		if query.Code != "" { //字符串
			do = do.Where(t.Code.Like("%" + query.Code + "%"))
		}
		if query.Sort != 0 { //整数
			do = do.Where(t.Sort.Eq(query.Sort))
		}
		if query.AppTemplateId != 0 { //整数
			do = do.Where(t.AppTemplateId.Eq(query.AppTemplateId))
		}
		if query.DefaultValueMode != 0 { //整数
			do = do.Where(t.DefaultValueMode.Eq(query.DefaultValueMode))
		}
		if query.DefaultValue != "" { //字符串
			do = do.Where(t.DefaultValue.Like("%" + query.DefaultValue + "%"))
		}
		if query.CreatedBy != 0 { //整数
			do = do.Where(t.CreatedBy.Eq(query.CreatedBy))
		}
		if query.UpdatedBy != 0 { //整数
			do = do.Where(t.UpdatedBy.Eq(query.UpdatedBy))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TOemAppTemplateFunction
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListOemAppTemplateFunction error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.OemAppTemplateFunction, len(list))
	for i, v := range list {
		result[i] = convert.OemAppTemplateFunction_db2pb(v)
	}
	return result, total, nil
}
