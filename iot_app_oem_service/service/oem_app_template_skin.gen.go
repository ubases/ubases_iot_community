// Code generated by sgen.exe,2022-10-24 08:40:56. DO NOT EDIT.
// versions: v1.0.0
// 如果确实需要修改,请修改后改文件名,可以移除文件名中的.gen,以防下次生成覆盖

package service

import (
	"cloud_platform/iot_common/iotutil"
	"context"
	"errors"

	"go-micro.dev/v4/logger"
	"gorm.io/gen/field"

	"cloud_platform/iot_app_oem_service/convert"
	iotmodel "cloud_platform/iot_model"
	"cloud_platform/iot_model/db_app_oem/model"
	"cloud_platform/iot_model/db_app_oem/orm"
	proto "cloud_platform/iot_proto/protos/protosService"
)

type OemAppTemplateSkinSvc struct {
	Ctx context.Context
}

// CopyOemAppTemplateSkin
func (s *OemAppTemplateSkinSvc) CopyOemAppTemplateSkin(tx *orm.Query, appTemplateId, newAppTemplateId int64) error {
	// fixme 请在这里校验参数
	t := tx.TOemAppTemplateSkin
	do := t.WithContext(context.Background())

	list, err := do.Where(t.AppTemplateId.Eq(appTemplateId)).Find()
	if err != nil {
		return err
	}
	saveList := make([]*model.TOemAppTemplateSkin, 0)
	for _, function := range list {
		function.Id = iotutil.GetNextSeqInt64()
		function.AppTemplateId = newAppTemplateId
		saveList = append(saveList, function)
	}
	if len(saveList) == 0 {
		return nil
	}
	err = do.Create(saveList...)
	if err != nil {
		return err
	}
	return err
}

// 创建OemAppTemplateSkin
func (s *OemAppTemplateSkinSvc) CreateOemAppTemplateSkin(req *proto.OemAppTemplateSkin) (*proto.OemAppTemplateSkin, error) {
	// fixme 请在这里校验参数
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateSkin
	do := t.WithContext(context.Background())
	req.Id = iotutil.GetNextSeqInt64()
	dbObj := convert.OemAppTemplateSkin_pb2db(req)
	err := do.Create(dbObj)
	if err != nil {
		logger.Errorf("CreateOemAppTemplateSkin error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据条件删除OemAppTemplateSkin
func (s *OemAppTemplateSkinSvc) DeleteOemAppTemplateSkin(req *proto.OemAppTemplateSkin) (*proto.OemAppTemplateSkin, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateSkin
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.Name != "" { //字符串
		do = do.Where(t.Name.Eq(req.Name))
	}
	if req.NameEn != "" { //字符串
		do = do.Where(t.NameEn.Eq(req.NameEn))
	}
	if req.Remark != "" { //字符串
		do = do.Where(t.Remark.Eq(req.Remark))
	}
	if req.Code != "" { //字符串
		do = do.Where(t.Code.Eq(req.Code))
	}
	if req.PreviewImages != "" { //字符串
		do = do.Where(t.PreviewImages.Eq(req.PreviewImages))
	}
	if req.AppTemplateId != 0 { //整数
		do = do.Where(t.AppTemplateId.Eq(req.AppTemplateId))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteOemAppTemplateSkin error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键删除OemAppTemplateSkin
func (s *OemAppTemplateSkinSvc) DeleteByIdOemAppTemplateSkin(req *proto.OemAppTemplateSkin) (*proto.OemAppTemplateSkin, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateSkin
	do := t.WithContext(context.Background())
	// fixme 请检查条件

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	_, err := do.Delete()
	if err != nil {
		logger.Errorf("DeleteByIdOemAppTemplateSkin error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// 根据数据库表主键批量删除OemAppTemplateSkin
func (s *OemAppTemplateSkinSvc) DeleteByIdsOemAppTemplateSkin(req *proto.OemAppTemplateSkinBatchDeleteRequest) (*proto.OemAppTemplateSkinBatchDeleteRequest, error) {
	var err error
	for _, k := range req.Keys {
		t := orm.Use(iotmodel.GetDB()).TOemAppTemplateSkin
		do := t.WithContext(context.Background())

		do = do.Where(t.Id.Eq(k.Id))

		_, err = do.Delete()
		if err != nil {
			logger.Errorf("DeleteByIdsOemAppTemplateSkin error : %s", err.Error())
			break
		}
	}
	return req, err
}

// 根据主键更新OemAppTemplateSkin
func (s *OemAppTemplateSkinSvc) UpdateOemAppTemplateSkin(req *proto.OemAppTemplateSkin) (*proto.OemAppTemplateSkin, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateSkin
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	if req.Name != "" { //字符串
		updateField = append(updateField, t.Name)
	}
	if req.NameEn != "" { //字符串
		updateField = append(updateField, t.NameEn)
	}
	if req.Remark != "" { //字符串
		updateField = append(updateField, t.Remark)
	}
	if req.Code != "" { //字符串
		updateField = append(updateField, t.Code)
	}
	if req.PreviewImages != "" { //字符串
		updateField = append(updateField, t.PreviewImages)
	}
	if req.AppTemplateId != 0 { //整数
		updateField = append(updateField, t.AppTemplateId)
	}
	if req.CreatedBy != 0 { //整数
		updateField = append(updateField, t.CreatedBy)
	}
	if req.UpdatedBy != 0 { //整数
		updateField = append(updateField, t.UpdatedBy)
	}
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}

	if !HasPrimaryKey {
		logger.Error("UpdateOemAppTemplateSkin error : Missing condition")
		return nil, errors.New("Missing condition")
	}

	dbObj := convert.OemAppTemplateSkin_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateOemAppTemplateSkin error : %s", err.Error())
		return nil, err
	}
	return req, err
}

// //根据主键更新所有字段OemAppTemplateSkin
func (s *OemAppTemplateSkinSvc) UpdateAllOemAppTemplateSkin(req *proto.OemAppTemplateSkin) (*proto.OemAppTemplateSkin, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateSkin
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数
	//要更新的字段,不包括主键
	var updateField []field.Expr

	updateField = append(updateField, t.Name)
	updateField = append(updateField, t.NameEn)
	updateField = append(updateField, t.Remark)
	updateField = append(updateField, t.Code)
	updateField = append(updateField, t.PreviewImages)
	updateField = append(updateField, t.AppTemplateId)
	updateField = append(updateField, t.CreatedBy)
	updateField = append(updateField, t.UpdatedBy)
	if len(updateField) > 0 {
		do = do.Select(updateField...)
	}
	//主键条件
	HasPrimaryKey := false
	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateAllOemAppTemplateSkin error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OemAppTemplateSkin_pb2db(req)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateAllOemAppTemplateSkin error : %s", err.Error())
		return nil, err
	}
	return req, err
}

func (s *OemAppTemplateSkinSvc) UpdateFieldsOemAppTemplateSkin(req *proto.OemAppTemplateSkinUpdateFieldsRequest) (*proto.OemAppTemplateSkin, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateSkin
	do := t.WithContext(context.Background())

	var updateField []field.Expr
	for _, v := range req.Fields {
		col, ok := t.GetFieldByName(v)
		if ok {
			updateField = append(updateField, col)
		}
	}
	if len(updateField) == 0 {
		err := errors.New("UpdateFieldsOemAppTemplateSkin error : missing updateField")
		logger.Error(err)
		return nil, err
	}
	do = do.Select(updateField...)

	//主键条件
	HasPrimaryKey := false
	if req.Data.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Data.Id))
		HasPrimaryKey = true
	}
	if !HasPrimaryKey {
		logger.Error("UpdateFieldsOemAppTemplateSkin error : Missing condition")
		return nil, errors.New("Missing condition")
	}
	dbObj := convert.OemAppTemplateSkin_pb2db(req.Data)
	_, err := do.Updates(dbObj)
	if err != nil {
		logger.Errorf("UpdateFieldsOemAppTemplateSkin error : %s", err.Error())
		return nil, err
	}
	return req.Data, nil
}

// 根据非空条件查找OemAppTemplateSkin
func (s *OemAppTemplateSkinSvc) FindOemAppTemplateSkin(req *proto.OemAppTemplateSkinFilter) (*proto.OemAppTemplateSkin, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateSkin
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	if req.Name != "" { //字符串
		do = do.Where(t.Name.Eq(req.Name))
	}
	if req.NameEn != "" { //字符串
		do = do.Where(t.NameEn.Eq(req.NameEn))
	}
	if req.Remark != "" { //字符串
		do = do.Where(t.Remark.Eq(req.Remark))
	}
	if req.Code != "" { //字符串
		do = do.Where(t.Code.Eq(req.Code))
	}
	if req.PreviewImages != "" { //字符串
		do = do.Where(t.PreviewImages.Eq(req.PreviewImages))
	}
	if req.AppTemplateId != 0 { //整数
		do = do.Where(t.AppTemplateId.Eq(req.AppTemplateId))
	}
	if req.CreatedBy != 0 { //整数
		do = do.Where(t.CreatedBy.Eq(req.CreatedBy))
	}
	if req.UpdatedBy != 0 { //整数
		do = do.Where(t.UpdatedBy.Eq(req.UpdatedBy))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindOemAppTemplateSkin error : %s", err.Error())
		return nil, err
	}
	res := convert.OemAppTemplateSkin_db2pb(dbObj)
	return res, err
}

// 根据数据库表主键查找OemAppTemplateSkin
func (s *OemAppTemplateSkinSvc) FindByIdOemAppTemplateSkin(req *proto.OemAppTemplateSkinFilter) (*proto.OemAppTemplateSkin, error) {
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateSkin
	do := t.WithContext(context.Background())
	// fixme 请检查条件和校验参数

	if req.Id != 0 { //整数
		do = do.Where(t.Id.Eq(req.Id))
	}
	dbObj, err := do.First()
	if err != nil {
		logger.Errorf("FindByIdOemAppTemplateSkin error : %s", err.Error())
		return nil, err
	}
	res := convert.OemAppTemplateSkin_db2pb(dbObj)
	return res, err
}

// 根据分页条件查找OemAppTemplateSkin,请确保req.Query的结构字段与数据表model结构体字段保持一致，否则会有编译问题
func (s *OemAppTemplateSkinSvc) GetListOemAppTemplateSkin(req *proto.OemAppTemplateSkinListRequest) ([]*proto.OemAppTemplateSkin, int64, error) {
	// fixme 请检查条件和校验参数
	var err error
	t := orm.Use(iotmodel.GetDB()).TOemAppTemplateSkin
	do := t.WithContext(context.Background())
	query := req.Query
	if query != nil {

		if query.Id != 0 { //整数
			do = do.Where(t.Id.Eq(query.Id))
		}
		if query.Name != "" { //字符串
			do = do.Where(t.Name.Like("%" + query.Name + "%"))
		}
		if query.NameEn != "" { //字符串
			do = do.Where(t.NameEn.Like("%" + query.NameEn + "%"))
		}
		if query.Remark != "" { //字符串
			do = do.Where(t.Remark.Like("%" + query.Remark + "%"))
		}
		if query.Code != "" { //字符串
			do = do.Where(t.Code.Like("%" + query.Code + "%"))
		}
		if query.PreviewImages != "" { //字符串
			do = do.Where(t.PreviewImages.Like("%" + query.PreviewImages + "%"))
		}
		if query.AppTemplateId != 0 { //整数
			do = do.Where(t.AppTemplateId.Eq(query.AppTemplateId))
		}
		if query.CreatedBy != 0 { //整数
			do = do.Where(t.CreatedBy.Eq(query.CreatedBy))
		}
		if query.UpdatedBy != 0 { //整数
			do = do.Where(t.UpdatedBy.Eq(query.UpdatedBy))
		}
	}
	orderCol, ok := t.GetFieldByName(req.OrderKey)
	if !ok {
		orderCol = t.Id
	}
	if req.OrderDesc != "" {
		do = do.Order(orderCol.Desc())
	} else {
		do = do.Order(orderCol)
	}

	var list []*model.TOemAppTemplateSkin
	var total int64
	if req.PageSize > 0 {
		limit := req.PageSize
		if req.Page == 0 {
			req.Page = 1
		}
		offset := req.PageSize * (req.Page - 1)
		list, total, err = do.FindByPage(int(offset), int(limit))
	} else {
		list, err = do.Find()
		total = int64(len(list))
	}
	if err != nil {
		logger.Errorf("GetListOemAppTemplateSkin error : %s", err.Error())
		return nil, 0, err
	}
	if len(list) == 0 {
		return nil, total, nil
	}
	result := make([]*proto.OemAppTemplateSkin, len(list))
	for i, v := range list {
		result[i] = convert.OemAppTemplateSkin_db2pb(v)
	}
	return result, total, nil
}
